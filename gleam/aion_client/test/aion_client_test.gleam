//// Test runner entry point: gleeunit discovers every `*_test` function in
//// the test modules (example coverage and the live conformance harness).

import gleeunit

pub fn main() {
  gleeunit.main()
}
