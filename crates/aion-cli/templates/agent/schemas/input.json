{
  "$comment": "Generated by aion generate from src/{{name}}_io.gleam — do not edit; run `aion generate`.",
  "type": "object",
  "required": ["task_id", "scout_prompt", "act_prompt", "verify_prompt", "review_timeout_ms"],
  "additionalProperties": false,
  "properties": {
    "task_id": { "type": "string" },
    "scout_prompt": { "type": "string" },
    "act_prompt": { "type": "string" },
    "verify_prompt": { "type": "string" },
    "review_timeout_ms": { "type": "integer" }
  }
}
