{
  "$comment": "Generated by aion generate from src/{{name}}_io.gleam — do not edit; run `aion generate`.",
  "type": "object",
  "required": ["workspace", "brief", "design", "checklist", "stories", "verify_fix_cap", "round_backoff_ms"],
  "additionalProperties": false,
  "properties": {
    "workspace": {
      "type": "object",
      "required": ["path", "branch", "placement", "isolation"],
      "additionalProperties": false,
      "properties": {
        "path": { "type": "string" },
        "branch": { "type": "string" },
        "placement": { "type": "string", "enum": ["local", "remote"] },
        "isolation": { "type": "string", "enum": ["worktree", "copy", "overlay", "vm"] }
      }
    },
    "brief": { "type": "string" },
    "design": { "type": "string" },
    "checklist": { "type": "string" },
    "stories": { "type": "array", "items": { "type": "string" } },
    "verify_fix_cap": { "type": "integer" },
    "round_backoff_ms": { "type": "integer" }
  }
}
