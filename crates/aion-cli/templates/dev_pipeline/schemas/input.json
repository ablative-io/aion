{
  "$comment": "Generated by aion generate from src/{{name}}_io.gleam — do not edit; run `aion generate`.",
  "type": "object",
  "required": ["repo_root", "brief_id", "reviewers", "base_ref", "placement", "isolation", "brief", "design", "checklist", "stories", "verify_fix_cap", "review_cap", "round_backoff_ms", "review_deadline_ms"],
  "additionalProperties": false,
  "properties": {
    "repo_root": { "type": "string" },
    "brief_id": { "type": "string" },
    "reviewers": { "type": "array", "items": { "type": "string" } },
    "base_ref": { "type": "string" },
    "placement": { "type": "string", "enum": ["local", "remote"] },
    "isolation": { "type": "string", "enum": ["worktree", "copy", "overlay", "vm"] },
    "brief": { "type": "string" },
    "design": { "type": "string" },
    "checklist": { "type": "string" },
    "stories": { "type": "array", "items": { "type": "string" } },
    "verify_fix_cap": { "type": "integer" },
    "review_cap": { "type": "integer" },
    "round_backoff_ms": { "type": "integer" },
    "review_deadline_ms": { "type": "integer" }
  }
}
