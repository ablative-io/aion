name = "nested_chain"
version = "0.1.0"
target = "erlang"
description = "Engine e2e fixture: a three-level child-workflow chain plus a self-spawning recursion probe."
licences = ["MIT"]

[dependencies]
aion_flow = { path = "../../../../../gleam/aion_flow" }
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_json = ">= 2.0.0 and < 4.0.0"
