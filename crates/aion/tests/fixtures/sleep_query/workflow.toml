# Engine e2e fixture descriptor consumed by the from-source test gates.

[[workflow]]
entry_module = "sleep_query"
entry_function = "run"
timeout_seconds = 30
input_schema = "schemas/input.json"
output_schema = "schemas/output.json"
activities = []
output = "sleep-query.aion"
