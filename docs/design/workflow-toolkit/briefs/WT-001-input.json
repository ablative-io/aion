{
  "repo_root": "/Users/tom/Developer/ablative/aion",
  "brief_id": "WT-001",
  "reviewers": [
    "Waffles the Terrible"
  ],
  "base_ref": "main",
  "placement": "local",
  "isolation": "worktree",
  "brief_document": {
    "id": "WT-001",
    "cluster": "workflow-toolkit",
    "title": "Reshape brief_dev to ADR-012: facts in the workflow, sealed reports through it, rendering on the worker",
    "depends_on": [],
    "blocked_by": [
      "RM-022 (aion_kit: the opaque payload seal/raw/peek primitive, the aion_flow pass-through type, and the template/json primitives this brief consumes) must land first"
    ],
    "checklist": [
      "C1",
      "C2",
      "C3",
      "C4",
      "C5",
      "C6",
      "C7",
      "C8",
      "C9",
      "C10",
      "C11",
      "C12",
      "C13",
      "C14",
      "C15",
      "C16",
      "C17",
      "C27",
      "C28"
    ],
    "stories": [
      "S3",
      "S5",
      "S6",
      "S7",
      "S8",
      "S10",
      "S12",
      "S13",
      "S14"
    ],
    "design_anchor": [
      "ADR-012",
      "ADR-008",
      "ADR-002",
      "ADR-011"
    ],
    "purpose": "Makes the deterministic workflow process thin (ADR-012). brief_dev currently decodes full ScoutReport/DevReport/ReviewReport and renders every stage prompt inside workflow code \u2014 heavy work re-executed on every replay (engine invariant 2) and the exact regression that crashed a real dogfood run with a silent heap-full. This brief reshapes the inner data flow: the four agentic activities return a small facts projection PLUS the full report sealed as an opaque aion_flow payload; the workflow threads sealed payloads and reads only facts; prompt rendering and report decoding move into the activity bodies on the worker (built on aion_kit's template + payload primitives). The agents' output is unchanged (ADR-012 quote, CN5) and the outer stacked-dev arc stays byte-immutable (CN1, ADR-008) \u2014 only where decoding and rendering happen moves. After the reshape the TK-002 dogfood must still run end to end through real norn (CN9).",
    "task": "Reshape examples/stacked-dev so brief_dev.gleam holds facts, not reports, and renders no prompts. SCOPE IN: the facts types and codecs (facts.gleam), the sealed-payload activity result reshape (activities.gleam, types.gleam, codecs_core.gleam, codecs_workflows.gleam), the worker-side render path (render.gleam + worker/src/render.rs built on the aion_kit template primitive), the agentic locals/handlers reshaped to render-and-seal and the enrich_brief activity reshaped to decode the sealed payloads, the brief_dev.gleam reshape itself, the brief_dev_output.json schema update + codegen regen, the two outer-arc consumption sites in stacked_dev.gleam, and the deletion of prompts.gleam with its five projection test modules (their pins move to render_test.gleam). SCOPE OUT: lifting the yg/cargo/git harness into a reusable module (WT-002) and lifting the control skeleton into a parameterised pipeline (WT-003) \u2014 this brief reshapes brief_dev in place; the harness locals (provision/warm/scoped/full/land) and the brief_dev control flow stay where they are, only their data shapes adjust. DEPENDENCY: blocked on RM-022 \u2014 this brief uses aion_kit's opaque payload (seal to wrap a report, raw to unwrap it worker-side) and the aion_flow pass-through payload type a workflow can hold without decoding, plus aion_kit's template primitive for the render path; add the aion_kit dependency to gleam.toml and the Rust aion-kit crate to worker/Cargo.toml as part of R1. THE FACTS RULE (CN4, P3): the facts a type carries are exactly the values the pre-reshape workflow read off a full report \u2014 DevFacts.blocked_requirement_ids (was dev_report.enrichments[].status == blocked), DevFacts.changed_files (was the deduplicated files_changed[].path), ReviewFacts.drifted (was enrichments[].alignment == drifted, with issues), ReviewFacts.has_fixes (was any enrichment fixes non-empty); scout produced no routing fact beyond success, so ScoutFacts is a success marker. No new routing signal is invented and none is dropped \u2014 the reviewer checks each fact field against the brief_dev.gleam control flow that read it. OUTER-ARC NOTE (CN1): stacked_dev.gleam derives its DevResult from brief_dev_result.dev today by reading a full DevReport (changed_files + summary); after the reshape it reads the carried facts (changed_files) and the carried dev summary fact instead, and threads the sealed reports straight to enrich_brief \u2014 the provision/gate/review/land/signal/spawn sites are not touched. CONTROL-FLOW NOTE (C13): the stage flow, the seven status phases, the bounded verify-fix loop with required verify_fix_cap/round_backoff_ms, and the six-variant BriefDevError are preserved exactly as RM-017 landed them; this brief changes the data the loop carries, not the loop.",
    "requirements": [
      {
        "id": "R1",
        "title": "aion_kit dependency and the facts types with codecs",
        "spec": "Structural. Add aion_kit to examples/stacked-dev/gleam.toml and the aion-kit Rust crate to examples/stacked-dev/worker/Cargo.toml (the package and worker consume RM-022's payload + template surface). Create src/stacked_dev/facts.gleam defining: DevFacts(blocked_requirement_ids: List(String), changed_files: List(String), summary: String); ReviewFacts(drifted: List(DriftedRequirement), has_fixes: Bool); ScoutFacts(ok: Bool). Each gets a hand-written codec that round-trips losslessly with deterministic encoding. DriftedRequirement is reused from types.gleam (id, issues) \u2014 do not redefine it. WHEN a facts codec encodes a value, THE SYSTEM SHALL emit the same canonical key order on every call (replay-stable), and SHALL NOT include any field of the full stage report beyond the projected facts. THE SYSTEM SHALL NOT add a facts field that the pre-reshape brief_dev did not read off a report (CN4): the summary on DevFacts is the one value the outer arc's DevResult derivation reads, carried as a fact precisely so the workflow needs no full report.",
        "acceptance": [
          "src/stacked_dev/facts.gleam defines exactly DevFacts, ReviewFacts, ScoutFacts with the fields named in the spec and no others.",
          "DevFacts.changed_files and DevFacts.blocked_requirement_ids correspond to the changed_files(dev_report) dedup and the enrichments[].status == blocked filter brief_dev.gleam performs today (the reviewer diffs the old workflow logic against the new fact derivation).",
          "Each facts codec round-trips a constructed value to an equal value, and re-encoding the decoded value yields a byte-identical string (deterministic key order).",
          "examples/stacked-dev/gleam.toml lists aion_kit and examples/stacked-dev/worker/Cargo.toml lists the aion-kit crate.",
          "grep for ScoutReport, DevReport, or ReviewReport in src/stacked_dev/facts.gleam returns no matches \u2014 the facts module references no full-report type."
        ],
        "files": {
          "create": [
            "examples/stacked-dev/src/stacked_dev/facts.gleam"
          ],
          "modify": [
            "examples/stacked-dev/gleam.toml",
            "examples/stacked-dev/worker/Cargo.toml"
          ],
          "delete": []
        },
        "checklist": [
          "C1",
          "C2"
        ],
        "stories": [
          "S3",
          "S12"
        ]
      },
      {
        "id": "R2",
        "title": "Sealed-payload activity results and their codecs",
        "spec": "Reshape the four agentic activities so each returns its facts paired with the full report sealed as an opaque aion_flow pass-through payload. In src/stacked_dev/types.gleam: add ScoutOutcome(facts: ScoutFacts, report: SealedPayload), DevOutcome(facts: DevFacts, report: SealedPayload), ReviewOutcome(facts: ReviewFacts, report: SealedPayload) where SealedPayload is aion_kit's opaque pass-through type; reshape StartupResult's Developed variant to Developed(dev_outcome: DevOutcome). In src/stacked_dev/activities.gleam: scout returns ScoutOutcome, dev_resume returns DevOutcome, dev_review returns ReviewOutcome, and dev (through the startup envelope) returns Developed(DevOutcome) \u2014 each binds the new outcome codec, NOT the generated full-report json_codec. In src/stacked_dev/codecs_core.gleam: add scout_outcome_codec, dev_outcome_codec, review_outcome_codec composing the facts codecs with aion_kit's payload codec; update startup_result_codec for the reshaped Developed. WHEN an outcome codec encodes, THE SYSTEM SHALL carry the sealed report as the opaque payload bytes without decoding them, and WHEN it decodes, THE SYSTEM SHALL leave the payload sealed (no raw/peek). THE SYSTEM SHALL NOT bind the generated scout_report/dev_report/review_report json_codec as any activity's output codec anymore \u2014 the report rides sealed. THE SYSTEM SHALL NOT alter the codecs or names of provision_workspace, warm_build, scoped_checks, full_checks, request_review, land, or assemble_wave (CN1).",
        "acceptance": [
          "scout's output type is ScoutOutcome, dev_resume's is DevOutcome, dev_review's is ReviewOutcome, and StartupResult.Developed carries a DevOutcome.",
          "grep shows no activity definition in activities.gleam binding codec.json_codec over stage_io.scout_report_decoder/dev_report_decoder/review_report_decoder \u2014 the generated report json_codecs are no longer activity output codecs.",
          "scout_outcome_codec, dev_outcome_codec, and review_outcome_codec round-trip a constructed outcome (facts + a sealed payload built from a schema-valid report) to an equal value with the payload still sealed.",
          "The eight non-agentic activity name constants and their codec bindings (provision_workspace, warm_build, scoped_checks, full_checks, request_review, land, enrich_brief, assemble_wave) are byte-identical to their pre-brief values."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev/types.gleam",
            "examples/stacked-dev/src/stacked_dev/activities.gleam",
            "examples/stacked-dev/src/stacked_dev/codecs_core.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C4"
        ],
        "stories": [
          "S3"
        ]
      },
      {
        "id": "R3",
        "title": "Worker-side render path on the aion_kit template primitive",
        "spec": "Move prompt rendering out of the workflow and into the worker. Create src/stacked_dev/render.gleam exposing scout_render, dev_render, review_render, and resume_render \u2014 pure functions of the brief document, resolved context, and the decoded prior-stage report(s), built on aion_kit's template primitive rather than re-implemented string assembly. Create worker/src/render.rs as the Rust mirror for the deployed worker. THE SYSTEM SHALL preserve the projection discipline of the retired prompts.gleam (C10): anchored ADRs as 'id: title \u2014 decision' lines, verbatim quotes with speaker attribution, requirements with inline resolved C#/S# texts, boundaries in every stage prompt; the dev render inlines scout findings per requirement; the review render renders the dev attestation and the measured check result as two labelled sections but NEVER the scout (ADR-010). render.gleam SHALL NOT import aion/workflow, aion/activity, aion/query, aion/signal, or any CLI/locals module \u2014 it is pure, called only from locals on the worker. THE SYSTEM SHALL NOT leave a second rendering path in workflow code: rendering exists only here and in worker/src/render.rs.",
        "acceptance": [
          "render.gleam exposes scout_render, dev_render, review_render, resume_render as pure functions; grep -E 'import (aion/(workflow|activity|query|signal)|stacked_dev/(cli|locals|activities))' src/stacked_dev/render.gleam returns no matches.",
          "review_render renders a labelled dev-attestation section and a labelled measured-checks section and does not render any scout block (ADR-010): grep confirms no scout-block call in review_render.",
          "dev_render inlines the scout finding for each requirement (the fixture's R1 scout approach sentinel appears within the R1 section).",
          "render.gleam composes prompts via the aion_kit template primitive (grep shows the aion_kit template import and usage), not bespoke concatenation duplicating prompts.gleam.",
          "worker/src/render.rs exists and exposes the same four render entrypoints the handlers call."
        ],
        "files": {
          "create": [
            "examples/stacked-dev/src/stacked_dev/render.gleam",
            "examples/stacked-dev/worker/src/render.rs"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C8",
          "C10"
        ],
        "stories": [
          "S10",
          "S13",
          "S14"
        ]
      },
      {
        "id": "R4",
        "title": "Agentic locals render-and-seal; enrich_brief decodes the sealed payloads",
        "spec": "In src/stacked_dev/locals.gleam reshape the four agentic locals. WHEN locals.scout / locals.dev / locals.dev_review / locals.dev_resume run, THE SYSTEM SHALL build the stage prompt by calling the matching render.gleam function (from the brief document, resolved context, and the decoded prior-stage payload(s) the input now carries), invoke norn exactly as before (same argv, same deterministic session ids '<branch>-scout' / '<branch>' / '<branch>-review', CN1), decode the full report from norn stdout (the existing require_report bare-or-{output} seam), project the facts from it, seal the full report with aion_kit, and return the (facts, sealed payload) outcome. THE SYSTEM SHALL NOT accept a pre-rendered prompt string on the activity input anymore \u2014 the input carries the document/context/prior payload and the local renders (the rendering crossed from workflow to worker). Reshape locals.enrich_brief: it now receives the sealed scout/dev/review payloads, unwraps each with aion_kit payload.raw + the report decoder, and merges the decoded full reports into the brief exactly as it does today (the CN3 authored-divergence guard and the append-only merge are unchanged). THE SYSTEM SHALL NOT decode a full report anywhere outside enrich_brief and the facts-projection step inside the four agentic locals \u2014 no workflow module decodes a report (CN2). worker/src/handlers.rs mirrors every reshaped local invocation-for-invocation (render, seal, project facts; enrich_brief unwraps).",
        "acceptance": [
          "Each agentic local builds its prompt via the corresponding render.gleam function (grep shows render.scout_render etc. called in locals), and no agentic local takes a pre-rendered prompt: ScoutInput/ReviewInput/DevInput/ResumeInput carry the document/context/prior payloads, not a prompt string.",
          "The norn argv and session ids in the four agentic locals are byte-identical to their pre-brief values (--print, --session-id <branch>[-scout|-review] / --resume, --workspace-root, --output-schema, --output-format json) \u2014 the reviewer diffs the argv lists.",
          "Each agentic local seals the decoded full report and returns the facts projected from it; enrich_brief unwraps the sealed payloads with aion_kit payload.raw and merges the decoded reports.",
          "grep for stage_io report decoders across all src/ modules shows them used only inside the four agentic locals (facts projection) and locals.enrich_brief (merge) \u2014 no workflow module (brief_dev.gleam, stacked_dev.gleam) decodes a report.",
          "worker/src/handlers.rs mirrors the reshaped scout/dev/dev_review/dev_resume/enrich_brief handlers (render-and-seal; unwrap-and-merge)."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev/locals.gleam",
            "examples/stacked-dev/worker/src/handlers.rs",
            "examples/stacked-dev/worker/src/types.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C3",
          "C7",
          "C8"
        ],
        "stories": [
          "S10",
          "S13"
        ]
      },
      {
        "id": "R5",
        "title": "Reshape brief_dev.gleam to thread sealed payloads and read only facts",
        "spec": "Reshape src/brief_dev.gleam (ADR-012, CN2). THE SYSTEM SHALL NOT import stacked_dev/prompts (deleted) or stacked_dev/render (worker-side), and SHALL NOT decode or field-access any ScoutReport, DevReport, or ReviewReport. WHEN scout returns, THE SYSTEM SHALL hold its ScoutOutcome (facts + sealed report) and route only on ScoutFacts. WHEN the startup fan-out returns, THE SYSTEM SHALL read DevFacts.blocked_requirement_ids to fail DevBlocked, and DevFacts.changed_files to drive scoped_checks \u2014 never a decoded report. WHILE verifying, THE SYSTEM SHALL run the bounded verify-fix loop exactly as today (scoped_checks over DevFacts.changed_files; on CheckFail, round >= verify_fix_cap fails VerifyFixExhausted, else a durable round_backoff_ms sleep then dev_resume, recursing with round+1) \u2014 the loop, the seven status phases (scouting, developing, verifying, fixing, reviewing, hardening, converged), and the six BriefDevError variants are unchanged (C13). WHEN dev_review returns, THE SYSTEM SHALL read ReviewFacts.drifted to fail ReviewDrifted (evaluated BEFORE the harden re-verify) and ReviewFacts.has_fixes to decide the harden re-run; on harden CheckFail it fails HardenRegressed. WHEN converged, THE SYSTEM SHALL return the reshaped BriefDevResult carrying the three sealed payloads and the facts the outer arc needs. THE SYSTEM SHALL NOT dispatch any activity other than scout, warm_build, dev, scoped_checks, dev_resume, dev_review, and SHALL NOT impose any cap/backoff/deadline beyond the two required inputs.",
        "acceptance": [
          "grep -E 'import .*stacked_dev/(prompts|render)' examples/stacked-dev/src/brief_dev.gleam returns no matches.",
          "grep for .enrichments, scout_report_decoder, dev_report_decoder, review_report_decoder, or any ScoutReport/DevReport/ReviewReport field access in brief_dev.gleam returns no matches \u2014 the workflow reads facts only (CN2, C5).",
          "The drift check (ReviewFacts.drifted -> ReviewDrifted) is evaluated before the has_fixes harden re-verify, preserving the pre-reshape ordering.",
          "grep -o 'set_status(\"[a-z]*\"' brief_dev.gleam yields exactly scouting, developing, verifying, fixing, reviewing, hardening, converged; workflow.sleep appears exactly once with duration.milliseconds(input.round_backoff_ms).",
          "The only activities.* calls in brief_dev.gleam are scout, warm_build, dev, scoped_checks, dev_resume, dev_review."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/brief_dev.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C5",
          "C12",
          "C13",
          "C14"
        ],
        "stories": [
          "S3",
          "S7"
        ]
      },
      {
        "id": "R6",
        "title": "Reshape BriefDevResult, the output schema, and regenerate the IO module",
        "spec": "Reshape the brief_dev result to carry sealed reports plus facts. In src/stacked_dev/types.gleam: BriefDevResult becomes BriefDevResult(scout: SealedPayload, dev: SealedPayload, review: SealedPayload, dev_facts: DevFacts, review_facts: ReviewFacts, verify_rounds: Int, build_warm: BuildWarm) \u2014 the three sealed payloads feed enrich_brief, the facts feed the outer arc's DevResult derivation, so the parent never decodes a report (C6, C17). In src/stacked_dev/codecs_workflows.gleam: update brief_dev_result_codec to compose the aion_kit payload codec for the three sealed reports with the facts codecs and the two scalars; brief_dev_input_codec is unchanged. Update schemas/brief_dev_output.json to the reshaped result inside the codegen v1 subset (no $ref/$defs/oneOf/anyOf/allOf/const/default/type-unions; additionalProperties false throughout) \u2014 the three sealed-report fields are opaque payload objects per aion_kit's payload schema shape, the facts fields mirror the facts types. Re-run aion codegen so src/aion_stacked_dev_io.gleam reflects the new output schema; the regenerated module SHALL NOT be hand-edited. THE SYSTEM SHALL NOT change schemas/brief_dev_input.json (the input is unchanged).",
        "acceptance": [
          "BriefDevResult has exactly the fields scout, dev, review, dev_facts, review_facts, verify_rounds, build_warm; scout/dev/review are SealedPayload, not full report types.",
          "brief_dev_result_codec round-trips a constructed BriefDevResult (sealed payloads built from schema-valid reports + facts) to an equal value with payloads still sealed.",
          "python3 -c \"import json; s=json.load(open('examples/stacked-dev/schemas/brief_dev_output.json')); assert s['additionalProperties'] is False and sorted(s['required'])==sorted(['scout','dev','review','dev_facts','review_facts','verify_rounds','build_warm'])\" exits 0.",
          "grep -F '\"$ref\"' and grep -F '\"oneOf\"' over examples/stacked-dev/schemas/*.json produce no matches.",
          "cargo run -p aion-cli -- codegen examples/stacked-dev --check exits 0 and src/aion_stacked_dev_io.gleam reflects the reshaped output schema."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev/types.gleam",
            "examples/stacked-dev/src/stacked_dev/codecs_workflows.gleam",
            "examples/stacked-dev/schemas/brief_dev_output.json",
            "examples/stacked-dev/src/aion_stacked_dev_io.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C6",
          "C15"
        ],
        "stories": [
          "S3"
        ]
      },
      {
        "id": "R7",
        "title": "Adjust the two outer-arc consumption sites without touching the contracts",
        "spec": "In src/stacked_dev.gleam adjust ONLY the two sites that consume the brief_dev child result (CN1, C16, C17). The dev_result_of derivation SHALL build the outer DevResult (session_id, files_touched, summary) from the reshaped BriefDevResult's carried facts \u2014 files_touched from dev_facts.changed_files, summary from dev_facts.summary \u2014 and SHALL NOT decode a full DevReport. enrich_then_land SHALL pass the three carried sealed payloads (scout, dev, review) straight to the enrich_brief activity, which unwraps them worker-side (R4). THE SYSTEM SHALL NOT change provision_workspace, the gate child spawn, request_review, the review_verdict signal/receive, land, the StartupTask/StartupResult envelope, or the brief_dev spawn_and_wait shape \u2014 those sites are byte-identical pre- and post-brief (CN1). IF the execution block's attestation is needed, THE SYSTEM SHALL source it from the sealed dev report via the enrich_brief worker path, not by decoding the report in the workflow.",
        "acceptance": [
          "dev_result_of reads dev_facts.changed_files and dev_facts.summary; grep shows no DevReport field access (.enrichments, .summary on a report, changed_files(dev_report)) in stacked_dev.gleam.",
          "enrich_then_land threads the three sealed payloads to enrich_brief; the workflow decodes no report.",
          "The provision/gate-spawn/request_review/review-signal/land/startup-envelope/brief_dev-spawn sites in stacked_dev.gleam are byte-identical to their pre-brief text (the reviewer diffs each block).",
          "The review_signal_name, status_query_name, and the workflow.spawn_and_wait(brief_dev.workflow_type, ...) call are unchanged."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C16",
          "C17"
        ],
        "stories": [
          "S8"
        ]
      },
      {
        "id": "R8",
        "title": "Delete prompts.gleam, move the pins to render_test, add facts/sealed-payload tests",
        "spec": "Delete src/stacked_dev/prompts.gleam and its five projection test modules (prompts_render_test, prompts_scout_test, prompts_dev_test, prompts_review_test, prompts_resume_test) \u2014 the in-workflow projection is retired, no second rendering path remains (ADR-002, C9). Create test/render_test.gleam carrying the moved budget-and-content pins against the seeded fixture (docs/design/brief-dev/briefs/BD-001.json via the existing prompt_fixture support): scout render <= 6000, dev render <= 9000, review render <= 12000 characters, the ADR/quote/boundary/requirement content assertions, and the ADR-010 no-scout-in-review assertion. Create test/facts_codecs_test.gleam: round-trip and wire-pin tests for the facts codecs (DevFacts, ReviewFacts, ScoutFacts) and the three outcome codecs and brief_dev_result_codec (sealed payloads stay sealed across the round-trip). Update test/aion_stacked_dev_test.gleam and test/support/shims.gleam for the facts/sealed-payload flow: the shims emit the report the worker seals, and the hermetic stage-outcome assertions read facts where they previously read decoded reports \u2014 the stage outcomes (scout failure, dev block, verify exhaustion, review drift, harden regression, happy path) are unchanged in meaning (C13). THE SYSTEM SHALL NOT invent report shapes outside the stage-contract schemas.",
        "acceptance": [
          "src/stacked_dev/prompts.gleam and the five prompts_*_test.gleam modules do not exist; grep -rl 'stacked_dev/prompts' examples/stacked-dev/src examples/stacked-dev/test returns nothing.",
          "test/render_test.gleam exists and contains the budget pins 6000, 9000, 12000 and the ADR-010 assertion that the review render contains no scout block.",
          "test/facts_codecs_test.gleam round-trips each facts type and each outcome codec; a BriefDevResult round-trips with its three payloads still sealed.",
          "cd examples/stacked-dev && gleam test runs render_test, facts_codecs_test, and the updated aion_stacked_dev_test and all pass.",
          "The hermetic suite still covers scout failure, dev block, verify exhaustion, review drift unfixed, review fixes with passing re-verify, harden regression, and the happy path \u2014 the same stage outcomes as before the reshape."
        ],
        "files": {
          "create": [
            "examples/stacked-dev/test/render_test.gleam",
            "examples/stacked-dev/test/facts_codecs_test.gleam"
          ],
          "modify": [
            "examples/stacked-dev/test/aion_stacked_dev_test.gleam",
            "examples/stacked-dev/test/support/shims.gleam"
          ],
          "delete": [
            "examples/stacked-dev/src/stacked_dev/prompts.gleam",
            "examples/stacked-dev/test/prompts_render_test.gleam",
            "examples/stacked-dev/test/prompts_scout_test.gleam",
            "examples/stacked-dev/test/prompts_dev_test.gleam",
            "examples/stacked-dev/test/prompts_review_test.gleam",
            "examples/stacked-dev/test/prompts_resume_test.gleam"
          ]
        },
        "checklist": [
          "C9",
          "C11"
        ],
        "stories": [
          "S7"
        ]
      },
      {
        "id": "R9",
        "title": "Wire-compat pins and the re-dogfood through real norn",
        "spec": "Update examples/stacked-dev/worker/tests/wire_compat.rs to pin every facts type and every sealed-payload outcome envelope (ScoutOutcome, DevOutcome, ReviewOutcome, the reshaped Developed, the reshaped BriefDevResult) byte-compatible BOTH directions against the Gleam codecs; update worker/tests/handlers_shims.rs for the render-and-seal handler shape. Then re-run the TK-002 dogfood through real norn against the aion repo (CN9, S5): deploy all four archives, dispatch the brief, and assert the pipeline scouts/develops/verifies/reviews/hardens and lands on main with the enriched brief in the merge \u2014 with no workflow-process heap exhaustion. WHILE the run is in review, THE SYSTEM SHALL survive a SIGKILL of the server and resume without re-executing completed activities (S6). THE SYSTEM SHALL NOT mark this brief complete on a green type-check alone \u2014 the dogfood landing is the proof the reshape works (the run is the verification, mirroring brief-dev's all-norn goal).",
        "acceptance": [
          "wire_compat.rs pins DevFacts, ReviewFacts, ScoutFacts, ScoutOutcome, DevOutcome, ReviewOutcome, and the reshaped BriefDevResult byte-identical both directions; cargo test in worker/ passes.",
          "The TK-002 dogfood run lands on main: the merged commit contains the enriched brief file (per-R# scout/dev/review blocks and the execution block), and the server logs show no heap-exhaustion crash in the workflow process.",
          "A SIGKILL of the server mid-review, followed by restart and the review_verdict signal, resumes the run to land with zero re-executed activities (verified against the event history).",
          "The dogfood's enriched brief validates against brief.schema.json (python3 validate.py on the landed brief)."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/worker/tests/wire_compat.rs",
            "examples/stacked-dev/worker/tests/handlers_shims.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C27",
          "C28"
        ],
        "stories": [
          "S5",
          "S6"
        ]
      }
    ],
    "boundaries": [
      "SHALL NOT lift the yg/cargo/git harness (provision/warm/scoped/full/land) into a reusable module \u2014 that is WT-002; this brief reshapes their data shapes in place only where the facts/sealed-payload flow requires, leaving their CLI invocations byte-unchanged.",
      "SHALL NOT lift the brief_dev control skeleton into a parameterised pipeline module \u2014 that is WT-003; brief_dev.gleam stays a concrete workflow body in this brief.",
      "SHALL NOT alter any OUTER stacked-dev contract \u2014 provision/gate/review/land argv and sequencing, the StartupTask/StartupResult envelope, the review_verdict signal, or the brief_dev spawn_and_wait shape (CN1, ADR-008). The only stacked_dev.gleam edits are the two child-result consumption sites (R7).",
      "SHALL NOT change the stage-contract schemas (scout/dev/review report) or any file under docs/design-system/ \u2014 the agents' output is unchanged (CN5, ADR-012 quote); only where reports are decoded moves. The drift gate must still pass.",
      "SHALL NOT introduce any default cap, backoff, deadline, retry, or timeout \u2014 verify_fix_cap and round_backoff_ms remain the only bounds in brief_dev.",
      "SHALL NOT keep both a workflow-side and a worker-side rendering path: prompts.gleam is deleted, not deprecated (ADR-002).",
      "SHALL NOT lift the norn agent driver into aion_kit \u2014 it stays worker-side (ADR-011); this brief only consumes aion_kit's payload and template primitives.",
      "SHALL NOT hand-edit src/aion_stacked_dev_io.gleam (regenerated by codegen) and SHALL NOT touch dispatch.gleam, gate.gleam, or assemble_wave (out of the thin-workflow scope)."
    ],
    "verification": [
      "Confirm RM-022 (aion_kit) has landed and the aion_kit payload (seal/raw/peek), the aion_flow pass-through type, and the template primitive are available before dispatching this brief (blocked_by).",
      "grep -rn 'stacked_dev/prompts' examples/stacked-dev/src examples/stacked-dev/test returns nothing, and src/stacked_dev/prompts.gleam plus the five prompts_*_test.gleam files do not exist.",
      "grep in brief_dev.gleam for ScoutReport/DevReport/ReviewReport field access, .enrichments, and the report decoders returns nothing \u2014 the workflow reads facts only (CN2).",
      "Diff the provision/gate-spawn/request_review/review-signal/land/startup-envelope/brief_dev-spawn blocks in stacked_dev.gleam against their pre-brief text: byte-identical (CN1).",
      "cargo run -p aion-cli -- codegen examples/stacked-dev --check exits 0; bash examples/stacked-dev/scripts/check-schema-drift.sh exits 0 (stage contracts untouched, CN5).",
      "cd examples/stacked-dev && gleam build && gleam test exit 0 (render_test, facts_codecs_test, and the updated hermetic suite run); cd worker && cargo test passes including wire_compat.",
      "cargo fmt --check and cargo clippy --workspace --all-targets -- -D warnings pass clean; no #[allow]/unwrap/expect/panic introduced; no file over 500 code lines (split render/facts if needed).",
      "The TK-002 dogfood lands on main with the enriched brief in the merge and no workflow-process heap exhaustion, and a mid-review SIGKILL resumes with zero re-executed activities (CN9, R9)."
    ]
  },
  "resolved_context": {
    "adrs": [
      {
        "id": "ADR-002",
        "title": "No backwards compatibility during the build",
        "decision": "Replace, don't add alongside. No compat shims, no zombie code, no #[deprecated] markers. Breaking changes are made cleanly and consumers move forward. Rejected: incremental deprecation cycles \u2014 they double the surface under test for an audience of zero.",
        "quote": "",
        "decided_by": "Tom"
      },
      {
        "id": "ADR-008",
        "title": "brief_dev replaces onatopp_dev inside the stacked-dev family",
        "decision": "Evolve in place: onatopp_dev.gleam is deleted and brief_dev.gleam takes its slot as stacked_dev's inner child; the outer arc keeps its live-proven contracts. Rejected: a parallel brief-dev family \u2014 two families serving one purpose is the zombie-code pattern ADR-002 prohibits, and the outer arc's provision/gate/review/land contracts took a full dogfood night to prove against real CLIs; duplicating them duplicates that risk.",
        "quote": "so brief Dev replacing on a top Dev in the stacked dev family. I guess that's fine but like I also don't mind sort of holding onto both for the time being",
        "decided_by": "Tom"
      },
      {
        "id": "ADR-011",
        "title": "The standard library carries cross-cutting primitives only; runtime harnesses stay out",
        "decision": "aion_kit (the worker standard library) carries only primitives that apply across the board \u2014 data transformation/wrangling, rendering/templating, and the opaque payload \u2014 plus further cross-cutting primitives as they prove general. Runtime harnesses, the norn agent driver chief among them, stay as worker-side code that consumers (Meridian) own and integrate; they may live in a worker for convenience but never ship as standard-library surface. Rejected: bundling the norn/agent harness into the standard library \u2014 it couples a general toolkit to one runtime and ships a Meridian concern to every consumer.",
        "quote": "Norn's too specific. Like it's Norn at the runtime, the agent harness, like I wrote that a couple of weeks ago. So like it's not even like I don't think we should ship that as part of the standard library. That would be more something that Meridian would consume.",
        "decided_by": "Tom"
      },
      {
        "id": "ADR-012",
        "title": "Workflow code stays thin: large activity results ride as opaque payloads",
        "decision": "Workflow code stays thin. Large activity results ride between stages as opaque sealed payloads the workflow never opens; the workflow decodes only a small facts projection \u2014 the few fields it needs to route control flow (pass/fail, changed files, blocked, drift). Decoding and prompt rendering happen in the consuming activity on the worker, which has a full heap and runs the work once rather than on every replay. Rejected: decoding and rendering full reports in workflow code \u2014 it puts the determinism boundary's heavy lifting in the wrong place and was the regression that exposed the beamr crash.",
        "quote": "we're not changing the output of the agents right we're still keeping all of that which is not decoding it all within the workflow so it also stays packaged up and then it passes out to the worker the worker unpacks it and provides the prompt to the agent",
        "decided_by": "Tom"
      }
    ],
    "checklist": [
      {
        "id": "C1",
        "text": "src/stacked_dev/facts.gleam defines DevFacts (blocked_requirement_ids: List(String), changed_files: List(String)), ReviewFacts (drifted: List(DriftedRequirement), has_fixes: Bool), and ScoutFacts (a success marker with no report fields), each with a hand-written round-tripping codec."
      },
      {
        "id": "C2",
        "text": "Every field of every facts type corresponds to a value brief_dev read off a full report before the reshape: DevFacts.blocked_requirement_ids from dev_report.enrichments[].status == blocked, DevFacts.changed_files from the deduplicated files_changed[].path, ReviewFacts.drifted from enrichments[].alignment == drifted with issues, ReviewFacts.has_fixes from any enrichment fixes non-empty \u2014 no field is invented and none of the old routing signals is dropped."
      },
      {
        "id": "C3",
        "text": "The facts are derived on the worker (in locals/handlers) from the full report, not in workflow code; the workflow receives the facts already projected."
      },
      {
        "id": "C4",
        "text": "scout, dev, dev_resume, and dev_review each return a result pairing the stage facts with the full stage report sealed as an opaque aion_flow pass-through payload (via the aion_kit seal primitive), and the workflow holds the sealed payload without ever calling raw/peek on it."
      },
      {
        "id": "C5",
        "text": "brief_dev.gleam contains no field access on ScoutReport, DevReport, or ReviewReport and no import of aion_stacked_dev_io's report decoders for control flow: grep for the report type field accessors in brief_dev.gleam returns nothing."
      },
      {
        "id": "C6",
        "text": "The reshaped BriefDevResult carries the three sealed stage payloads (for enrich_brief) plus the facts the outer arc reads (the changed-file paths and the dev summary the outer DevResult derivation needs), and crosses the parent boundary with its codec round-tripping losslessly."
      },
      {
        "id": "C7",
        "text": "The enrich_brief activity decodes the sealed scout/dev/review payloads on the worker (aion_kit payload.raw + json) to merge the full reports into the brief, and no full report is decoded in any workflow module."
      },
      {
        "id": "C8",
        "text": "Prompt rendering for scout, dev, dev_resume, and dev_review happens in the activity bodies on the worker (src/stacked_dev/render.gleam and worker/src/render.rs), built via the aion_kit template primitive over the brief document, resolved context, and the decoded prior-stage payloads."
      },
      {
        "id": "C9",
        "text": "src/stacked_dev/prompts.gleam is deleted along with its five projection test modules (prompts_render/scout/dev/review/resume_test.gleam); brief_dev.gleam no longer imports stacked_dev/prompts and there is no second in-workflow rendering path left alongside the worker-side one (ADR-002)."
      },
      {
        "id": "C10",
        "text": "render.gleam preserves the projection discipline of the retired prompts.gleam: anchored ADRs as 'id: title \u2014 decision' lines, verbatim quotes with speaker attribution, requirements with inline resolved C#/S# texts, boundaries in every stage prompt, and the dev projection inlines scout findings per requirement while the review projection renders the dev attestation and measured checks but never the scout (ADR-010)."
      },
      {
        "id": "C11",
        "text": "render_test.gleam pins each stage prompt's content and character budget against the seeded fixture (docs/design/brief-dev/briefs/BD-001.json) with the same budgets the deleted prompts tests asserted (scout 6000, dev 9000, review 12000)."
      },
      {
        "id": "C12",
        "text": "brief_dev.gleam threads sealed payloads between stages and reads only the facts projection for routing: DevBlocked from DevFacts.blocked_requirement_ids, scoped_checks files from DevFacts.changed_files, ReviewDrifted from ReviewFacts.drifted, the harden re-verify trigger from ReviewFacts.has_fixes."
      },
      {
        "id": "C13",
        "text": "The stage flow, the seven status phases (scouting, developing, verifying, fixing, reviewing, hardening, converged), the bounded verify-fix loop with required verify_fix_cap and round_backoff_ms, and the six-variant BriefDevError taxonomy are preserved exactly as RM-017 landed them \u2014 the reshape changes the data flow, not the control flow."
      },
      {
        "id": "C14",
        "text": "brief_dev.gleam imports no rendering module (prompts is deleted, render is worker-side) and dispatches only scout, warm_build, dev, scoped_checks, dev_resume, and dev_review \u2014 the same six activity calls, now with reshaped result types."
      },
      {
        "id": "C15",
        "text": "schemas/brief_dev_output.json is updated to the reshaped result (sealed reports + facts) inside the codegen v1 subset (no $ref/$defs/oneOf/default), aion codegen --check passes clean, and src/aion_stacked_dev_io.gleam is regenerated, not hand-edited."
      },
      {
        "id": "C16",
        "text": "stacked_dev.gleam is touched only at the two sites that consume the brief_dev child result (the outer DevResult derivation and the enrich_brief calls); provision/gate/review/land argv, the StartupTask/StartupResult envelope, the review_verdict signal, and the brief_dev spawn_and_wait shape are byte-identical pre- and post-cluster (CN1)."
      },
      {
        "id": "C17",
        "text": "The outer DevResult (session_id, files_touched, summary) and the enrich_brief inputs are produced from the reshaped BriefDevResult without decoding a full report in workflow code: files_touched comes from the carried facts and the sealed reports flow straight to enrich_brief."
      },
      {
        "id": "C27",
        "text": "The TK-002 dogfood runs end to end through real norn against the aion repo after WT-001: dispatched, scouted/developed/verified/reviewed/hardened, landed on main with the enriched brief in the merge, and a SIGKILL mid-run resumes with no re-executed activities and no workflow-process heap exhaustion (CN9)."
      },
      {
        "id": "C28",
        "text": "wire_compat.rs pins every facts type and sealed-payload envelope byte-compatible both directions against the Gleam codecs; handlers_shims.rs covers the render-and-seal handler shape."
      }
    ],
    "stories": [
      {
        "id": "S3",
        "text": "As a workflow author, I want my workflow code to stay thin by default \u2014 threading sealed payloads and reading only facts \u2014 so that I don't reintroduce the per-replay heavy-decode regression every new family would otherwise copy."
      },
      {
        "id": "S5",
        "text": "As an AI team member, I want the reshaped pipeline to land a real brief end to end through norn after the change, so that I can dispatch work knowing the lighter pipeline still completes the whole arc, not just type-checks."
      },
      {
        "id": "S6",
        "text": "As an AI team member, I want a multi-brief dogfood run to survive a kill -9 of the server and resume without re-executing completed activities, so that a long run is never lost and the thin-workflow reshape is proven durable under crash."
      },
      {
        "id": "S7",
        "text": "As an AI lead reviewing the reshape, I want to confirm by grep and by type signatures that the workflow decodes no full report and renders no prompt, so that I can verify the determinism boundary holds without re-reading every stage by hand."
      },
      {
        "id": "S8",
        "text": "As an AI lead, I want to confirm the outer stacked-dev contracts are byte-unchanged, so that I can let the inner reshape through without re-proving the provision/gate/review/land arc that took a full dogfood night to land."
      },
      {
        "id": "S10",
        "text": "As a norn agent, I want my prompt rendered on the worker that dispatches me \u2014 from the brief, the resolved context, and the prior stages \u2014 so that I receive the same resolved, budgeted prompt as before even though the workflow no longer builds it."
      },
      {
        "id": "S12",
        "text": "As a future maintainer, I want the facts projection documented as the negative space of the old in-workflow decode, so that when I add a new routing signal I know it must come from a fact the workflow already needed, not a freshly decoded report field."
      },
      {
        "id": "S13",
        "text": "As Tom, I want the agents' output kept exactly as it is \u2014 sealed and passed to the worker which unpacks it and builds the prompt \u2014 so that the reshape moves where decoding happens without changing what the agents produce or how the briefs are enriched."
      },
      {
        "id": "S14",
        "text": "As Tom, I want the standard library kept to genuinely cross-cutting primitives \u2014 the payload, templating, data wrangling \u2014 with the norn agent harness staying worker-side, so that the toolkit doesn't ship a Meridian-specific runtime to every consumer."
      }
    ],
    "constraints": [
      {
        "id": "CN1",
        "text": "The OUTER stacked-dev contracts are byte-immutable in this cluster: provision_workspace/full_checks(gate)/request_review/land argv and sequencing, the StartupTask/StartupResult startup envelope, the single review_verdict signal, and the brief_dev child workflow type plus its spawn_and_wait shape do not change. They are live-proven (ADR-008); only the INNER brief_dev data flow is reshaped. A diff touching any of those sites is a cluster defect (mirrors brief-dev CN8)."
      },
      {
        "id": "CN2",
        "text": "Deterministic workflow code (brief_dev.gleam) decodes ONLY the facts projection and never a full ScoutReport, DevReport, or ReviewReport; full reports cross stage boundaries as opaque sealed aion_flow payloads the workflow holds without opening (ADR-012, engine invariant 2). grep for ScoutReport/DevReport/ReviewReport field access in brief_dev.gleam returns nothing after the reshape."
      },
      {
        "id": "CN3",
        "text": "Prompt rendering and full-report decoding happen on the worker (activity bodies / locals), never in workflow code; brief_dev.gleam does not import stacked_dev/prompts (which is deleted) or stacked_dev/render. The render path is built on aion_kit's template primitive (RM-022), not re-implemented."
      },
      {
        "id": "CN4",
        "text": "The facts projection is the negative space of the pre-reshape in-workflow decode: every field a facts type carries (dev: blocked R# ids, deduplicated changed-file paths; review: drifted R# ids with issues, has-fixes; scout: success only) was a field brief_dev read off a full report before. No new routing signal is invented and none is dropped (P3)."
      },
      {
        "id": "CN5",
        "text": "The stage-contract schemas (scout/dev/review report) and the design-system canon they mirror are unchanged \u2014 the agents' output is identical (ADR-012 quote); only the consumption point moves. The drift gate (brief-dev CN7) still passes; no file under docs/design-system/ is touched."
      },
      {
        "id": "CN6",
        "text": "The lifted worker-harness (WT-002) runs the same CLIs with byte-identical argv in the same order as the live-proven originals: yg branch add/provision, cargo build, yg graph affected --plain --direct-only then yg diagnostics check (scoped and --workspace), meridian review request <branch> --reviewer... --as Meridian, git add -A then git commit then yg branch merge <branch> --yes from repo_root. Extraction is a move, not a rewrite (P5, CN1)."
      },
      {
        "id": "CN7",
        "text": "The dev-pipeline skeleton (WT-003) is parameterised by an explicit PipelineConfig \u2014 stage prompts, stage schemas, gate commands, and the required caps/backoff \u2014 and bakes no default cap, backoff, deadline, or timeout (ADR-001 spirit, CN2 of brief-dev): every bound is a config or input field."
      },
      {
        "id": "CN8",
        "text": "The norn agent driver is not part of aion_kit and is not lifted into the toolkit (ADR-011): it stays worker-side; the pipeline skeleton and the template parameterise the agent step. aion_kit surface used by this cluster is the opaque payload (seal/raw/peek) and the template/json primitives only."
      },
      {
        "id": "CN9",
        "text": "After WT-001 the TK-002 dogfood completes through real norn end to end with no workflow-process heap exhaustion: dispatched from the roadmap, scouted/developed/verified/reviewed/hardened, and landed on main with the enriched brief riding the merge. The reshape is proven by the run, not only the type-checker (WT-001 verification, goal 3)."
      },
      {
        "id": "CN10",
        "text": "The WT briefs are SERIAL within the cluster (WT-001 \u2192 WT-002 \u2192 WT-003): all three edit examples/stacked-dev (the workflow, the worker, the template) and would collide; parallelism is across clusters, not within this one (RM-023). The whole cluster is blocked on aion_kit (RM-022) landing first."
      }
    ],
    "intention": "Authoring the next agentic family should cost a day, not a brief-set. When this is done, brief_dev is the proof of a pattern rather than a pile of bespoke code: the deterministic workflow process holds only the few thin facts it routes on and threads everything else as sealed payloads it never opens, the worktree/build/check/land harness is a module a new family imports instead of re-deriving, and the scout\u2192dev\u2192verify\u2192review\u2192harden skeleton is parameterised by (prompts, schemas, gate commands) so a new family is configuration plus its own prompts and schemas. The dogfood stays the dev process: aion still develops aion through this pipeline, but the pipeline is lighter, replay is cheap, and the class of crash that killed a real run is designed out.",
    "design_path": "docs/design/workflow-toolkit",
    "provenance": {
      "requested_by": "Tom",
      "quote": "Authoring the next agentic family should cost a day, not a brief-set. When this is done, brief_dev is the proof of a pattern rather than a pile of bespoke code: the deterministic workflow process hold"
    }
  },
  "verify_fix_cap": 3,
  "review_cap": 1,
  "round_backoff_ms": 2000,
  "review_deadline_ms": 300000,
  "workspace_id": "2d5fdd51-1f25-45a4-8f86-4d4c978d1355",
  "clone_url": "https://github.com/tomWhiting/aion.git"
}
