{
  "repo_root": "/Users/tom/Developer/ablative/aion",
  "brief_id": "AU-006",
  "reviewers": [
    "Waffles the Terrible"
  ],
  "base_ref": "main",
  "placement": "local",
  "isolation": "worktree",
  "brief_document": {
    "id": "AU-006",
    "cluster": "aion-dashboard",
    "title": "Namespace selection and query scoping",
    "depends_on": [
      "AU-002"
    ],
    "blocked_by": [],
    "checklist": [
      "C33",
      "C34"
    ],
    "stories": [
      "S10"
    ],
    "purpose": "Make the dashboard multi-tenant-aware: a selected namespace scopes every query and every subscription so an operator investigates one tenant cleanly.",
    "task": "Build the namespace feature: a context holding the selected namespace (loaded from listNamespaces), a selector to switch it, and the scoping wiring so every query key and every subscription filter carries the selected namespace and re-scopes on switch.",
    "requirements": [
      {
        "id": "R1",
        "title": "Namespace context and selector",
        "spec": "THE SYSTEM SHALL provide a namespace context holding the selected namespace (populated from listNamespaces) and a selector component letting the operator switch namespaces.",
        "acceptance": [
          "src/features/namespace/context/NamespaceContext.tsx exposes the selected namespace and a setter, with the available namespaces loaded via the api client's listNamespaces.",
          "NamespaceSelector renders the available namespaces (shadcn select) and updates the context on change.",
          "The selector renders loading and error states for the namespace list.",
          "A test asserts selecting a namespace updates the context value."
        ],
        "files": {
          "create": [
            "apps/aion-dashboard/src/features/namespace/context/NamespaceContext.tsx",
            "apps/aion-dashboard/src/features/namespace/components/NamespaceSelector.tsx",
            "apps/aion-dashboard/src/features/namespace/index.ts"
          ],
          "modify": []
        },
        "checklist": [
          "C33"
        ],
        "stories": [
          "S10"
        ]
      },
      {
        "id": "R2",
        "title": "Scoping every query and subscription",
        "spec": "THE SYSTEM SHALL scope every query and every subscription by the selected namespace, and WHEN the namespace changes THE SYSTEM SHALL re-scope both the active query and the live subscription. No view SHALL issue an unscoped query when a namespace is selected.",
        "acceptance": [
          "The list query key, the history query key, and the subscription filter all include the selected namespace.",
          "Changing the namespace invalidates/re-issues the active query and re-establishes the active subscription under the new namespace.",
          "A test switching namespace asserts the query is re-issued and the subscription re-established with the new namespace, and that no query is issued without a namespace when one is selected."
        ],
        "files": {
          "create": [],
          "modify": [
            "apps/aion-dashboard/src/features/namespace/context/NamespaceContext.tsx"
          ]
        },
        "checklist": [
          "C34"
        ],
        "stories": [
          "S10"
        ]
      }
    ],
    "boundaries": [
      "SHALL NOT define the REST client or WebSocket manager \u2014 AU-002, AU-003 (this brief threads the namespace through them).",
      "SHALL NOT build the list or detail views \u2014 AU-004, AU-005 (this brief provides the namespace they scope by).",
      "SHALL NOT define server namespace semantics \u2014 that is cluster AW."
    ],
    "verification": [
      "bun run check (Biome) passes clean.",
      "tsc --noEmit passes clean.",
      "bun test passes for the namespace context and scoping tests.",
      "No unscoped query when a namespace is selected; no lint-disable comments added."
    ]
  },
  "resolved_context": {
    "adrs": [],
    "checklist": [
      {
        "id": "C33",
        "text": "A namespace context holds the selected namespace and a selector lets the operator switch it."
      },
      {
        "id": "C34",
        "text": "Every query and every subscription is scoped by the selected namespace; switching namespace re-scopes both the active query and the live subscription."
      }
    ],
    "stories": [
      {
        "id": "S10",
        "text": "As a platform operator, I want to select a namespace so that the list, history, and live stream are all scoped to the tenant I am investigating."
      }
    ],
    "constraints": [
      {
        "id": "CO1",
        "text": "Match the apps/web stack exactly: React 19, TypeScript, Vite, Tailwind v4, Bun, Biome (100-char), shadcn/Radix primitives, TanStack Query. No new framework, styling system, or state library."
      },
      {
        "id": "CO2",
        "text": "Wire types come from the house generated-types pipeline (Rust #[derive(TS)] \u2192 src/types/generated/). The dashboard never hand-defines a type aion-core/aion-proto owns and never hand-edits a generated file."
      },
      {
        "id": "CO3",
        "text": "The dashboard consumes the AW API/WebSocket contract; it defines no server endpoint, no WebSocket protocol, and no asset-hosting mechanism. Not-yet-pinned contract details are isolated in the api module for a one-file change."
      },
      {
        "id": "CO4",
        "text": "Feature-folder convention from apps/web: each feature is components/ + hooks/ (+ context/lib as needed) + types.ts + index.ts. Barrels re-export; logic lives in named files."
      },
      {
        "id": "CO5",
        "text": "Server state goes through TanStack Query; live updates flow through the WebSocket manager and patch query caches or local view state. No bespoke fetch-into-useState for server data."
      },
      {
        "id": "CO6",
        "text": "Every async surface renders explicit loading, empty, and error states. No surface spins indefinitely or blanks on failure."
      },
      {
        "id": "CO7",
        "text": "The WebSocket manager reconnects with bounded backoff on drop and the active view resyncs (re-fetch history/query, or replay after last-seen sequence where supported). A connection indicator is always present; a dropped socket never leaves a silently stale view."
      },
      {
        "id": "CO8",
        "text": "The event timeline is ordered strictly by event sequence number and de-duplicates events arriving via both history fetch and live stream."
      },
      {
        "id": "CO9",
        "text": "Biome lint + format clean (bun run check) and tsc --noEmit clean. No lint-disable comments to silence findings."
      },
      {
        "id": "CO10",
        "text": "Namespace selection scopes every query and every subscription; switching namespace re-scopes both. No unscoped query when a namespace is selected."
      }
    ],
    "intention": "Build the human-facing operational web UI for aion-server: a browser application that lets an operator see which workflows are running and which have failed, drill into a single execution's full event history rendered as a timeline, and watch events stream live as they happen. This cluster builds the frontend only. It consumes the HTTP/gRPC list/query/history API and the WebSocket event stream that the server (cluster AW) exposes and hosts as static assets; it does not run the engine, persist anything, or define the API. It must match the existing house frontend stack (React 19 + TypeScript + Vite + Tailwind v4 + Biome + Bun, shadcn/Radix, TanStack Query, a singleton WebSocket manager with reconnect, generated types from Rust) exactly, so it reads as part of the same codebase as apps/web. When done, an operator pointed at a running aion-server can observe every workflow in every namespace, drill into any execution's full history, and watch events stream in real time \u2014 with production-grade loading, empty, error, and reconnection behaviour throughout.",
    "design_path": "docs/design/aion-dashboard",
    "provenance": {
      "requested_by": "Tom",
      "quote": "Build the human-facing operational web UI for aion-server: a browser application that lets an operator see which workflows are running and which have failed, drill into a single execution's full event"
    }
  },
  "verify_fix_cap": 3,
  "review_cap": 1,
  "round_backoff_ms": 2000,
  "review_deadline_ms": 300000,
  "workspace_id": "2d5fdd51-1f25-45a4-8f86-4d4c978d1355",
  "clone_url": "https://github.com/tomWhiting/aion.git"
}
