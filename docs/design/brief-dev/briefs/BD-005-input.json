{
  "repo_root": "/Users/tom/Developer/ablative/aion",
  "brief_id": "BD-005",
  "reviewers": [
    "Waffles the Terrible"
  ],
  "base_ref": "main",
  "placement": "local",
  "isolation": "worktree",
  "brief_document": {
    "id": "BD-005",
    "cluster": "brief-dev",
    "title": "stacked_dev integration and worker parity",
    "depends_on": [
      "BD-003",
      "BD-004"
    ],
    "blocked_by": [],
    "checklist": [
      "C23",
      "C24",
      "C25",
      "C30",
      "C31",
      "C32"
    ],
    "stories": [
      "S2",
      "S18",
      "S19"
    ],
    "design_anchor": [
      "ADR-006",
      "ADR-008"
    ],
    "purpose": "Integrate the v2 pipeline into the live-proven outer arc and restore deployed parity: StackedDevInput reshapes to carry the v2 brief document and resolved context (ADR-008 \u2014 evolve in place, no parallel family), stacked_dev spawns brief_dev as its inner child and writes the execution block into the worktree brief before land so the enriched record and the implementation arrive on main in the same commit lineage, and the standalone worker gains handlers for scout, dev_review, and enrich_brief mirroring the locals invocation-for-invocation with every new payload pinned byte-compatible. The outer arc's live-proven CLI contracts are immutable here (CN8): request_review argv with the branch positional first and --as Meridian, land as git commit in the worktree then yg merge from repo_root, and the single review_verdict signal (ADR-006 \u2014 quorum is Meridian's, one signal decides).",
    "task": "Reshape and rewire, replacing rather than adding alongside (ADR-002 via ADR-008). Gleam side: StackedDevInput drops the brief/design/checklist/stories document strings in favour of brief_document (BD-001's BriefDocument) and resolved_context (BD-001's ResolvedContext); schemas/input.json mirrors the reshape with additionalProperties false and every cap/deadline still required; codecs_workflows.gleam re-encodes the input; stacked_dev.gleam spawns brief_dev (BD-003's child) instead of onatopp_dev, lifts each BriefDevError variant into a typed StackedDevError variant, and \u2014 between the Approve verdict and the land activity \u2014 runs BD-004's enrich_brief with the ExecutionEnrichment so the landed merge carries the brief's own provenance (C25, ADR-009). The phase flow (provision \u2192 child \u2192 gate child \u2192 review loop \u2192 land), the gate child, the review loop, the signal contract, and the land sequence are unchanged (C24, CN8). Worker side: handlers for scout, dev_review, and enrich_brief mirror the locals BD-003 and BD-004 landed, invocation for invocation; the dev/dev_resume handlers track BD-003's reshaped DevInput (the projected prompt replaces the four document fields \u2014 derive the serde shape from the landed Gleam codec, never from memory); SERVED_ACTIVITIES grows to the eleven names the family now dispatches; wire_compat.rs pins every new payload byte-compatible both directions; handlers_shims.rs proves the new handlers against fake CLIs. Also mechanically adapt the existing hermetic suite (base_input, child registration) so the package test gate stays green at this brief's landing commit \u2014 new scenarios remain BD-007's. SPLIT NOTE on C32: this brief pins the brief document, resolved context, stage reports, enrich payloads, and brief_dev/stacked_dev IO; the dispatch IO payloads do not exist until BD-006, which extends wire_compat.rs with their pins when it creates them. NOTE on coverage tooling: examples/stacked-dev/schemas/input.json and examples/stacked-dev/worker/src/main.rs are required by this work but are absent from design.json's structure array \u2014 per CLAUDE.md, when a structure annotation disagrees with a brief, the brief is authoritative. Out of scope: brief_dev.gleam internals and the scout/dev_review activity definitions (BD-003), prompts.gleam (BD-002), enrich.gleam and the enrich_brief local (BD-004), the dispatch workflow (BD-006), and all new hermetic scenarios, e2e, and template work (BD-007).",
    "requirements": [
      {
        "id": "R1",
        "title": "Reshape StackedDevInput around the v2 brief document",
        "spec": "In src/stacked_dev/types.gleam, replace StackedDevInput's brief: String, design: String, checklist: String, and stories: List(String) fields with brief_document: BriefDocument and resolved_context: ResolvedContext (both BD-001 types, consumed as-is). All other fields \u2014 repo_root, brief_id, reviewers, base_ref, placement, isolation, verify_fix_cap, review_cap, round_backoff_ms, review_deadline_ms \u2014 remain required exactly as today. brief_id stays an explicit input rather than being derived from brief_document.id: run identity is the caller's (re-dispatch after a failed run mints a fresh identity, ADR-005), and the live-proven branch derivation stacked-dev-<brief_id> keeps consuming it. THE SYSTEM SHALL NOT keep the old string fields alongside the new ones, SHALL NOT make any field optional, and SHALL NOT introduce a default for any cap, backoff, or deadline.",
        "acceptance": [
          "StackedDevInput has exactly twelve fields: repo_root, brief_id, reviewers, base_ref, placement, isolation, brief_document, resolved_context, verify_fix_cap, review_cap, round_backoff_ms, review_deadline_ms.",
          "brief_document is BD-001's BriefDocument type and resolved_context is BD-001's ResolvedContext type \u2014 no new wrapper types are declared.",
          "grep for \"design: String\" and \"checklist: String\" in the StackedDevInput declaration returns nothing \u2014 the document strings are gone, not deprecated."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev/types.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C23"
        ],
        "stories": []
      },
      {
        "id": "R2",
        "title": "Mirror the reshape in schemas/input.json",
        "spec": "Rewrite examples/stacked-dev/schemas/input.json (the stacked_dev dispatch-boundary schema named by workflow.toml) to mirror the reshaped input: the properties brief, design, checklist, and stories are removed; brief_document and resolved_context are added as object sub-schemas that inline-mirror the package brief-document and resolved-context shapes with the same required and additionalProperties: false discipline as the design-system canon (no $ref, no default keyword anywhere). The root keeps additionalProperties: false, and every cap and deadline (verify_fix_cap, review_cap, round_backoff_ms, review_deadline_ms) stays in the required array \u2014 the caller decides every bound (ADR-001 lineage). THE SYSTEM SHALL NOT loosen brief_document to a bare {\"type\": \"object\"} \u2014 a malformed document must break at dispatch, not mid-run in an agent prompt.",
        "acceptance": [
          "The root required array is exactly [\"repo_root\", \"brief_id\", \"reviewers\", \"base_ref\", \"placement\", \"isolation\", \"brief_document\", \"resolved_context\", \"verify_fix_cap\", \"review_cap\", \"round_backoff_ms\", \"review_deadline_ms\"].",
          "properties contains no key named brief, design, checklist, or stories at the root, and root additionalProperties is false.",
          "The brief_document sub-schema declares required including id, cluster, title, and requirements, with additionalProperties false and no default keyword; `python3 -c \"import json; json.load(open('examples/stacked-dev/schemas/input.json'))\"` exits 0.",
          "The string \"default\" does not appear as a schema keyword anywhere in the file."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/schemas/input.json"
          ],
          "delete": []
        },
        "checklist": [
          "C23"
        ],
        "stories": []
      },
      {
        "id": "R3",
        "title": "Reshape the stacked_dev input codec",
        "spec": "In src/stacked_dev/codecs_workflows.gleam, rewrite stacked_dev_input_codec() to encode and decode the reshaped StackedDevInput: brief_document via BD-001's brief document codec and resolved_context via BD-001's resolved-context codec, with the four old document-string fields removed from both the encoder and the decoder. Field names on the wire are exactly the input-schema property names (snake_case, declaration order). THE SYSTEM SHALL NOT tolerate the old shape on decode \u2014 a payload carrying \"brief\" as a string and lacking brief_document fails to decode (no backwards compatibility, ADR-002).",
        "acceptance": [
          "stacked_dev_input_codec() round-trips a fixture input carrying a two-requirement brief document and a resolved context: decode(encode(input)) == Ok(input).",
          "Decoding the pre-reshape JSON literal (with \"brief\"/\"design\"/\"checklist\"/\"stories\" string fields and no brief_document) returns a decode error.",
          "The encoded fixture contains the substrings \"\\\"brief_document\\\":\" and \"\\\"resolved_context\\\":\" and does not contain \"\\\"design\\\":\"."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev/codecs_workflows.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C23"
        ],
        "stories": []
      },
      {
        "id": "R4",
        "title": "Rewire the outer arc: brief_dev child, lifted errors, execution block before land",
        "spec": "In src/stacked_dev.gleam: WHEN the workspace is provisioned, THE SYSTEM SHALL spawn the brief_dev child (BD-003's entry, by its workflow type and codecs) via workflow.spawn_and_wait, passing the workspace, brief_document, resolved_context, verify_fix_cap, and round_backoff_ms, and SHALL NOT reference onatopp_dev. WHEN brief_dev fails with a typed BriefDevError, THE SYSTEM SHALL lift each variant into a typed StackedDevError variant preserving its payload \u2014 scout failure, dev block, verify exhaustion (rounds and diagnostics), review drift (the drifted R# ids and their issues), and stage failure \u2014 and SHALL NOT collapse a typed child error into a generic StageFailed message. WHEN the gate has passed and the review verdict is Approve, THE SYSTEM SHALL, strictly before running the land activity, run BD-004's enrich_brief with ExecutionEnrichment carrying: the measured gate results from the gate child, the dev attestation from the child's dev report (recorded separately from the gate \u2014 P1), review_verdict \"approved\", the workspace branch, the dev session id, status \"landed\", merged_into base_ref, landed_commit \"\" (a commit cannot contain its own hash; ADR-009 records the merge itself as provenance), and completed_at stamped by the activity \u2014 so the subsequent git add/commit inside land carries the enriched brief into the merge (C25). The stacked_dev entry in workflow.toml SHALL add \"enrich_brief\" to its activities list. THE SYSTEM SHALL NOT alter the request_review argv contract, the land sequence (git add -A and git commit in the worktree, then yg branch merge from repo_root), the review_verdict signal name or payload, the review loop's cap/deadline/backoff handling, or the gate child's place in the phase flow (C24, CN8, ADR-006).",
        "acceptance": [
          "stacked_dev.gleam contains no import of onatopp_dev; the child spawn names brief_dev's workflow type and uses brief_dev's input/result/error codecs.",
          "Every BriefDevError variant has a distinct StackedDevError lifting that preserves its payload fields \u2014 VerifyExhausted keeps rounds and diagnostics, and the review-drift lifting carries the drifted R# ids and issues.",
          "In execute's Approve path, the enrich_brief activity run appears between the receive of the Approve verdict and the land activity run, and the land activity body is byte-unchanged in locals.gleam and handlers.rs.",
          "The ExecutionEnrichment built in the Approve path populates gate from the gate child's result and attestation from the brief_dev dev report \u2014 two distinct sources, never one copied into the other.",
          "The stacked_dev [[workflow]] entry in workflow.toml lists \"enrich_brief\" in activities; the request_review and land entries are otherwise unchanged.",
          "The review loop still parks on the single review_verdict signal with the caller's review_deadline_ms and review_cap \u2014 no second verdict signal and no quorum logic exists in the workflow (ADR-006)."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/src/stacked_dev.gleam",
            "examples/stacked-dev/workflow.toml"
          ],
          "delete": []
        },
        "checklist": [
          "C24",
          "C25"
        ],
        "stories": [
          "S2",
          "S18",
          "S19"
        ]
      },
      {
        "id": "R5",
        "title": "Worker serde types for the new payloads",
        "spec": "In worker/src/types.rs, add serde types mirroring the Gleam codecs field-for-field (names, enum tag strings, and declaration order \u2014 both sides emit compact JSON in declaration order): the brief document and its requirement/stage-block/execution shapes, the resolved context, the scout/dev/review report types, EnrichInput with the four-variant Enrichment union (tag \"stage\"), and the brief_dev and reshaped stacked_dev IO shapes the family now carries. Update DevInput to BD-003's reshaped shape (the projected prompt replaces the brief/design/checklist/stories fields) \u2014 derive every field from the landed Gleam codec source, not from this brief's prose. THE SYSTEM SHALL NOT keep the old DevInput fields alongside the new shape and SHALL NOT invent field names the Gleam codecs do not emit.",
        "acceptance": [
          "Every new type in types.rs names the Gleam codec function it mirrors in its doc comment, matching the file's existing convention.",
          "DevInput in types.rs has exactly the fields BD-003's dev-input codec encodes, in the same order \u2014 verified by the wire_compat literal for the dev StartupTask variant.",
          "The Enrichment union deserializes from {\"stage\":\"scout\",...}, {\"stage\":\"dev\",...}, {\"stage\":\"review\",...}, and {\"stage\":\"execution\",...} and from no other tag value."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/worker/src/types.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C30"
        ],
        "stories": []
      },
      {
        "id": "R6",
        "title": "Worker handlers: scout, dev_review, enrich_brief, and dev parity",
        "spec": "In worker/src/handlers.rs, add handler bodies mirroring the locals invocation for invocation. WHEN serving scout, THE SYSTEM SHALL invoke norn with --print, --session-id <branch>-scout, --workspace-root <workspace path>, --output-schema <the scout-report stage schema JSON>, --output-format json, and the projected scout prompt positionally, exactly as locals does. WHEN serving dev_review, THE SYSTEM SHALL invoke norn with the <branch>-review session id and the review-report stage schema, and SHALL NOT ever pass the dev session id (CN4 \u2014 fresh eyes are the point). WHEN parsing norn stdout for any stage, THE SYSTEM SHALL attempt exactly two shapes \u2014 the bare report, then the real {\"output\": ...} completion envelope \u2014 and fail terminally carrying the head of the output when both fail; THE SYSTEM SHALL NOT attempt any third shape or silent fallback (C31). WHEN serving enrich_brief, THE SYSTEM SHALL mirror locals.enrich_brief: derive the same worktree path, deep-compare the authored subset, fail terminally on divergence without writing, and write the merged document on equality. Update the dev/dev_resume handlers to BD-003's reshaped DevInput (the handler passes the projected prompt through and no longer assembles a prompt from document fields). Failure classification follows the locals exactly: can't-execute is terminal; expected negative outcomes are successful results carrying data (CN5).",
        "acceptance": [
          "The scout handler's norn argv is [\"--print\", \"--session-id\", \"<branch>-scout\", \"--workspace-root\", <workspace.path>, \"--output-schema\", <scout schema>, \"--output-format\", \"json\", <prompt>] \u2014 identical, element for element, to the locals invocation.",
          "The dev_review handler's session argument is the input's review session id ending \"-review\", and no code path in handlers.rs passes a dev session id to the review invocation.",
          "Each stage's output parser tries the bare shape, then a struct with the single field output, and returns a terminal failure mentioning both attempted shapes on a third shape \u2014 no serde fallback beyond those two.",
          "The enrich_brief handler refuses an authored-field divergence with a terminal failure naming the divergent field and leaves the file byte-unchanged, matching locals.enrich_brief behaviour on the same fixture.",
          "handlers.rs no longer contains a dev_prompt assembly function \u2014 the dev handler forwards the prompt field from the reshaped DevInput."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/worker/src/handlers.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C30",
          "C31"
        ],
        "stories": []
      },
      {
        "id": "R7",
        "title": "SERVED_ACTIVITIES grows to the family's eleven names",
        "spec": "In worker/src/main.rs, extend SERVED_ACTIVITIES to [&str; 11] adding \"scout\", \"dev_review\", and \"enrich_brief\" to the existing eight, and register each new handler through the existing blocking adapter. The doc comment count and the registration list stay in lockstep with the workflow.toml activities declarations across the family's entries as of this brief (the dispatch entry's assemble_wave joins in BD-006). THE SYSTEM SHALL NOT register an activity name absent from SERVED_ACTIVITIES or vice versa.",
        "acceptance": [
          "SERVED_ACTIVITIES is [&str; 11] containing exactly provision_workspace, warm_build, dev, scoped_checks, dev_resume, full_checks, request_review, land, scout, dev_review, enrich_brief.",
          "main.rs has exactly eleven register_activity calls and each name matches a SERVED_ACTIVITIES entry.",
          "Every activity name listed by any [[workflow]] entry in examples/stacked-dev/workflow.toml at this brief's landing commit appears in SERVED_ACTIVITIES."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/worker/src/main.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C30"
        ],
        "stories": []
      },
      {
        "id": "R8",
        "title": "Wire-compat pins for every new payload",
        "spec": "In worker/tests/wire_compat.rs, pin every payload this wave adds or reshapes byte-compatible both directions against the Gleam codecs via the existing assert_wire helper (decode the literal, require equality, re-encode, require the exact literal): the brief document (including a requirement carrying scout/dev/review blocks and a document carrying an execution block), the resolved context, each stage report, EnrichInput for all four Enrichment variants, the reshaped DevInput inside its StartupTask envelope, and the brief_dev and reshaped stacked_dev workflow IO shapes the wave carries. Pins assert the CURRENT format only \u2014 THE SYSTEM SHALL NOT keep literals for the retired pre-reshape shapes (ADR-002), and each literal SHALL be hand-derived from the Gleam codec source with the codec function named in the test comment, never sketched from memory.",
        "acceptance": [
          "wire_compat.rs has a test per new payload listed in the spec, each round-tripping its literal byte-for-byte in both directions.",
          "The EnrichInput pins cover all four stage tags and the execution variant's literal contains distinct gate and attestation objects.",
          "No literal in the file contains the retired \"\\\"design\\\":\" document-string field of the old StackedDevInput/DevInput shapes.",
          "`cd examples/stacked-dev/worker && cargo test --test wire_compat` passes."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/worker/tests/wire_compat.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C32"
        ],
        "stories": []
      },
      {
        "id": "R9",
        "title": "Shim-based handler tests for the new handlers",
        "spec": "In worker/tests/handlers_shims.rs, cover the three new handlers with fake-CLI shims on a private PATH, following the file's existing pattern. WHEN the scout handler runs against a norn shim, the recorded argv SHALL contain --print, --session-id <branch>-scout, --output-schema, and --output-format json, and the shim's reply SHALL be a captured real {\"output\": <scout report>} completion envelope that the handler parses (P4 \u2014 fixtures are captured real envelopes, never invented shapes). WHEN the dev_review handler runs, the argv SHALL carry the -review session and the parsed review report SHALL surface the shim's notes intact. The parser tests SHALL cover the bare shape, the envelope shape, and the terminal failure on a third shape. The enrich_brief tests SHALL cover the clean write (file decodes as the merged document afterwards) and the divergence refusal (terminal, file unchanged) without any CLI shim \u2014 it is file IO, not a CLI invocation.",
        "acceptance": [
          "A scout handler test asserts the norn shim's argv log contains \"--session-id\" followed by a value ending \"-scout\", and the handler returns the report carried in the shim's captured-real envelope.",
          "A dev_review handler test asserts the argv session value ends \"-review\" and that a distinguishable note string from the shim's review report arrives in the handler's returned report verbatim.",
          "A parsing test feeds a third shape (neither bare nor {\"output\": ...}) and asserts a terminal failure whose message names both attempted shapes.",
          "An enrich_brief divergence test asserts the on-disk file bytes are identical before and after the refused call.",
          "`cd examples/stacked-dev/worker && cargo test` passes with the new tests included."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/worker/tests/handlers_shims.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C30",
          "C31"
        ],
        "stories": []
      },
      {
        "id": "R10",
        "title": "Keep the hermetic Gleam suite green across the reshape",
        "spec": "Mechanically adapt the existing hermetic suite to this brief's reshape so the package test gate passes at the landing commit: base_input in test/aion_stacked_dev_test.gleam builds a fixture brief document and resolved context instead of the four document strings, and support/shims.gleam registers the brief_dev child double (BD-003's execute) where it registered onatopp_dev's, with sample activity inputs updated to the reshaped payload types. Adaptation only: THE SYSTEM SHALL NOT add new scenarios, new shim writers, or new assertions beyond what compiling and passing the existing scenarios requires \u2014 scenario coverage for the v2 stages is BD-007's scope. Existing assertions that name retired internals (onatopp status query names) are updated to brief_dev's equivalents established by BD-003.",
        "acceptance": [
          "`cd examples/stacked-dev && gleam test` passes at this brief's landing commit.",
          "base_input constructs a StackedDevInput whose brief_document and resolved_context are fixture values and which carries no document-string fields.",
          "support/shims.gleam contains no reference to onatopp_dev, and registers brief_dev's execute as the inner child double.",
          "No test function is added or deleted in this requirement \u2014 the suite's public scenario set is unchanged from BD-003's landing state."
        ],
        "files": {
          "create": [],
          "modify": [
            "examples/stacked-dev/test/aion_stacked_dev_test.gleam",
            "examples/stacked-dev/test/support/shims.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C23",
          "C24"
        ],
        "stories": []
      }
    ],
    "boundaries": [
      "THE BRIEF SHALL NOT modify brief_dev.gleam, the scout/dev_review activity definitions, their locals, or the stage prompts they consume (BD-003); the child is integrated, not reauthored.",
      "THE BRIEF SHALL NOT modify enrich.gleam or the enrich_brief Gleam local implementation (BD-004); the worker handler mirrors it and the outer arc calls it.",
      "THE BRIEF SHALL NOT create or modify prompts.gleam (BD-002) or any stage schema, stage codec, document type, or resolved-context type (BD-001).",
      "THE BRIEF SHALL NOT create dispatch.gleam, the dispatch schemas, the assemble_wave activity, or any dispatch IO type or pin (BD-006).",
      "THE BRIEF SHALL NOT add new hermetic scenarios, live e2e coverage, or dev-pipeline template changes (BD-007); test edits here are limited to R10's mechanical adaptation.",
      "THE IMPLEMENTATION SHALL NOT change the request_review argv (branch positional first, --as Meridian), the land sequence (git add/commit in the worktree, then yg branch merge from repo_root), or the single review_verdict signal contract \u2014 these are live-proven and immutable in this cluster (CN8, ADR-006).",
      "THE IMPLEMENTATION SHALL NOT introduce a default or fallback for any cap, backoff, deadline, or reviewer list \u2014 every bound stays a required input the caller chose (CN2)."
    ],
    "verification": [
      "Run `cd examples/stacked-dev && gleam build && gleam test` \u2014 clean build, all tests pass.",
      "Run `cd examples/stacked-dev/worker && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test` \u2014 the standalone worker crate gates pass (it is not a workspace member; test it directly).",
      "Run `grep -rn \"onatopp\" examples/stacked-dev/src examples/stacked-dev/test examples/stacked-dev/worker/src examples/stacked-dev/workflow.toml` \u2014 no matches anywhere in the family.",
      "Run `grep -n \"review request\" examples/stacked-dev/src/stacked_dev/locals.gleam examples/stacked-dev/worker/src/handlers.rs` and confirm the argv assembly is byte-unchanged from the pre-brief state (branch positional first, --as Meridian).",
      "Run `python3 -c \"import json; s=json.load(open('examples/stacked-dev/schemas/input.json')); print(sorted(s['required'])); assert s['additionalProperties'] is False\"` and confirm the required list matches R2.",
      "Confirm the enrich_brief run in stacked_dev.gleam sits between the Approve receive and the land activity, and that the lifted error union in types.gleam covers every BriefDevError variant with payloads intact.",
      "Run `grep -c \"register_activity\" examples/stacked-dev/worker/src/main.rs` \u2014 exactly 11."
    ]
  },
  "resolved_context": {
    "adrs": [
      {
        "id": "ADR-006",
        "title": "Multi-reviewer verdicts: votes aggregate in Meridian, one signal decides",
        "decision": "The workflow keeps a single review_verdict signal as THE decision. Reviewers vote via `meridian review complete --verdict`; the Meridian coordinator applies the quorum policy and fires the one aion signal. Rejected: per-reviewer signals into the workflow \u2014 quorum policy is a Meridian concern, and every workflow re-implementing vote-counting is the wrong layer. Integration seam: Meridian needs the branch\u2192workflow-id mapping at review-request time.",
        "quote": "keeping the same signal thing but allowing... casting the votes... to Meridian and then Meridian provides the signal",
        "decided_by": "Tom, with Waffles"
      },
      {
        "id": "ADR-008",
        "title": "brief_dev replaces onatopp_dev inside the stacked-dev family",
        "decision": "Evolve in place: onatopp_dev.gleam is deleted and brief_dev.gleam takes its slot as stacked_dev's inner child; the outer arc keeps its live-proven contracts. Rejected: a parallel brief-dev family \u2014 two families serving one purpose is the zombie-code pattern ADR-002 prohibits, and the outer arc's provision/gate/review/land contracts took a full dogfood night to prove against real CLIs; duplicating them duplicates that risk.",
        "quote": "so brief Dev replacing on a top Dev in the stacked dev family. I guess that's fine but like I also don't mind sort of holding onto both for the time being",
        "decided_by": "Tom"
      }
    ],
    "checklist": [
      {
        "id": "C23",
        "text": "StackedDevInput carries the v2 brief document and resolved context in place of the four document strings; the input schema mirrors it with additionalProperties false and every cap/deadline still required."
      },
      {
        "id": "C24",
        "text": "stacked_dev spawns brief_dev (not onatopp_dev) and lifts its typed errors into the outer union; the gate child, review loop, signal contract, and land sequence are unchanged."
      },
      {
        "id": "C25",
        "text": "The landed merge contains the enriched brief file: implementation and execution record arrive on main in the same commit lineage."
      },
      {
        "id": "C30",
        "text": "Worker handlers for scout, dev_review, and enrich_brief mirror locals invocation-for-invocation; SERVED_ACTIVITIES lists every activity name the family dispatches."
      },
      {
        "id": "C31",
        "text": "Norn scout and review invocations use --print, --session-id or --resume, --output-schema with the generated stage schemas, and --output-format json; output parsing accepts the bare shape and the real {\"output\": ...} envelope only."
      },
      {
        "id": "C32",
        "text": "wire_compat.rs pins every payload that exists at its landing byte-compatible both directions against the Gleam codecs; the dispatch IO pins complete with the dispatch brief."
      }
    ],
    "stories": [
      {
        "id": "S2",
        "text": "As Tom, I want each brief's review gate to DM its assigned reviewers \u2014 usually AI leads, me for design-level calls \u2014 with the workflow parked until the verdict signal so that the decision point is explicit without me being the bottleneck."
      },
      {
        "id": "S18",
        "text": "As an AI lead reviewing code at the review gate, I want the review request and the enriched brief on the branch to carry everything a rigorous review needs \u2014 per-criterion acceptance evidence, measured gate results, attestation divergence \u2014 so that I can let nothing through without re-deriving context."
      },
      {
        "id": "S19",
        "text": "As an AI lead, I want the review_verdict signal to be sender-agnostic so that my verdict, cast through the Meridian coordinator, decides the run exactly as a human's would."
      }
    ],
    "constraints": [
      {
        "id": "CN1",
        "text": "Deterministic workflow code performs no filesystem reads, no ledger parsing, and no reference resolution: dispatcher activities resolve roadmap/decisions/checklist/stories references and assemble the brief context before any workflow logic sees it (P5)."
      },
      {
        "id": "CN2",
        "text": "Every cap, backoff, deadline, and concurrency limit is a required input field, schema-enforced with additionalProperties: false; the engine and the workflows impose no bound of their own (ADR-001, ADR-003)."
      },
      {
        "id": "CN3",
        "text": "enrich_brief is append-only: it refuses to alter any authored field (deep-compares the authored subset before writing) and fails terminally on divergence rather than silently overwriting."
      },
      {
        "id": "CN4",
        "text": "Stage sessions are deterministic and disjoint: '<branch>' for dev (existing), '<branch>-scout' for scout, '<branch>-review' for review. The reviewer NEVER shares the dev session \u2014 fresh eyes are the point of the stage (REVIEW.md)."
      },
      {
        "id": "CN5",
        "text": "Activity failure taxonomy: can't-execute is terminal failure; expected negative outcomes are successful results carrying data (P3). No retries of broken environments."
      },
      {
        "id": "CN6",
        "text": "The family deploys as a unit (shared content hash, child type = entry module name); all archives rebuilt and redeployed together on any change."
      },
      {
        "id": "CN7",
        "text": "Package stage-contract schemas are byte-identical to docs/design-system/schemas/ canon, enforced by a drift gate that runs with the test suite."
      },
      {
        "id": "CN8",
        "text": "The outer arc's live-proven CLI contracts are immutable in this cluster: request_review argv (branch positional first, --as Meridian), land = git commit in worktree then yg merge from repo_root, single review_verdict signal (ADR-006)."
      },
      {
        "id": "CN9",
        "text": "Hermetic tests run under gleeunit's sequential runner (exclusive PATH is VM-global); e2e suites never run concurrently with gleam builds in the same checkout."
      }
    ],
    "intention": "Design-system v2 briefs become executable: an all-norn pipeline (scout \u2192 dev \u2192 verify-fix \u2192 adversarial review \u2192 harden) runs as a durable aion workflow, enriching the brief in place at every stage, composed under the live-proven stacked_dev outer arc (provision \u2192 dev \u2192 gate \u2192 human review \u2192 land), with a dispatcher above that selects briefed work from the roadmap ledger and fans it out. When this is done, 'do this roadmap item' means authoring documents and approving a DM \u2014 the workflow does the rest, every stage durable across crashes, every claim recorded next to what was actually measured. aion develops aion; the dogfood becomes the dev process.",
    "design_path": "docs/design/brief-dev",
    "provenance": {
      "requested_by": "Tom",
      "quote": "Design-system v2 briefs become executable: an all-norn pipeline (scout \u2192 dev \u2192 verify-fix \u2192 adversarial review \u2192 harden) runs as a durable aion workflow, enriching the brief in place at every stage, c"
    }
  },
  "verify_fix_cap": 3,
  "review_cap": 1,
  "round_backoff_ms": 2000,
  "review_deadline_ms": 300000,
  "workspace_id": "2d5fdd51-1f25-45a4-8f86-4d4c978d1355",
  "clone_url": "https://github.com/tomWhiting/aion.git"
}
