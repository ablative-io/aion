{
  "id": "AT-011",
  "cluster": "aion-time-signals",
  "title": "race: first result wins, cancel and record the rest",
  "depends_on": ["AT-009"],
  "blocked_by": [],
  "checklist": ["C31"],
  "stories": ["S6"],
  "purpose": "Implement the race primitive: spawn N children, return the first result to arrive, then cancel the remaining children and record their cancellation. This is the pattern for primary/fallback calls and timeouts-via-race, built on the correlation/cancellation substrate from AT-009.",
  "task": "Implement race over a fixed list of child specs. Spawn each as a linked child carrying its correlation token (AT-009), block in selective receive for the first result to arrive, return it, and cancel the remaining in-flight children via the AT-009 cancel path, recording their cancellation.",
  "requirements": [
    {
      "id": "R1",
      "title": "Return the first result to arrive",
      "spec": "WHEN race is called with N child specs, THE SYSTEM SHALL spawn N linked children and return the first result to arrive.",
      "acceptance": [
        "race.rs defines race(specs) that spawns each spec as a linked child tagged with its correlation token (AT-009 R1) and returns the first arriving result.",
        "The winner is the first result message received, matched by token to its spawn index (the index is available on the result if the caller needs to know which won).",
        "A test spawns 3 children, delivers the second-spawned child's result first, and asserts race returns that result.",
        "If the first result to arrive is a failure, race returns that failure (race resolves on first settle — first to finish wins whether success or failure); a doc comment states this and a test covers a first-arriving failure."
      ],
      "files": {
        "create": ["crates/aion/src/concurrency/race.rs"],
        "modify": ["crates/aion/src/concurrency/mod.rs"]
      },
      "checklist": ["C31"],
      "stories": ["S6"]
    },
    {
      "id": "R2",
      "title": "Cancel and record the losers",
      "spec": "WHEN race has a winner, THE SYSTEM SHALL cancel the remaining in-flight children via the AT-009 cancel path AND record their cancellation.",
      "acceptance": [
        "After the first result settles, race cancels the remaining children (AT-009 R3), terminating their linked processes and recording cancellation for each.",
        "No losing child's later result is returned or recorded as a completion (the winner stands; late results are drained without effect).",
        "A test asserts that after the winner is chosen, the other two children are terminated and their cancellation recorded, and a late result from a loser does not alter the race outcome."
      ],
      "files": {
        "create": [],
        "modify": ["crates/aion/src/concurrency/race.rs"]
      },
      "checklist": ["C31"],
      "stories": ["S6"]
    }
  ],
  "boundaries": [
    "SHALL NOT reimplement correlation, matching, or cancellation — those are AT-009.",
    "SHALL NOT implement all or map — those are AT-010 and AT-012.",
    "SHALL NOT define the Gleam workflow.race surface — that is AF."
  ],
  "verification": [
    "cargo clippy --workspace --all-targets -- -D warnings passes.",
    "cargo test -p aion passes, including winner-selection and loser-cancellation tests.",
    "cargo fmt --check passes.",
    "Losing children are cancelled and recorded; late loser results have no effect (verified by test).",
    "No #[allow], #[expect], or #[ignore] directives in new code."
  ]
}
