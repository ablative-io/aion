{
  "id": "IG-008",
  "cluster": "aion-integration",
  "title": "Clean tracked process artifacts from repository",
  "depends_on": [],
  "blocked_by": [],
  "checklist": [],
  "stories": [],
  "purpose": "The repo contains ~11 GB of .yggdrasil-worktrees/ plus tracked .meridian/.claude process files, a committed empty server.log, and a .commit-msg.tmp. These are development artifacts that shouldn't be in the repository — they bloat clones, leak process state, and confuse new contributors.",
  "task": "Remove tracked development artifacts, add appropriate .gitignore entries, and document that worktrees and process files are local-only.",
  "requirements": [
    {
      "id": "R1",
      "title": "Gitignore coverage",
      "spec": "THE SYSTEM SHALL add .gitignore entries for .yggdrasil-worktrees/, .meridian/, .claude/, server.log, .commit-msg.tmp, and any other development process artifacts.",
      "acceptance": [
        "git status shows no untracked process artifacts after the changes.",
        "New worktrees and process files are automatically ignored.",
        ".gitignore entries cover .yggdrasil-worktrees/, .meridian/, .claude/, server.log, and .commit-msg.tmp."
      ]
    },
    {
      "id": "R2",
      "title": "Remove tracked artifacts",
      "spec": "THE SYSTEM SHALL remove currently tracked development artifacts from the repository index (git rm --cached) without deleting the local files.",
      "acceptance": [
        "git ls-files shows no .yggdrasil-worktrees entries.",
        "git ls-files shows no .meridian entries.",
        "git ls-files shows no .claude entries.",
        "git ls-files shows no server.log or .commit-msg.tmp entries.",
        "Local files remain on disk — only the git index is cleaned."
      ]
    },
    {
      "id": "R3",
      "title": "Document development setup",
      "spec": "THE SYSTEM SHALL add a CONTRIBUTING.md or development section noting that worktrees and process files are local-only and gitignored.",
      "acceptance": [
        "New developers understand that .yggdrasil-worktrees, .meridian, and .claude directories exist locally but aren't committed.",
        "The documentation explains which directories are gitignored and why.",
        "The documentation is discoverable from the root of the repository."
      ]
    }
  ],
  "boundaries": [
    "SHALL NOT rewrite git history (no git filter-branch or BFG).",
    "SHALL NOT delete local working files — only untrack from git.",
    "SHALL NOT modify .claude/settings.json or any configuration that affects Claude Code behavior."
  ],
  "verification": [
    "git status is clean after the changes.",
    ".gitignore covers all development process artifacts.",
    "git ls-files shows no process files (.yggdrasil-worktrees, .meridian, .claude, server.log, .commit-msg.tmp).",
    "Local development files remain on disk.",
    "CONTRIBUTING.md or equivalent documentation exists and is accurate."
  ]
}
