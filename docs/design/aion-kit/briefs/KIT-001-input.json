{
  "repo_root": "/Users/tom/Developer/ablative/aion",
  "brief_id": "KIT-001",
  "reviewers": [
    "Waffles the Terrible"
  ],
  "base_ref": "main",
  "placement": "local",
  "isolation": "worktree",
  "brief_document": {
    "id": "KIT-001",
    "cluster": "aion-kit",
    "title": "Opaque payload and the aion_kit package skeleton",
    "depends_on": [],
    "blocked_by": [],
    "checklist": [
      "C1",
      "C2",
      "C3",
      "C4",
      "C5",
      "C6",
      "C7",
      "C8",
      "C17",
      "C18"
    ],
    "stories": [
      "S3",
      "S5",
      "S6",
      "S7",
      "S8",
      "S10",
      "S11"
    ],
    "design_anchor": [
      "ADR-011",
      "ADR-012",
      "ADR-001",
      "ADR-002"
    ],
    "purpose": "Establishes the cluster's foundation and its keystone: the aion_kit Gleam package skeleton (manifest + declaration-only root) and the opaque sealed-payload mechanism that lets a deterministic workflow carry an activity result it never opens (ADR-012). The TYPE a workflow holds \u2014 Sealed \u2014 is added to the authoring SDK aion_flow (workflow code imports it); the HELPERS that produce and read it \u2014 seal, raw, peek \u2014 land in aion_kit/payload (worker-side primitives). The split is the design's central boundary call (see solution): hold-and-forward is workflow vocabulary, open-and-decode is activity vocabulary, and the line between them is the determinism boundary. This is the only intra-cluster dependency: KIT-002 (template) and KIT-003 (json) build on this skeleton. brief_dev is NOT reshaped here \u2014 that is RM-023; this brief ships and proves the mechanism with its own tests.",
    "task": "Create the gleam/aion_kit package: gleam.toml (depending on aion_flow, gleam_stdlib, gleam_json; dev-dep gleeunit; matching the gleam/aion_flow manifest shape exactly \u2014 erlang target, licences array, github repository) and the declaration-only root module src/aion_kit.gleam (a PackageRoot marker, matching aion_flow.gleam and aion_client.gleam). Add the Sealed pass-through type to aion_flow at src/aion/payload.gleam: an opaque type wrapping a content-type tag (String) and the raw payload bytes (String \u2014 one binary, not a decoded structure), with a constructor and the two accessors content_type and raw_bytes, and NO accessor that returns a decoded view. Create src/aion_kit/payload.gleam with the helpers over aion_flow's Sealed: seal(value, encoder, content_type) building a Sealed from a typed value via a caller-supplied json encoder and explicit content-type (no default content-type \u2014 ADR-001); raw(sealed) returning the opaque bytes and content_type(sealed) returning the tag for a consuming activity to decode with its own decoder; peek(sealed, decoder) decoding ONLY a caller-supplied thin facts view (the ADR-012 facts projection over a Sealed), ignoring unnamed fields, returning Result(view, codec.DecodeError) \u2014 typed failure, never a panic. Write the tests: gleam/aion_flow/test/payload_test.gleam (Sealed construction, accessors, and the hold-without-decode property \u2014 seal a value, forward only raw bytes, decode on the far side) and gleam/aion_kit/test/payload_test.gleam (seal/raw/peek round-trip, and peeking a small facts view from a sealed report far larger than the view). In scope: the package skeleton, the Sealed type, the payload helpers, their tests, and the purity/boundary guarantees C17/C18 for the files this brief introduces. Out of scope: template (KIT-002), json wrangling (KIT-003), and any reshape of brief_dev or its prompts.gleam/enrich.gleam (RM-023).",
    "requirements": [
      {
        "id": "R1",
        "title": "aion_kit package skeleton",
        "spec": "Create gleam/aion_kit/gleam.toml and gleam/aion_kit/src/aion_kit.gleam. THE SYSTEM SHALL declare the package name 'aion_kit', target 'erlang', licences [\"MIT\", \"Apache-2.0\"], and the github repository { type, user: \"tomWhiting\", repo: \"aion\" }, matching the gleam/aion_flow manifest shape. THE SYSTEM SHALL list dependencies gleam_stdlib (>= 0.34.0 and < 2.0.0), gleam_json (>= 2.0.0 and < 4.0.0), and aion_flow, and dev-dependency gleeunit (>= 1.0.0 and < 2.0.0). The root module SHALL contain only module documentation and a declaration-only PackageRoot marker type \u2014 no functions, no re-exports of submodules. THE SYSTEM SHALL NOT add a dependency on any worker crate, the norn driver, or any agent/process-spawning runtime (ADR-011).",
        "acceptance": [
          "gleam/aion_kit/gleam.toml sets name = \"aion_kit\" and target = \"erlang\".",
          "The [dependencies] table lists gleam_stdlib, gleam_json, and aion_flow; the [dev-dependencies] table lists gleeunit; no other dependency is present.",
          "src/aion_kit.gleam defines exactly one public type PackageRoot with a single PackageRoot constructor and no public functions.",
          "grep -i 'norn' gleam/aion_kit/gleam.toml produces no matches.",
          "cd gleam/aion_kit && gleam build exits 0."
        ],
        "files": {
          "create": [
            "gleam/aion_kit/gleam.toml",
            "gleam/aion_kit/src/aion_kit.gleam"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C1",
          "C2"
        ],
        "stories": [
          "S8",
          "S10"
        ]
      },
      {
        "id": "R2",
        "title": "The Sealed pass-through type in aion_flow",
        "spec": "Create gleam/aion_flow/src/aion/payload.gleam defining the Sealed type a workflow holds and threads between activities without decoding. THE SYSTEM SHALL model Sealed as an opaque type carrying a content-type tag (String) and the raw payload bytes (String). THE SYSTEM SHALL expose a constructor that takes a content-type and bytes, an accessor content_type returning the tag, and an accessor raw_bytes returning the bytes. THE SYSTEM SHALL NOT expose any accessor, method, or field that returns a decoded structured view of the bytes \u2014 holding a Sealed costs one binary on the workflow heap, never a decoded nested structure (ADR-012, CN3). The module SHALL NOT import aion/workflow, aion/activity, aion/query, or aion/signal, SHALL NOT perform IO, and SHALL contain no panic, assert, or let-assert.",
        "acceptance": [
          "src/aion/payload.gleam defines an opaque Sealed type; for s = the constructor called with content_type \"application/json\" and bytes \"{\\\"k\\\":1}\", content_type(s) == \"application/json\" and raw_bytes(s) == \"{\\\"k\\\":1}\".",
          "The module exposes no function returning a decoded view of the bytes (only content_type and raw_bytes read the contents, both returning String).",
          "grep -E 'import aion/(workflow|activity|query|signal)' gleam/aion_flow/src/aion/payload.gleam produces no matches.",
          "grep -E 'panic|let assert|assert ' gleam/aion_flow/src/aion/payload.gleam produces no matches in library code.",
          "cd gleam/aion_flow && gleam build exits 0."
        ],
        "files": {
          "create": [
            "gleam/aion_flow/src/aion/payload.gleam"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C4"
        ],
        "stories": [
          "S5",
          "S11"
        ]
      },
      {
        "id": "R3",
        "title": "seal, raw, and content_type helpers",
        "spec": "In gleam/aion_kit/src/aion_kit/payload.gleam, build the construction and forwarding helpers over aion_flow's Sealed. WHEN seal(value, encoder, content_type) is called, THE SYSTEM SHALL encode value to a JSON string via the caller-supplied encoder and wrap it in a Sealed tagged with the caller-supplied content_type. THE SYSTEM SHALL NOT supply a default content-type \u2014 the caller passes it explicitly (ADR-001). WHEN raw(sealed) is called, THE SYSTEM SHALL return the Sealed's opaque bytes so a consuming activity decodes them with its own typed decoder; content_type(sealed) SHALL return the tag. THE SYSTEM SHALL NOT decode the bytes inside seal or raw. The module SHALL NOT import aion/workflow, aion/activity, aion/query, or aion/signal, and SHALL contain no panic, assert, or let-assert.",
        "acceptance": [
          "For a value encoded by a json encoder producing \"{\\\"hello\\\":\\\"world\\\"}\", seal(value, encoder, \"application/json\") yields a Sealed s with raw(s) == \"{\\\"hello\\\":\\\"world\\\"}\" and content_type(s) == \"application/json\".",
          "A round-trip test seals a value, takes raw(s), decodes that String with the matching decoder, and recovers the original value \u2014 proving the activity-to-activity forwarding path (S3).",
          "seal takes the content-type as an explicit argument; there is no overload or branch that fills in a content-type when one is not provided.",
          "grep -E 'import aion/(workflow|activity|query|signal)' gleam/aion_kit/src/aion_kit/payload.gleam produces no matches.",
          "cd gleam/aion_kit && gleam test runs the payload suite and the seal/raw round-trip case passes."
        ],
        "files": {
          "create": [
            "gleam/aion_kit/src/aion_kit/payload.gleam"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C6",
          "C7"
        ],
        "stories": [
          "S3"
        ]
      },
      {
        "id": "R4",
        "title": "peek: a thin facts view from a Sealed",
        "spec": "Add peek to gleam/aion_kit/src/aion_kit/payload.gleam. WHEN peek(sealed, decoder) is called, THE SYSTEM SHALL decode ONLY the caller-supplied thin facts view from the Sealed's bytes and SHALL ignore every field the view's decoder does not name (the ADR-012 facts projection over a Sealed: the workflow reads the few fields it needs to route control flow \u2014 pass/fail, blocked, changed files, drift \u2014 without materialising the whole report). IF the bytes do not satisfy the thin decoder, THEN THE SYSTEM SHALL return a typed codec.DecodeError and SHALL NOT panic. peek SHALL NOT materialise or return any field outside the supplied view, and SHALL be pure (no IO, no clock, no entropy) so a workflow may call it across the determinism boundary (P3, invariant 2).",
        "acceptance": [
          "Given a Sealed whose bytes are a JSON object with ten fields, peek(sealed, decoder) where decoder names only two of them returns Ok of a two-field view and the other eight fields are absent from the result.",
          "peek(sealed, decoder) on bytes that are not valid JSON, or that lack a field the decoder requires, returns Error(codec.DecodeError(...)) \u2014 verified by asserting the Error branch, with no panic.",
          "A test seals a report value far larger than the facts view (e.g. a record with a long string field and a list) and peeks a small facts view (e.g. a single boolean and one string) successfully (C8, S6).",
          "grep -E 'panic|let assert' gleam/aion_kit/src/aion_kit/payload.gleam produces no matches.",
          "cd gleam/aion_kit && gleam test runs the payload suite and the peek-ignores-the-rest case passes."
        ],
        "files": {
          "create": [
            "gleam/aion_kit/test/payload_test.gleam"
          ],
          "modify": [
            "gleam/aion_kit/src/aion_kit/payload.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C8"
        ],
        "stories": [
          "S6"
        ]
      },
      {
        "id": "R5",
        "title": "Hold-without-decode property test on the aion_flow side",
        "spec": "Create gleam/aion_flow/test/payload_test.gleam proving the Sealed type's hold-and-forward property from the workflow's vantage. THE SYSTEM SHALL construct a Sealed, read its content_type and raw_bytes, and demonstrate that forwarding the Sealed (or its bytes) to a decoder on the far side recovers the value \u2014 WITHOUT the Sealed type exposing a decoded view at any point. THE SYSTEM SHALL assert that the only contents-reading surface is content_type and raw_bytes (both String-returning). The test SHALL NOT import aion_kit (the aion_flow package must prove its own type without depending downward).",
        "acceptance": [
          "The test constructs a Sealed with bytes \"{\\\"changed\\\":3}\", asserts raw_bytes equals that string and content_type equals the tag, then decodes the bytes with a json decoder and asserts the recovered value \u2014 the Sealed never yielding a decoded view itself.",
          "grep -E 'import aion_kit' gleam/aion_flow/test/payload_test.gleam produces no matches.",
          "cd gleam/aion_flow && gleam test runs payload_test and it passes.",
          "cd gleam/aion_flow && gleam format --check reports no changes for the new module and test."
        ],
        "files": {
          "create": [
            "gleam/aion_flow/test/payload_test.gleam"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C5"
        ],
        "stories": [
          "S5",
          "S7"
        ]
      },
      {
        "id": "R6",
        "title": "Skeleton builds, gates clean, and the boundary holds",
        "spec": "Wire the package so it builds, tests, and formats clean, and verify the cluster-wide purity and boundary guarantees for the files this brief introduces. THE SYSTEM SHALL ensure cd gleam/aion_kit && gleam build, gleam test, and gleam format --check all succeed against the aion_flow dependency. THE SYSTEM SHALL ensure no file in gleam/aion_kit/src nor gleam/aion_flow/src/aion/payload.gleam imports aion/workflow, aion/activity, aion/query, aion/signal, references 'norn', or spawns any process/port (CN1, ADR-011), and SHALL ensure library code in those files contains no panic, assert, or let-assert and reads no wall clock, entropy, or IO (CN2, CN7). This requirement carries C17 and C18 for the cluster; KIT-002 and KIT-003 re-verify the same properties for the files they add via their own verification steps (split noted in this task and theirs).",
        "acceptance": [
          "cd gleam/aion_kit && gleam build && gleam test && gleam format --check all exit 0.",
          "grep -rE 'import aion/(workflow|activity|query|signal)' gleam/aion_kit/src gleam/aion_flow/src/aion/payload.gleam produces no matches.",
          "grep -ri 'norn' gleam/aion_kit/src gleam/aion_flow/src/aion/payload.gleam produces no matches.",
          "grep -rE 'panic|let assert|spawn|erlang:open_port|process' gleam/aion_kit/src gleam/aion_flow/src/aion/payload.gleam produces no matches in library code (test modules excluded).",
          "Each source file under gleam/aion_kit/src and gleam/aion_flow/src/aion/payload.gleam is at or under 500 code lines (excluding comments and whitespace)."
        ],
        "files": {
          "create": [],
          "modify": [
            "gleam/aion_kit/gleam.toml",
            "gleam/aion_kit/src/aion_kit.gleam",
            "gleam/aion_kit/src/aion_kit/payload.gleam"
          ],
          "delete": []
        },
        "checklist": [
          "C3",
          "C17",
          "C18"
        ],
        "stories": [
          "S7",
          "S8"
        ]
      }
    ],
    "boundaries": [
      "SHALL NOT create aion_kit/template.gleam or any text-composition primitive \u2014 that is KIT-002.",
      "SHALL NOT create aion_kit/json.gleam or any project/merge/pluck helper \u2014 that is KIT-003.",
      "SHALL NOT modify, reshape, or rewire examples/stacked-dev (brief_dev, prompts.gleam, enrich.gleam, activities, locals) \u2014 consuming the toolkit is RM-023.",
      "SHALL NOT re-export, wrap, or alias aion_client's Payload or brief_dev's bespoke helpers \u2014 aion_kit is the replacement, not a shim (ADR-002).",
      "SHALL NOT add a default content-type, a size or depth cap, or any hardcoded bound to Sealed, seal, raw, or peek (ADR-001).",
      "SHALL NOT add an accessor or function that returns a decoded structured view of a Sealed's bytes (CN3) \u2014 only raw, content_type, and the caller-decoder peek read the contents.",
      "SHALL NOT add a dependency on a worker crate, the norn driver, or any agent/process-spawning runtime (ADR-011)."
    ],
    "verification": [
      "cd gleam/aion_kit && gleam build && gleam test && gleam format --check all exit 0.",
      "cd gleam/aion_flow && gleam build && gleam test && gleam format --check all exit 0 (the new src/aion/payload.gleam and test/payload_test.gleam build and run).",
      "grep -rE 'import aion/(workflow|activity|query|signal)' gleam/aion_kit/src gleam/aion_flow/src/aion/payload.gleam produces no matches.",
      "grep -ri 'norn' gleam/aion_kit/src gleam/aion_flow/src/aion/payload.gleam produces no matches.",
      "The Sealed type exposes only content_type and raw_bytes as contents-reading surface; peek lives in aion_kit/payload and takes the decoder from the caller (no decoded view escapes the type).",
      "A round-trip test proves seal -> raw -> caller-decode recovers the original value, and a peek test proves a small view decodes out of a far larger sealed report."
    ]
  },
  "resolved_context": {
    "adrs": [
      {
        "id": "ADR-001",
        "title": "No arbitrary limits, no assumed defaults",
        "decision": "Configurable values come from the builder/author or are deferred to the layer that owns them (e.g. beamr's own defaults). No caps, rate limits, or hardcoded defaults invented at the aion layer. Values are discussed before implementation. Rejected: convenience defaults \u2014 they are decisions made for the user without telling them.",
        "quote": "",
        "decided_by": "Tom"
      },
      {
        "id": "ADR-002",
        "title": "No backwards compatibility during the build",
        "decision": "Replace, don't add alongside. No compat shims, no zombie code, no #[deprecated] markers. Breaking changes are made cleanly and consumers move forward. Rejected: incremental deprecation cycles \u2014 they double the surface under test for an audience of zero.",
        "quote": "",
        "decided_by": "Tom"
      },
      {
        "id": "ADR-011",
        "title": "The standard library carries cross-cutting primitives only; runtime harnesses stay out",
        "decision": "aion_kit (the worker standard library) carries only primitives that apply across the board \u2014 data transformation/wrangling, rendering/templating, and the opaque payload \u2014 plus further cross-cutting primitives as they prove general. Runtime harnesses, the norn agent driver chief among them, stay as worker-side code that consumers (Meridian) own and integrate; they may live in a worker for convenience but never ship as standard-library surface. Rejected: bundling the norn/agent harness into the standard library \u2014 it couples a general toolkit to one runtime and ships a Meridian concern to every consumer.",
        "quote": "Norn's too specific. Like it's Norn at the runtime, the agent harness, like I wrote that a couple of weeks ago. So like it's not even like I don't think we should ship that as part of the standard library. That would be more something that Meridian would consume.",
        "decided_by": "Tom"
      },
      {
        "id": "ADR-012",
        "title": "Workflow code stays thin: large activity results ride as opaque payloads",
        "decision": "Workflow code stays thin. Large activity results ride between stages as opaque sealed payloads the workflow never opens; the workflow decodes only a small facts projection \u2014 the few fields it needs to route control flow (pass/fail, changed files, blocked, drift). Decoding and prompt rendering happen in the consuming activity on the worker, which has a full heap and runs the work once rather than on every replay. Rejected: decoding and rendering full reports in workflow code \u2014 it puts the determinism boundary's heavy lifting in the wrong place and was the regression that exposed the beamr crash.",
        "quote": "we're not changing the output of the agents right we're still keeping all of that which is not decoding it all within the workflow so it also stays packaged up and then it passes out to the worker the worker unpacks it and provides the prompt to the agent",
        "decided_by": "Tom"
      }
    ],
    "checklist": [
      {
        "id": "C1",
        "text": "gleam/aion_kit/gleam.toml exists naming the package aion_kit, target erlang, with dependencies gleam_stdlib, gleam_json, and aion_flow, and dev-dependency gleeunit, matching the gleam/aion_flow manifest shape (licences array, github repository)."
      },
      {
        "id": "C2",
        "text": "gleam/aion_kit/src/aion_kit.gleam is a declaration-only package-root module (a PackageRoot marker type and module docs only, no functions), matching the aion_flow/aion_client root convention."
      },
      {
        "id": "C3",
        "text": "cd gleam/aion_kit && gleam build and gleam test both exit 0 against the aion_flow dependency, and gleam format --check reports no changes."
      },
      {
        "id": "C4",
        "text": "aion_flow exposes a Sealed pass-through type in src/aion/payload.gleam carrying a content-type tag and the raw payload bytes as a String, with no accessor that returns a decoded structured view of the bytes."
      },
      {
        "id": "C5",
        "text": "A workflow can hold a Sealed and forward it as the next activity's input without decoding: a test seals a value, reads only its raw bytes, and decodes the value only with an explicit decoder on the far side \u2014 the Sealed type itself never materialises the decoded structure."
      },
      {
        "id": "C6",
        "text": "aion_kit/payload.seal turns a typed value into a Sealed via a caller-supplied json encoder and an explicit content-type, inventing no default content-type."
      },
      {
        "id": "C7",
        "text": "aion_kit/payload.raw returns the Sealed's opaque bytes (and content_type accessor returns the tag) so a consuming activity can decode them with its own typed decoder."
      },
      {
        "id": "C8",
        "text": "aion_kit/payload.peek decodes ONLY a caller-supplied thin facts view from a Sealed, ignoring every field the view does not name, and returns a typed decode error (never a panic) when the bytes do not satisfy the thin decoder; a test peeks a small facts view out of a sealed report far larger than the view."
      },
      {
        "id": "C17",
        "text": "No file in gleam/aion_kit/src nor gleam/aion_flow/src/aion/payload.gleam imports aion/workflow, aion/activity, aion/query, aion/signal, references the norn agent driver, or spawns any process/port \u2014 grep for those imports and for 'norn' over the package finds no matches (CN1, ADR-011)."
      },
      {
        "id": "C18",
        "text": "No public function in aion_kit or the Sealed accessors reads a wall clock, draws entropy, or performs IO; library code contains no panic, assert, or let-assert \u2014 decode and lookup failures are Result/Option values (CN2, CN7)."
      }
    ],
    "stories": [
      {
        "id": "S3",
        "text": "As an activity author, I want to seal a large typed result into an opaque payload and to unseal one I receive with my own decoder so that I do the heavy decode once, on the worker, rather than forcing the workflow to open it."
      },
      {
        "id": "S5",
        "text": "As a workflow author, I want to hold an activity result as an opaque payload and pass it straight to the next activity without decoding it so that my deterministic workflow heap stays small and replay never re-decodes a large report."
      },
      {
        "id": "S6",
        "text": "As a workflow author, I want to peek only the few facts I need to route control flow (pass/fail, blocked, changed files, drift) out of an opaque payload so that I can branch on a result without materialising its whole structure."
      },
      {
        "id": "S7",
        "text": "As a workflow author, I want every toolkit primitive to be pure and deterministic so that I can call the same projection and JSON helpers from workflow code or activity code without risking a replay divergence."
      },
      {
        "id": "S8",
        "text": "As a future maintainer, I want the toolkit scoped to cross-cutting primitives with the runtime harnesses kept out so that I can tell at a glance what belongs in the standard library and what is a consumer's own concern."
      },
      {
        "id": "S10",
        "text": "As a Meridian engineer building a worker, I want to depend on aion_kit and use its templating, JSON wrangling, and payload helpers without it dragging in the norn agent driver so that the toolkit couples to no agent runtime and I integrate my own harness on top."
      },
      {
        "id": "S11",
        "text": "As a Meridian engineer, I want the opaque payload type to live in the authoring SDK my workflows already import so that threading a sealed result between activities needs no extra dependency in my deterministic workflow code."
      }
    ],
    "constraints": [
      {
        "id": "CN1",
        "text": "Runtime-agnostic: no module in aion_kit or the aion_flow payload module imports or references the norn agent driver, any agent/process-spawning runtime, or engine-runtime internals; grep for 'norn' and for process/port spawning in the package finds nothing (ADR-011)."
      },
      {
        "id": "CN2",
        "text": "Pure and deterministic: every public function in aion_kit/template, aion_kit/json, aion_kit/payload, and every aion_flow Sealed accessor reads no wall clock, draws no entropy, and performs no IO \u2014 they are total functions of their arguments and safe to call from workflow code or activity code (invariant 2)."
      },
      {
        "id": "CN3",
        "text": "Hold-without-decode: the Sealed type exposes no API that returns a decoded structured view of its contents; the only reads are raw (the opaque bytes) and peek (a caller-supplied thin decoder). Holding a Sealed on the workflow heap costs one binary, never a decoded nested structure (ADR-012)."
      },
      {
        "id": "CN4",
        "text": "No imported wire schemas and no $ref/$defs/oneOf/anyOf/default in anything this cluster introduces; aion_kit ships no JSON schema files, and the json module is value-level wrangling, not schema validation (ADR-007)."
      },
      {
        "id": "CN5",
        "text": "No arbitrary defaults or limits: no module supplies a default content-type, a default merge policy, a size or depth cap, or any hardcoded bound; absent input (a missing projection field, a missing pluck path, a decode failure) is returned as a typed result the caller handles (ADR-001)."
      },
      {
        "id": "CN6",
        "text": "No backwards-compatibility shims: aion_kit does not re-export, wrap, or alias brief_dev's bespoke prompts.gleam/enrich.gleam helpers or aion_client's Payload; it is the replacement those call sites move to under RM-023, not a parallel path (ADR-002)."
      },
      {
        "id": "CN7",
        "text": "Each source file stays at or under 500 code lines (excluding tests, comments, whitespace); no #[allow]-equivalent suppression, and no Gleam panic/assert/let-assert in library code \u2014 decode and lookup failures are returned as Result/Option values."
      }
    ],
    "intention": "Authoring a new aion workflow family should be writing prompts, schemas, and a gate \u2014 not re-deriving the data plumbing underneath every one. When this lands, the plumbing that brief_dev hand-rolled inside one example (text composition for prompts, JSON wrangling for reports and enrichment, and a way to carry an activity result a workflow never wants to open) is a small, well-tested package that ships with the worker, so the next family inherits all of it for free. aion_kit is a toolbox of cross-cutting primitives, deliberately lean: it carries only what genuinely applies across the board, and it stays out of the determinism boundary's way by being pure. The norn agent driver and every other runtime harness stay where they belong \u2014 in the worker layer that consumers own (ADR-011) \u2014 so the standard library couples to no particular agent runtime and ships no consumer-specific concern.",
    "design_path": "docs/design/aion-kit",
    "provenance": {
      "requested_by": "Tom",
      "quote": "Authoring a new aion workflow family should be writing prompts, schemas, and a gate \u2014 not re-deriving the data plumbing underneath every one. When this lands, the plumbing that brief_dev hand-rolled i"
    }
  },
  "verify_fix_cap": 3,
  "review_cap": 1,
  "round_backoff_ms": 2000,
  "review_deadline_ms": 300000,
  "workspace_id": "2d5fdd51-1f25-45a4-8f86-4d4c978d1355",
  "clone_url": "https://github.com/tomWhiting/aion.git"
}
