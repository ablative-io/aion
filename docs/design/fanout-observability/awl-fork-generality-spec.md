# AWL direct path — fork generality + ride-along refusals (lane `awl-fork-generality`)

## 0. Where you are working, and what is already there

- Repo: `/Users/tom/Developer/ablative/aion`. Lane worktree **already exists**: `/Users/tom/Developer/ablative/aion/.worktrees/awl-fork-generality`, branch `awl-fork-generality`, based on `awl-pipes` tip `aa6ab892` (identical commit to `awl-pipes`). Do not create a new worktree; work here. All paths below are relative to this worktree unless absolute.
- **The worktree is dirty with sanctioned-looking but UNFINISHED, NON-COMPILING WIP** (uncommitted): modified `crates/aion-awl/src/mir/{func.rs, print/function.rs, lower/{activity,build,child_call,driver,expr,flow,fork_child,forks,mod,outcome,pipes,slots}.rs}` plus a new untracked `crates/aion-awl/src/mir/lower/wait.rs` (441 lines) and a stray untracked `awl-pipes-gates/` dir (harmless residue; you may delete it). The WIP implements most of this lane's ride-alongs. **Treat the WIP as salvage: audit it against this spec, complete it, do not blindly trust it.** Nothing in it has been gated or tested.
- Baseline oracle census, measured by me on clean `awl-pipes` (`cargo test -p aion-awl --lib every_lowered_fixture_emits_and_validates -- --nocapture`): **`BC-3 oracle: 51 fixtures emitted + validated, 18 refused`** (69 valid fixtures; main was 46/23 before pipes). You must report the post-lane census verbatim.
- Gates dir: create `<worktree>/awl-fork-generality-gates/` (untracked); redirect every full cargo/gleam output there; keep an exit-code manifest file listing every gate and its code. Never pipe cargo output through grep/tail/head. `CARGO_TARGET_DIR=/Users/tom/Developer/ablative/aion/target` on every cargo command.
- Workspace laws: no `unwrap`/`expect`/`panic` anywhere incl. tests; no `#[allow]`/`#[expect]`/`#[ignore]`; files ≤500 CODE lines; `mod.rs` re-exports only; backtick identifiers in doc comments (doc_markdown is DENY). Stage explicit paths only; commit on `awl-fork-generality` with trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; no push, no merge, no main, no deploys.

## 1. The architecture in one paragraph

Reference semantics = the Gleam emitter (`crates/aion-awl/src/emitter/*`). Direct path = AST→MIR (`src/mir/lower/*`) then MIR→BEAM (`src/mir/select/*`). Everything the direct path cannot lower must refuse with a clean `LowerError::Unsupported` carrying the same diagnostic class as the reference (`src/mir/lower/flow.rs:14-33` doc header). Fork statements already lower on `awl-pipes`: collection action forks via lifted `workflow.map`/`try_fold` bodies (`src/mir/lower/fork_action.rs`), collection child forks via spawn-all/ordered-await folds (`src/mir/lower/fork_child.rs`), named forks via one `workflow.all` — typed when homogeneous, raw wire-unified twins + per-position decode when heterogeneous (`src/mir/lower/fork_named.rs:21-109`). Slot planning reserves lifted-fn refs before lowering (`src/mir/lower/build.rs`, `src/mir/lower/slots.rs`); a hole or misalignment is a `Planning` error.

## 2. Gap inventory (verified against code, emitter vs direct)

### 2a. The one remaining FORK-STATEMENT gap: call-site config on fork branches
- Emitter ACCEPTS site config on fork branches: collection action fork passes `fork.call.config` into `activity_value` for both parallel and sequential forms (`src/emitter/forks.rs:218-229`, the `activity_value(emitter, &fork.call.call, fork.call.config.as_ref(), …)` call at :223-229); homogeneous named fork at `src/emitter/forks.rs:300-313` (`activity_value(…, branch.config.as_ref(), …)` :304-311); heterogeneous named fork via `activity_value_raw(…, call.config.as_ref(), …)` at `src/emitter/forks.rs:351-366` (:359-365). Per-key merge site-over-declaration lives in `src/emitter/stmts.rs:84-104` (retry, then timeout, then task_queue always, then node).
- Direct REFUSES both fork forms with the global `call-site config` class: `src/mir/lower/forks.rs:325-327` (collection, inside `collection_branch`, :313-354) and `src/mir/lower/fork_named.rs:32-34`. This is the recorded BC-2b-5 parity exception (`src/mir/lower/forks.rs:27-34` doc header; ledger entry `docs/design/aion-authoring/awl/AWL-BC-IR.md:133-145`). **This lane closes it** — Phase 2.
- Child branches: the emitter refuses config on a child fork branch with the child-config class (`src/emitter/forks.rs:136-141`), same wording as the child call statement (`src/mir/lower/child_call.rs:110-118` already carries the exact string on the direct side: "`node`/`timeout` cannot pin a child workflow call — the engine routes children, not a queue"). Also checker-caught upstream (`src/checker/walk.rs:382-392`), and site `retry` is a check error (invalid fixture `tests/fixtures/rev2/declarations/invalid/call_site_retry_override.awl`), so site config = node/timeout only in check-clean documents. Keep the defensive refusal, but with the CHILD class, not the global class.

### 2b. Fork shapes that stay REFUSED — language boundary, not a direct gap (document + pin, do NOT implement)
- Collection fork body beyond one unbound call: emitter's own stopgap gate (`src/emitter/forks.rs:48-53`, `single_action_branch` :402-407). Direct mirrors at `src/mir/lower/forks.rs:318-324`. The SDK has no per-item statement-sequence primitive (`workflow.map` takes one activity per item — `gleam/aion_flow/src/aion/workflow.gleam:84-91`). Richer per-item bodies beyond one configured call are the LANGUAGE boundary; do not invent semantics.
- Named fork child branches (`src/emitter/forks.rs:280-283` ↔ `src/mir/lower/fork_named.rs:37-43`) and non-action named branches (`src/emitter/forks.rs:288-294` ↔ `fork_named.rs:44-49`).
- Indexing inside a PARALLEL fork branch (`src/emitter/forks.rs:174-179, 249-254` — non-empty branch prelude — ↔ `src/mir/lower/forks.rs:342-348`). Sequential branches DO allow indexing on both sides (emitter flushes the branch prelude inside the fold body, `src/emitter/forks.rs:238-245`; direct only gates when `!sequential`).
- Mid-chain routes: the emitter's Plan refuses routes to non-entry steps (`src/emitter/graph.rs:331-345`); direct mirrors via `region_of_entry` miss (`src/mir/lower/flow.rs:422` in the WIP tree). Parity refusal — the lane prompt listed this as a possible ride-along; it is NOT one, the emitter refuses it too. Document.
- Indexing inside outcome guards (emitter: non-empty guard prelude errors in `src/emitter/outcomes.rs`; the WIP added the mirroring refusal `src/mir/lower/outcome.rs:23-37`, class `indexing inside an outcome guard`). Keep.
- `sort` over non-comparable key (`src/mir/lower/pipes.rs:152-159`, pinned by `src/mir/deferred_tests.rs:94-123`). Keep.

### 2c. Ride-alongs — WIP state (audit + complete; each is emitter-supported)
The uncommitted WIP already implements, per my diff read (verify each against the cited reference):
1. **Call-site config on plain action calls**: `activity_value`/`activity_call` grew a `site_config` param and `apply_action_config` does the per-key `site.or(decl)` merge in reference order (WIP `src/mir/lower/activity.rs:42-51, 103-112, 151-215`; reference `src/emitter/stmts.rs:84-104`). `flow.rs::lower_call` passes `call.config.as_ref()` (WIP `src/mir/lower/flow.rs:320-360`). Committed baseline refused with the global class.
2. **Awaited child call statement + fire-and-forget spawn**: `src/mir/lower/child_call.rs:120-216` (`lower_child_call_stmt` = `workflow.spawn_and_wait |> map_child_error` + TryBind, reference `src/emitter/stmts.rs:214-239`; `lower_spawn_stmt` = `workflow.spawn |> map_spawn_error`, result discarded, reference `src/emitter/stmts.rs:242-267`), wired in `flow.rs::lower_statement` (WIP :300-307). Shared `child_input_json` extracted and reused by `fork_child.rs` (WIP fork_child diff) and `pipe_child_stage`.
3. **`wait` with and without timeout**: new `src/mir/lower/wait.rs` — no-timeout = `workflow.receive({snake}_signal()) |> map_receive_error` + TryBind; timeout = lifted receive thunk + lifted 4-arm case fn returning through one host `TryBind`, two reserved slots per timeout wait (`count_wait_fns` at wait.rs:46-58), reference `src/emitter/stmts.rs:269-315`. Uses only select-supported ops (`Tail::If`/`Test::IsTagged`, `CallLocal`, `CallRt`, `TryBind`) — critically it does NOT use `Stmt::WaitTimeoutCase`, which select refuses (`src/mir/select/flow.rs:351`). Verify that stays true (grep). Runtime fns `WfReceive`/`WfWithTimeout`/`MapReceiveError` are already declared (`src/mir/runtime.rs:33-34,46`). Planning: `FnPlan.signals` map + `FnPlan.waits` slot vec (WIP `build.rs:78-87, 201-209, 244-256, 272-273`), `WaitSlots` (WIP `slots.rs:72-122`), `FnOrigin::Wait` (WIP `func.rs:125-131`, printer `print/function.rs:53`), driver appends waits after forks (WIP `driver.rs:89, 97-102`). Wait slots follow all fork slots so wait-free modules keep byte-identical `FnRef`s — preserve that invariant.
4. **`Expr::Index` over an arbitrary base**: WIP `src/mir/lower/expr.rs:172-217` — lowers the base via `lower_expr` (any base, not just vars), then `RuntimeFn::RtIndex` (`aion@awl@runtime:index/3`) with the byte-identical out-of-range message `"index {index} out of range at line {line}, column {column}"` + TryBind; reference prelude `src/emitter/exprs.rs:240-251`. Select-compatible (CallRt takes any `Value`; the `require_var` restriction at `src/mir/select/flow.rs:65-73` applies to `FieldGet`, not `CallRt`).
5. **Non-empty list literals**: WIP `src/mir/lower/expr.rs:143-166` — each item as a PLAIN expression (never `lower_arg_for`, no per-item `Some(...)` wrapping), element type = first item's type; reference `src/emitter/exprs.rs:209-216` and `:93-96`. Rides the already-landed `ListNew` fix (`src/mir/select/emit.rs` `list_new` — empty list materializes nil; commits 36543e0e/091e6d1f; runtime pin `tests/runtime_codecs/fix_cycle.rs:58-94`). Committed baseline refused `non-empty list literal`.
6. **Outcome-guard indexing refusal (parity)**: WIP `src/mir/lower/outcome.rs:23-37`, using `expr_contains_index` which the WIP moved to `src/mir/lower/expr.rs:222-232`.

### 2d. Compile breakage to fix first
`activity_value` grew to 8 params but two CLEAN (unmodified) files still call the 7-arg form → the 3 build errors:
- `src/mir/lower/fork_named.rs:67` — `activity_value(ctx, plan, &branch.call, None, scope, stmts, raw)`
- `src/mir/lower/fork_action.rs:233-235` (`build_map_body`) and `:273-275` (`build_fold_body`) — `activity_value(ctx, build.plan, build.call, None, &fn_scope, &mut stmts, false)`
Do NOT just insert `None`: these three sites are exactly where Phase 2 threads the real branch config. Fix and feature land together.

## 3. Phase plan

### Phase 0 — restore the build, snapshot the WIP
1. `cargo build -p aion-awl` (expect the 3 errors above). Apply Phase 2's config threading (below) at those sites — that both fixes the build and closes the fork gap.
2. Run `cargo fmt` (whole tree), then full `cargo test -p aion-awl` to a gates file. EXPECT failures in the un-reconciled ratchet pins (Phase 3 list); anything else failing is a WIP defect — fix it against the cited reference semantics before proceeding.
3. Commit checkpoint on the lane branch (explicit paths: the 14 dirty files + `wait.rs` + any Phase 2 edits) so salvage is not lost.

### Phase 1 — audit the WIP ride-alongs (verify, don't trust)
For each of 2c items 1-6, read the WIP implementation side-by-side with the cited emitter reference and confirm behavioral equivalence, especially:
- Config merge ORDER: retry → timeout → task_queue (always) → node (`src/emitter/stmts.rs:84-104` ↔ WIP `activity.rs:151-215`). Retry via `activity.RetryPolicy(max_attempts, Fixed(delay) | Exponential(initial, 2.0, max))` (`src/emitter/stmts.rs:141-156` ↔ `activity.rs:238-281`).
- Child spawn ABI: 6-arg string-name spawn tuple — name literal, T-WIT witness closure, JSON input, `JsonValueCodec` input codec, output codec, `ErrCodec` (`src/mir/lower/child_call.rs:26-58`); `needs_child_witness` must return true for ALL reachable child-spawn forms now (WIP `build.rs:88-90` comment change says it does — verify the traversal in `src/mir/lower/forks.rs:93-125` was actually extended to statement-level child calls/spawns/waits or that the witness planning happens elsewhere; if a statement-level child call can reach `spawn_wait_args` with `plan.child_witness == None` you get a Planning error at lower time — write a focused test: a document whose ONLY child use is an awaited statement call).
- Wait slot alignment: every timeout wait consumes exactly 2 slots in traversal order (pre-order into loop bodies, early-stop after a route — `wait.rs:46-58` mirrors `forks.rs:58-70`); `WaitSlots::append_into` misalignment is a Planning error (`slots.rs:108-121`).
- `expr_contains_index` coverage (moved helper, `expr.rs:222-232`): must include every recursive expr position (`CollectionPredicate` subject/predicate appear absent — compare against `src/emitter/graph.rs:415-454` `expr_refs` coverage and extend if the checker admits indexes there).

### Phase 2 — fork-branch call-site config (closes BC-2b-5)
Design, mapped 1:1 to the reference:
1. `src/mir/lower/forks.rs::collection_branch` (:313-354): move the classification (action vs child) BEFORE the config gate. New behavior:
   - Action branch: carry `branch.config.as_ref()` in `CollectionBranch` (add a `config: Option<&'f crate::ast::ConfigLine>` field; the struct currently holds only `call`/`returns`/`kind`, :303-307).
   - Child branch with config: refuse with the CHILD class — reuse the exact string from `src/mir/lower/child_call.rs:112-118` (factor `child_config_refusal` to `pub(super)` and call it), anchored on `branch.span`. This replaces the current global-class refusal for the child case and matches `src/emitter/forks.rs:136-141`.
   - Keep the parallel-indexing gate (:342-348) and the single-unbound-call gate (:318-324) untouched.
2. Thread config through the action fan-out: add `config: Option<&'a crate::ast::ConfigLine>` to `ActionFork` (`src/mir/lower/fork_action.rs:20-32`) and `ForkBuild` (:149-160); pass it as `site_config` in `build_map_body` (:233-235) and `build_fold_body` (:273-275) instead of `None`. No capture changes: `ConfigLine` holds only literals (durations, node names, retry counts) so `branch_free_names` (`forks.rs:358-367`) is unaffected.
3. Named forks: in `src/mir/lower/fork_named.rs` delete the refusal (:32-34) and pass `branch.config.as_ref()` at :67 (both the homogeneous typed path and the heterogeneous raw path go through the same `activity_value`, which applies `apply_action_config` regardless of `raw` — matching the emitter, where `activity_value_raw` shares `activity_value_form` with config applied, `src/emitter/stmts.rs:44-105`).
4. Child fan-out unchanged (`fork_child.rs` takes no config path; the refusal happens in `collection_branch`).
5. Update the stale docs: `src/mir/lower/forks.rs:27-34` header paragraph (the deliberate-exception note) — rewrite to state config now lowers on action branches with the per-key merge, child branches keep the child class; and update the ledger entry `docs/design/aion-authoring/awl/AWL-BC-IR.md:133-145` (mark LANDED, name the new pins).

### Phase 3 — ratchet/pin reconciliation (every one of these WILL fail or lie after Phases 0-2; reconcile deliberately)
1. `src/mir/fork_tests.rs::fork_branch_call_site_config_refuses_with_the_scope_class` (:358-403) — FLIP into a support pin, e.g. `fork_branch_call_site_config_lowers_with_per_key_merge`: lower both bodies (named branch with `timeout 30m`, collection branch with `node edge01`), assert Ok, and assert the printed MIR (`print` module, same technique as :39-135) shows the config runtime calls (`act_timeout`/`act_node` imports) INSIDE the lifted fork fn (collection) and in the branch sequence before `workflow.all` (named). Add a third case: child collection fork branch with config refuses with the child-class message (LowerError::new, not Unsupported — assert the rendered string).
2. `tests/compile.rs::call_site_node_override_yields_the_override` (~:250-300) — the compile now succeeds; keep the `action_requirements` row assertions, replace the refusal match with a success assertion (compiled artifact non-empty).
3. `tests/compile.rs::refusal_passthrough_is_lossless` (:349-380) — `guard_optional_wait.awl` now lowers (wait landed); repoint to a still-refused fixture: `tests/fixtures/rev2/loop-outcomes/valid/on_failure_compensation.awl` (refuses `on failure` while Phase 5 defers it).
4. `src/mir/deferred_tests.rs::combined_loop_fixtures_advance_to_the_next_refusal` (:26-47) — `ship_release_combined` advances from `wait` to `on failure`; fixture `dev_brief` stays `parallel region` if Phase 4 defers, else advances/lowers. Update the assertions to the new honest refusals.
5. `src/mir/deferred_tests.rs::deferred_fixture_errors_cleanly` (:49-59) — `guard_optional_wait` lowers now; flip to a lowering+verify pin (like `post_join_combinator_now_lowers`, :63-89).
6. `src/mir/deferred_tests.rs::ordinary_call_index_refuses_as_expression_at_the_index_span` (:129-164) — indexes lower now; flip to a support pin (lower + `verify` + assert the module contains the `RtIndex` import), preserving the span-anchoring intent by asserting the runtime out-of-range MESSAGE literal for line 13 col 18 appears among the module literals.
7. `src/mir/fork_tests.rs::stopgap_refusals_match_the_reference_classes` (:299-356) and `named_child_branch_refuses_cleanly` (:407-438) — unchanged; they pin the language boundary. Re-run and keep green.
8. MIR goldens under `tests/mir-goldens/` — fixtures that newly lower (wait/spawn/config/index fixtures) may need golden additions; fixtures that already lowered must NOT change bytes (config threading passes `None` semantics identically when no site config exists — `apply_action_config` with `site=None` reduces to the old decl-only path; verify by diffing goldens).

### Phase 4 — dependency-parallel regions (OPTIONAL: implement if budget allows, else defer WITH the reason below written into `flow.rs`'s header)
Gap: direct refuses any multi-member layer (`src/mir/lower/flow.rs:105-120`, class `parallel region`). Emitter is richer twice over (`src/emitter/steps.rs`):
- Single-bare-call layers (every member step = one unbound declared-action call, no outcomes/on-failure — `layer_calls`, :214-230) dispatch as ONE `workflow.all`: typed when homogeneous (:235-282), raw twins + per-position decode when heterogeneous (delegates to `lower_hetero_parallel`, :247-248 → `src/emitter/forks.rs:351-400`).
- Richer layers degrade to WRITTEN ORDER with an explicit generated comment (:169-180). The MIR twin of that visibility marker already exists: `FlowFn.degraded_parallel` (`src/mir/func.rs:167`, printed as S13 by `src/mir/print/function.rs:35-37`), currently always false.
Design if implemented: generalize `lower_region` to flatten `region.layers` into the step sequence the emitter walks (each layer's members in written order); for a multi-member layer, first try the single-call-layer gate (mirror `layer_calls`), lowering the typed/raw `workflow.all` by REUSING `fork_named`'s machinery (`AssertList` binds + `bind_branches` per-position decode, `fork_named.rs:97-166`); otherwise lower members sequentially and set `degraded_parallel: true` on the region `FlowFn` (`flow.rs:195-204`). Planning impact: `chain_params`/`plan.chains` (`build.rs`, `chain.rs`) assume one member per layer — the flattened order must be identical at plan and lower time; and hetero layers need raw twins planned, so `raw_action_inventory` (`src/mir/lower/forks.rs:130-172`) must also scan multi-member layers. Fixtures unlocked: `dag-fork/valid/after_multi_diamond.awl` (all-layer), `dag-fork/valid/release_pipeline_combined.awl` + `flagship/valid/dev_brief.awl` fixture (degraded). If deferred: keep class `parallel region`, document the two sub-shapes and this design sketch in the `flow.rs` header, and keep `deferred_tests` pinning `dev_brief` at `parallel region`.

### Phase 5 — explicit defers (write these into `src/mir/lower/flow.rs`'s header, keeping its existing structure :14-33)
- **`on failure`** (`flow.rs:216`, fixture pressure: `on_failure_compensation.awl`, `ship_release_combined.awl`): DEFER. Reason: the reserved `Stmt::Attempt` op exists in MIR (`src/mir/ops.rs:219-226`, verified at `src/mir/verify.rs:134`) but instruction selection REFUSES it (`src/mir/select/flow.rs:358` → `unsupported_stmt`), so support needs new select-side emission (attempt closure call + defs-tuple threading mirroring `src/emitter/steps.rs:301-354`), which is select-surface work beyond this lane's lowering scope. The lowering sketch stays recorded: lift the attempt body to a `FlowFn` returning `Ok(defs-tuple)`; host `CallLocal` + `Tail::If IsTagged(ok, arity 2)` with the compensation block (must end in a route — `src/emitter/steps.rs:485-501`) in the else arm; success continuation nests in the then-arm since reconvergence is impossible; refuse the body-terminal-route combination with the emitter's class (`src/emitter/steps.rs:302-311`).
- **Substeps** (`flow.rs:219, 316`): DEFER. Reason: zero valid-fixture pressure (no `rev2/**/valid` fixture contains substeps — verified by corpus grep), mechanical but large (per-substep fns over `plan.sub_params`/`sub_node`, sibling/parent route frames, parent-outcome boundary — reference `src/emitter/subs.rs:20-60`, shape gates `src/emitter/graph.rs:73-116`). `FnOrigin::SubStep` already exists.
- **Mid-chain routes**: NOT a gap — parity refusal (`src/emitter/graph.rs:331-345`); document as language boundary.
- **Multi-statement/bound collection-fork bodies**: LANGUAGE boundary (emitter stopgap is single unbound call per item, `src/emitter/forks.rs:48-53`); the operator's richer fan-out per-item bodies are served within the boundary by: per-item call-site config (Phase 2), sequential-branch indexing preludes (already lowerable), pipe-into-child and child fan-outs (landed in pipes/forks). Keep the refusal + pins.

## 4. Execution-parity test plan (crates/aion-awl/tests/runtime_codecs/)

Pattern = `fix_cycle.rs` / `pipes.rs`: direct-selected module + gleam-built reference module (REAL emitter, `harness::reference_module_at`) in ONE beamr VM (`harness::build_vm`), identical driver values, byte-for-byte result parity. Parity fixtures live in `tests/fixtures/parity/` (OUTSIDE rev2 so the BC-3 oracle denominator stays fixed — `tests/runtime_codecs/pipes.rs:21-31`). Throwaway gleam projects build under `target/awl-test-scratch` (`harness.rs:53-60`), never /tmp. Register any new test module in `tests/runtime_codecs.rs`.

New harness stubs (add to `tests/runtime_codecs/harness.rs`, following `dispatch_refused_ebin` :251-282 and `child_host_ebin` :139-231):
- `collect_echo_ebin()`: test `aion_flow_ffi` exporting `collect_all/2` and `collect_map/2` returning `{error, <<"terminal:", JoinedSpecs/binary>>}` where JoinedSpecs concatenates the spec strings in order. Why it works: `workflow.all`/`map` funnel through `ffi.collect_all/collect_map(id, specs)` where each spec is a JSON object embedding `correlation` (input order), `name`, the codec-encoded `input`, and the full merged `config` JSON (retry/timeout_ms/heartbeat_ms/labels/task queues/node) — `gleam/aion_flow/src/aion/workflow/concurrency.gleam:118-176, 208-223`. The `terminal:` prefix maps identically to `error.Terminal` on both sides (`concurrency.gleam:178-206`). Byte parity of the terminal error therefore proves per-item input bytes, per-branch merged config, AND dispatch order all match the reference.
- `dispatch_echo_ebin()`: `dispatch_activity(Name, Input, Config) -> {error, <<"terminal:", Name/binary, "|", Input/binary, "|", Config/binary>>}` — the per-item wire for sequential folds and plain calls (`gleam/aion_flow/src/aion/workflow/run.gleam:100-115`; config JSON built at dispatch).
- `collect_ok_ebin(payloads_per_call)` (for completion/order tests): `collect_map/2` returning `{ok, [P0, P1, …]}` with payloads valid for the action's return codec (e.g. return type `String` → payloads `<<"\"r0\"">>` etc.), letting the fan-out COMPLETE so the join binding and result ORDER are observable end-to-end.
- Wait stubs (Phase 1 proof): `receive_signal`-stubbing ffi returning a payload; `with_timeout(Fun, _Deadline)` calling the fun and wrapping per the SDK contract, plus a timed-out variant, to drive all 4 case arms (`src/emitter/stmts.rs:292-313` arm set; FFI names `gleam/aion_flow/src/aion/internal/ffi.gleam:53-74`). Derive the exact erlang tuple shapes from `aion/internal/ffi.gleam` + `aion/workflow/timer.gleam` before writing the stub; if the wire contract cannot be faithfully stubbed, fall back to dispatch-terminal parity + MIR print pins and SAY SO in the test doc comment (see open questions).

Required tests (names are prescriptive; one fixture may serve several):
1. `parallel_action_fork_site_config_dispatches_with_reference_parity` — parity fixture `fork_config.awl`: action with decl `node shell, timeout 20m`; step `fork item in items` whose single call carries `node edge01, timeout 5m`; `collect_echo_ebin`; assert direct == reference bytes AND the echo contains `edge01` (override) — proves per-key site-over-decl merge on the map path.
2. `sequential_action_fork_site_config_dispatches_per_item_with_reference_parity` — same fixture, `sequential` variant; `dispatch_echo_ebin`; parity on the first item's terminal echo.
3. `named_fork_homogeneous_site_config_parity` — two branches of the SAME action with different `node` pins → still typed `workflow.all`; `collect_echo_ebin`; parity proves both configs present in source order.
4. `named_fork_heterogeneous_site_config_parity` — two different actions, one configured → raw-twin path; `collect_echo_ebin`; parity proves wire-identical raw inputs + per-branch config (guards the `raw=true` + `apply_action_config` composition).
5. `fork_inside_loop_completes_two_passes_with_reference_parity` — adapt `tests/fixtures/rev2/loop-outcomes/valid/fork_in_loop_live_ins.awl` into `tests/fixtures/parity/`; `collect_ok_ebin`; loop must run ≥2 passes with a collection fork in the body (exercises fork-slot consumption inside loop lowering, `src/mir/lower/forks.rs:58-70`); assert final joined results + order parity.
6. `sequential_fork_branch_index_prelude_executes_with_reference_parity` — sequential fork whose branch call argument indexes (`check(doc: docs[0])`); `dispatch_echo_ebin`; parity pins that sequential branches keep index preludes while `stopgap_refusals` keeps the parallel refusal.
7. `plain_call_site_config_merges_per_key_with_reference_parity` — ride-along 1: decl `node a, timeout X, retry …` + site `node b`; `dispatch_echo_ebin`; parity + echo shows `b` and the DECL retry (site retry is checker-illegal).
8. `awaited_child_call_binds_result_with_reference_parity` and `spawn_fire_and_forget_dispatches_with_reference_parity` — ride-along 2: child-host-style stub (`spawn_child`/`await_child` with process-dictionary correlation, precedent `harness.rs:139-231`); awaited call's bound result flows into the route payload; spawn discards.
9. `wait_signal_binds_payload_with_reference_parity` and `wait_timeout_arms_execute_with_reference_parity` — ride-along 3: no-timeout bind, then timeout waits driven through Ok / TimedOut / Inner arms via the wait stubs; assert `Option` wrapping parity (`Some(value)` / `None`).
10. `index_and_lists_execute_with_reference_parity` — ride-alongs 4+5 in one fixture: non-empty list literal, index over a field base (non-var base), concat; out-of-range variant asserts the byte-identical runtime message (line/column-bearing, `src/mir/lower/expr.rs:195-199` ↔ `src/emitter/exprs.rs:240-251`).
11. (If Phase 4 implemented) `parallel_layer_single_calls_ride_one_all_with_reference_parity` (after_multi_diamond shape, collect echo) and `parallel_layer_rich_bodies_degrade_to_written_order_with_reference_parity` (release_pipeline shape, dispatch echo order assertion + a MIR print pin asserting `degraded_parallel: true`).

MIR-level pins (in `src/mir/fork_tests.rs`, print-based, technique at :39-135): Phase 3 item 1's flipped config pin; keep all existing fork pins green.

## 5. Gates (each to `awl-fork-generality-gates/<name>.log`, exit codes in `awl-fork-generality-gates/MANIFEST`)
1. `cargo build -p aion-awl`
2. `cargo clippy --all-targets -p aion-awl`
3. `cargo test -p aion-awl` — **report the BC-3 oracle census line verbatim** in your final output, alongside the 51/18 baseline. Expected movement if Phases 0-3 land and 4-5 defer: the wait fixtures (`signal_wait`, `header-types/combined`, `wait_timeout_optional`, `wait_and_sleep`, `step_bodies_combined`, `guard_optional_wait`), spawn fixtures (`spawn_detached`, `declarations_combined`), `call_site_override`, and `index_and_concat` flip to emitted; `on_failure_compensation` + `ship_release_combined` stay refused at `on failure`; `after_multi_diamond`, `release_pipeline_combined`, flagship `dev_brief` stay at `parallel region` unless Phase 4 lands. Enumerate every remaining refused fixture with its class in your report; any fixture refusing with an UNEXPECTED class is a defect.
4. `cargo test -p aion-awl-package`
5. `cargo check -p aion-server`
6. `cargo fmt` (run, never check).
No ops-console changes are in scope; do not touch `apps/aion-ops-console` or the embed.

## 6. Ordered checklist
1. [ ] Create `awl-fork-generality-gates/`; optionally delete stray `awl-pipes-gates/`.
2. [ ] Phase 2 config threading at the 3 broken call sites (`fork_named.rs:67`, `fork_action.rs:233-235, 273-275`) + `collection_branch`/`fork_named` refusal rework + struct field threading → build green.
3. [ ] `cargo fmt`; full `cargo test -p aion-awl` snapshot to gates; triage: Phase 3 pin failures expected, anything else = WIP defect to fix now.
4. [ ] Checkpoint commit (explicit paths, trailer line).
5. [ ] Phase 1 audit of ride-alongs 1-6 against cited reference lines; fix divergences; add the child-witness-planning focused test.
6. [ ] Phase 3 reconciliation items 1-8.
7. [ ] Harness stubs (`collect_echo_ebin`, `dispatch_echo_ebin`, `collect_ok_ebin`, wait stubs) + parity fixtures under `tests/fixtures/parity/` + tests 1-10 (11 if Phase 4).
8. [ ] Phase 5 documentation: `flow.rs` header defers, `forks.rs` header rewrite, `AWL-BC-IR.md:133-145` ledger update.
9. [ ] Phase 4 decision: implement (then tests 11 + deferred_tests updates) or defer with the documented reason; either way `dev_brief`/`ship_release` ratchet assertions must state the true current refusal.
10. [ ] Full gate battery to gates dir + MANIFEST; report census verbatim + remaining-refusal enumeration.
11. [ ] Final commit(s) on `awl-fork-generality`, explicit paths only, trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. No push, no merge.

---

## Execution notes (recorded by the lane during the build — deviations from the plan above)

- **The error-echo channel in §4 does not exist as described.** `awl/error.gleam::map_activity_error` deliberately collapses every activity error to `AwlActivityFailed("activity failed")`, so a terminal error carrying the joined specs never reaches the workflow result. The parity tests instead record the wire in the workflow process dictionary (`awl_ffi_echo`) inside the echo stubs and read it back with a per-test `aion_awl_echo_runner` erlc module running the driver in-process — the compared value is the `{Result, Echo}` pair, which is strictly stronger than the collapsed result alone.
- **§5 Phase 5 substeps rationale corrected**: there IS valid-fixture pressure (`loop-outcomes/valid/substeps_two_stage.awl` refuses at `substeps`); the defer stands on size vs. one fixture, not on zero pressure.
- **Phase 4 deferred**: the design sketch is recorded in the `flow.rs` header; `dev_brief`/`after_multi_diamond`/`release_pipeline_combined` stay pinned at `parallel region`.
- **A select-side WIP gap surfaced beyond the spec's inventory**: `TemplateFn::SignalRef` (T-SIG) had no shell recipe in `select/shells.rs` (refused `T-SIG shell`), blocking every wait fixture at emit. Implemented as the `signal.new/2` recipe twin (name binary + payload codec call + `call_ext_last`).
- **`needs_child_witness` traversal was NOT extended by the WIP** (the spec's Phase 1 warning was right): statement-level child calls and spawns hit `child spawn has no planned witness` Planning errors. Fixed by adding the `Statement::Call`/`Statement::Spawn` child arms.
