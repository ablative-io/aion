{
  "cluster": "aion-server",
  "sections": [
    {
      "name": "Wire Contract Scaffold (aion-proto)",
      "items": [
        {"id": "C1", "text": "aion-proto is a workspace member depending on aion-core plus external crates only (prost, tonic, serde) — no aion, aion-server, or store-backend dependency.", "done": false},
        {"id": "C2", "text": "build.rs compiles the .proto files via tonic-build and the generated code is included through a thin lib.rs that contains only declarations and re-exports.", "done": false},
        {"id": "C3", "text": "aion-proto sets unsafe_code = \"deny\" and inherits the workspace clippy lints; cargo check -p aion-proto passes.", "done": false}
      ]
    },
    {
      "name": "Common Wire Types and Error Taxonomy",
      "items": [
        {"id": "C4", "text": "common.proto and convert.rs define the proto<->aion-core mapping for WorkflowId, RunId, ActivityId, TimerId, Payload, and WorkflowStatus with lossless round-trip.", "done": false},
        {"id": "C5", "text": "The wire Event/WorkflowFilter/WorkflowSummary reuse aion-core's types via a thin envelope (namespace, request id, serialised core payload); no wire clone of those types exists.", "done": false},
        {"id": "C6", "text": "WireError is a closed taxonomy with stable codes covering not-found, namespace-denied, sequence-conflict, unknown-query, query-timeout, not-running, lagged, and backend categories.", "done": false},
        {"id": "C7", "text": "WireError maps from aion's EngineError, aion-store's StoreError, and query/signal/namespace failures, with a documented mapping and no Debug-formatted internals on the wire.", "done": false}
      ]
    },
    {
      "name": "Workflow Management Wire API",
      "items": [
        {"id": "C8", "text": "workflow.proto defines a service with StartWorkflow, Signal, Query, Cancel, ListWorkflows, and DescribeWorkflow RPCs and their request/response messages.", "done": false},
        {"id": "C9", "text": "Each request carries a namespace; inputs and results are carried as aion-core Payload, and ListWorkflows carries a WorkflowFilter and returns WorkflowSummary values.", "done": false},
        {"id": "C10", "text": "Workflow-management wire messages have serde types deriving Serialize + Deserialize and round-trip losslessly to/from their proto form.", "done": false}
      ]
    },
    {
      "name": "Event Streaming Wire Shape",
      "items": [
        {"id": "C11", "text": "events.proto defines a SubscriptionRequest with per-workflow, filtered (by type/status/namespace), and firehose variants, plus the streamed Event envelope.", "done": false},
        {"id": "C12", "text": "The streamed Event envelope carries the aion-core Event unmodified plus the owning namespace, and round-trips losslessly.", "done": false}
      ]
    },
    {
      "name": "Worker Protocol Wire Shape",
      "items": [
        {"id": "C13", "text": "worker.proto defines a bidirectional-streaming service with RegisterWorker, ActivityTask, ActivityResult, and Heartbeat messages.", "done": false},
        {"id": "C14", "text": "ActivityTask carries the activity type, input Payload, and the WorkflowId/ActivityId correlation; ActivityResult carries a result Payload or an ActivityError; Heartbeat carries liveness and optional progress.", "done": false},
        {"id": "C15", "text": "RegisterWorker carries the worker's namespace and the activity types it implements.", "done": false}
      ]
    },
    {
      "name": "Server Scaffold, Config, and State",
      "items": [
        {"id": "C16", "text": "aion-server is a workspace member (binary) depending on aion, aion-proto, and a store backend, with a thin main.rs where anyhow is the only error type at the top level.", "done": false},
        {"id": "C17", "text": "Config types (store DSN, listen ports, TLS, auth, dashboard asset path, namespace mode, worker heartbeat window, WebSocket buffer bound) derive Deserialize only — never Serialize — and no operational value is a hardcoded default.", "done": false},
        {"id": "C18", "text": "ServerError is a thiserror taxonomy used throughout the server library modules; no unwrap/expect in library code and lock/stream poison is handled explicitly.", "done": false},
        {"id": "C19", "text": "Shared server state holds the engine handle and the NamespaceResolver and is constructed once at startup.", "done": false}
      ]
    },
    {
      "name": "Namespace Isolation",
      "items": [
        {"id": "C20", "text": "A NamespaceResolver authorises a caller for a namespace and scopes an operation to it, abstracting whether namespaces map to separate engine/store instances or one shared engine with a namespace key.", "done": false},
        {"id": "C21", "text": "Namespace authorisation and scoping happen at the adapter boundary before any Engine call; no cross-namespace operation (start/signal/query/cancel/list/subscribe/worker dispatch) reaches the engine.", "done": false}
      ]
    },
    {
      "name": "Workflow API Handlers (gRPC + HTTP)",
      "items": [
        {"id": "C22", "text": "A single handler layer translates each workflow-management request into exactly one (or a small fixed composition of) Engine call(s); it performs no retry, scheduling, sequencing, or other durable decision.", "done": false},
        {"id": "C23", "text": "A tonic gRPC service and an axum HTTP/JSON facade both delegate to the shared handler layer; the transport is a thin skin.", "done": false},
        {"id": "C24", "text": "Handler outcomes (including failures) are serialised as the wire response or a mapped WireError; engine internals never cross the wire.", "done": false}
      ]
    },
    {
      "name": "WebSocket Event Streaming",
      "items": [
        {"id": "C25", "text": "A SubscriptionRequest is validated for namespace scope and mapped onto the engine's EventFilter, then engine.subscribe is called to obtain the stream tail.", "done": false},
        {"id": "C26", "text": "A per-connection task forwards events from the engine stream to the socket until the client closes or the subscribed workflow terminates, then drops the subscription with no leak.", "done": false},
        {"id": "C27", "text": "Each connection has a bounded outbound buffer; a consumer that cannot keep up is closed with a typed lag WireError, and the engine broadcast is never back-pressured by a subscriber.", "done": false}
      ]
    },
    {
      "name": "Remote-Worker Protocol (server side)",
      "items": [
        {"id": "C28", "text": "The server accepts a worker's bidirectional gRPC stream, records its RegisterWorker advertisement in a connected-worker registry keyed by namespace and activity type.", "done": false},
        {"id": "C29", "text": "A scheduled Tier-3 activity is matched to a registered worker for that activity type in that namespace and pushed as an ActivityTask; no Temporal-style polling.", "done": false},
        {"id": "C30", "text": "A returned ActivityResult is fed into the engine's activity contract so the engine records ActivityCompleted/ActivityFailed and resumes; the server does not record activity events or decide retries itself.", "done": false},
        {"id": "C31", "text": "A worker that misses its heartbeat window or disconnects before reporting is marked lost and its in-flight task is surfaced to the engine as failed, leaving the retry decision to the engine.", "done": false}
      ]
    },
    {
      "name": "Dashboard Hosting",
      "items": [
        {"id": "C32", "text": "The server serves cluster AU's built static asset bundle from a configured (or embedded) path.", "done": false},
        {"id": "C33", "text": "The dashboard is backed only by the same public gRPC/HTTP handlers and WebSocket feed every other client uses; there is no dashboard-specific API.", "done": false}
      ]
    }
  ]
}
