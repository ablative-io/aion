{
  "id": "AP-007",
  "cluster": "aion-package",
  "title": "Version record production and round-trip conformance suite",
  "depends_on": ["AP-005", "AP-006"],
  "blocked_by": [],
  "checklist": ["C25", "C26", "C27"],
  "stories": ["S1", "S2", "S7"],
  "purpose": "Define and produce the cross-system WorkflowVersion record from a loaded Package so the engine references a workflow version through one shared type, and provide the conformance suite that proves the write and read paths fit together: a built package loads back intact, builds are reproducible, and integrity rejection actually fires. WorkflowVersion is defined in aion-package (not aion-core) because it embeds the ContentHash, an aion-package type — aion-core is a leaf and cannot reference it (D10).",
  "task": "Define the WorkflowVersion record type in aion-package and a function producing it from a loaded Package (logical entry module, content hash, declared activities, input/output schemas). Then build the round-trip conformance suite: write a package, load it, assert manifest/beams/namespaced-names survive; assert two builds of identical inputs are byte-identical; assert a post-pack alteration of the beams is rejected on load. Do not write the record to any store.",
  "requirements": [
    {
      "id": "R1",
      "title": "WorkflowVersion record and production from a loaded Package",
      "spec": "THE SYSTEM SHALL define the WorkflowVersion record in aion-package and produce it from a loaded Package, carrying the logical entry module, the content hash, the declared activities, and the input/output schemas.",
      "acceptance": [
        "version.rs defines WorkflowVersion (logical entry module, ContentHash, declared activities, input/output schemas) deriving Serialize, Deserialize, Clone, Debug, PartialEq, and a function (e.g. Package::version_record()) returning it.",
        "The record carries the logical entry module, the content hash, the declared activities, and the input/output schemas drawn from the loaded Package.",
        "WorkflowVersion is the single canonical version type: it is defined here (it embeds the aion-package ContentHash, which aion-core cannot reference), and consumers — the engine (aion) — depend on aion-package to use it rather than re-deriving a parallel type.",
        "A unit test builds and loads a package, produces its version record, and asserts each field matches the manifest and content hash."
      ],
      "files": {
        "create": ["crates/aion-package/src/version.rs"],
        "modify": ["crates/aion-package/src/lib.rs"]
      },
      "checklist": ["C25"],
      "stories": ["S1"]
    },
    {
      "id": "R2",
      "title": "Write/read round-trip suite",
      "spec": "THE SYSTEM SHALL provide a test suite that builds a package, loads it back, and asserts the manifest, beam set, and namespaced module names survive intact.",
      "acceptance": [
        "tests/roundtrip.rs builds a package (non-trivial: multiple beams, declared activities, schemas, with and without source) and loads it via Package::load_from_bytes.",
        "The loaded manifest equals the built manifest (with the version stamped), the loaded BeamSet equals the input BeamSet, and the optional source matches.",
        "The loaded deployed module names equal namespace::deployed_name(logical, content_hash) for every module.",
        "The test is a real runtime test, not gated out or ignored."
      ],
      "files": {
        "create": ["crates/aion-package/tests/roundtrip.rs"],
        "modify": []
      },
      "checklist": ["C26"],
      "stories": ["S1"]
    },
    {
      "id": "R3",
      "title": "Reproducibility and integrity-rejection assertions",
      "spec": "THE SYSTEM SHALL assert that two builds of identical inputs are byte-identical and that a package whose beams are altered after packing fails to load with IntegrityMismatch.",
      "acceptance": [
        "A test builds identical inputs twice and asserts the output byte buffers are equal (reproducibility, CO8).",
        "A test takes a valid package's bytes, mutates a byte inside a beam entry (re-zipping or patching the entry), and asserts Package::load returns PackageError::IntegrityMismatch.",
        "A test asserts that including vs omitting source yields the same content hash / version (CO7).",
        "All tests run under cargo test without #[ignore]."
      ],
      "files": {
        "create": [],
        "modify": ["crates/aion-package/tests/roundtrip.rs"]
      },
      "checklist": ["C27"],
      "stories": ["S2", "S7"]
    }
  ],
  "boundaries": [
    "SHALL NOT write the version record to any store — that is AC (in-memory) / AS (libSQL) / AX (Postgres).",
    "SHALL NOT test VM registration or execution — that is the engine cluster (AE).",
    "SHALL NOT define a version type in aion-core; WorkflowVersion is canonical here because it embeds the aion-package ContentHash."
  ],
  "verification": [
    "cargo clippy --workspace --all-targets -- -D warnings passes.",
    "cargo test -p aion-package passes, including the round-trip suite.",
    "cargo fmt --check passes.",
    "No #[allow], #[expect], or #[ignore] directives in new code."
  ]
}
