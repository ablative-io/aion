# AWL-2 build plan — front-end rebuild

Status: LIVE — executing as an ultracode multi-agent workflow (Tom's go,
2026-07-10). This document is the plan of record and the running decision
log; the workflow appends to the log as phases complete.

Spec of record: [AWL-2-SPEC.md](AWL-2-SPEC.md) (aion main `124b1825`).
Golden examples: [examples/rev2/](examples/rev2/) — extracted verbatim from
the spec; the corpus grows around them.

## Objective

Rebuild the `aion-awl` crate against the rev-2 surface: lexer, parser,
canonical printer, typechecker, schema derivation, and the Gleam-stopgap
emitter port, plus the `aion awl` CLI rewire and migration of the committed
example/fixture corpus. End state: aion main compiles and gates green with
the rev-2 front end and NO old-grammar surface anywhere; `aion awl
check/fmt/emit` work on rev-2 files; the emitted module for the migrated
awl_hello example compiles under `gleam build`.

## Scope

**In:** `crates/aion-awl` (all modules), `crates/aion-cli/src/awl.rs`,
`crates/aion-awl/tests/**` (fixtures + goldens rebuilt), `examples/awl-hello`
migration, this document's decision log.

**Out (explicitly):** AWL-BC bytecode emission (#240 — the canonical model
is its input; it consumes this work, it is not part of it), LSP/tree-sitter,
`aion new` scaffolding, worker scaffolding, visual editor, on-the-fly tier,
live deploy/run proof (my hands with Tom after merge — requires the queued
server/worker binary swap, which this build does not touch).

## Method

- **No compatibility parse.** The old grammar dies in this change; old
  fixtures are deleted or re-expressed, never kept alongside.
- **Fixtures first.** Phase 1 authors the rev-2 golden corpus (valid +
  deliberately-invalid with expected diagnostics) before any implementation.
  Every later phase's objective gate is the corpus.
- **Serial implementation, parallel judgment.** Implementation agents run
  one at a time in a single shared worktree (branch `awl2-front-end`);
  adversarial review panels (3 lenses: spec-fidelity, correctness,
  conventions) run in parallel against each phase's output, followed by one
  bounded fix round and a verification pass. Fixture authoring fans out in
  parallel (distinct files, single consolidating commit).
- **Fable on every judgment seat** (implementation, panels, fixes) per
  Tom's direction; the shared model default is the session model.
- **Gates per phase** (agent-run, then re-run by the operator's hands at
  merge): `cargo fmt` + porcelain check, `cargo clippy -p aion-awl
  --all-targets -- -D warnings`, `cargo test -p aion-awl`. Integration
  phase adds `-p aion-cli` and the full workspace battery.
- **Merge bar:** nothing merges to main until every gate is re-run bare by
  the coordinating session's own hands on the integrated branch; the
  workflow itself never merges or pushes to main.
- Known flake provenance protocol applies (#243/#244/#248): isolated re-run
  once; green → proceed, recorded.

## Phases

1. **Fixtures** — fan out per construct family: (a) header/inputs/outcomes +
   shorthand types + enums; (b) schema doors (inline `schema {…}`, file
   import) + `?` optionality; (c) worker/action/child/spawn declarations;
   (d) step bodies: calls, `->` bindings, pipes, combinators, `wait`/`sleep`;
   (e) `after` DAG + fork/join (collection, named-branch, `sequential`);
   (f) loop/`counting` + conditional outcomes/routing + `on failure` +
   substep grammar (parse-level). Each family delivers valid fixtures AND
   invalid fixtures with expected-diagnostic annotations. A consolidator
   audits coverage against the spec's keyword inventory and commits.
2. **Lexer** — token tables for the rev-2 inventory (`->`, `|>`, `?`,
   keywords, duration/list/record literals, `//!` / `///` doc lines as data,
   `.field` accessors), spans source-correct (the AWL-0 defect regression
   suite's discipline carries over).
3. **Parser + canonical printer** — one phase, one property:
   `parse ∘ print = id`, `print ∘ parse ∘ print = print`, comments and doc
   lines lossless. Grammar per spec; ast.rs rebuilt around the unified
   anatomy (inputs/outcomes, DAG edges, fork groups, loop nodes with
   language-owned counters).
4. **Typechecker + schema derivation** — contracts on calls (required named
   args), binding flow along the graph, `after`/route target existence, no
   unreachable steps, route-cycle boundedness, outcome exhaustiveness
   (`otherwise` / enum totality), guard-dependent optionality
   (`is present`), `?` ↔ `required` in both schema directions, explicit
   null rejected everywhere, imported-schema projection rules carried over.
5. **Emitter port + CLI** — the Gleam-stopgap emitter re-targeted at the
   rev-2 canonical model; `aion awl check/fmt/emit` rewired; emitted
   fixtures compile under real `gleam build` (existing compile-proof test
   pattern). DAG semantics map to the Gleam SDK; the mapping decisions are
   recorded in the decision log by the implementing agent.
6. **Integration** — migrate `examples/awl-hello` (rev-2 .awl + regenerated
   module), delete every old-grammar artifact, full workspace battery,
   completeness critic (what's missing vs the spec — anything found feeds a
   final bounded fix round), decision-log consolidation into this document.

## Design decisions locked before the build (from the 2026-07-10 session)

| # | decision | rationale |
|---|---|---|
| D1 | No compat parse; rebuild clean | one grammar, one truth; old surface has one committed example |
| D2 | Fixtures authored before implementation | objective per-phase gate no agent can talk past |
| D3 | Loop counters are language-owned (`counting`) | survey fix 1: worker-carried counters rot silently |
| D4 | `?` optionality; explicit null invalid everywhere | survey fix 2; closes null-vs-Option permanently |
| D5 | Outcome types carry promised evidence | survey fix 3; outcome types ARE the handoff contract |
| D6 | Combinators (filter/map/sort/count) in-language, fixed vocabulary | worker round-trip for a filter is absurd; pure ⇒ replay-safe |
| D7 | `race` dropped from rev-2 | `wait … timeout` covers signal-or-deadline; no fixture demands first-wins |
| D8 | Emitter stays Gleam-stopgap in this build | AWL-BC (#240) consumes the canonical model separately |
| D9 | Doc comments are the one prose mechanism (`about` dead) | one load-bearing mechanism, models know it |

## Decision log (appended during the build)

Format: `| date | phase | decision | rationale |` — entries added by the
workflow's implementation agents and consolidated at integration.

| date | phase | decision | rationale |
|---|---|---|---|
| 2026-07-10 | lexer (fix round) | Inline `schema {` switches the lexer into raw capture: the brace-balanced body (string-aware counting, braces included) becomes ONE `SchemaBody` token, verbatim, exempt from all AWL lexical rules; JSON validation belongs to the parser | the spec's paste-verbatim door must accept legal JSON Schema the AWL token grammar cannot express (negative numbers, `1e-3`/`1E5` exponents, `\uXXXX`/`\/` escapes, foreign indentation), and raw bytes keep re-emit lossless |
| 2026-07-10 | lexer (fix round) | The raw door is `schema {` on one line; a brace on the NEXT line lexes as an ordinary `LeftBrace` for the parser to refuse | canonical shape is same-line; a positional trigger keeps capture deterministic |
| 2026-07-10 | lexer (fix round) | Doc-line classification is positional: `///` / `//!` trailing code lex as trivia `Comment`, never `DocLine`/`DocHeader` | the spec defines doc LINES; mid-line doc data would attach an author's trailing note to the NEXT declaration and break whole-line round-tripping |
| 2026-07-10 | lexer (fix round) | `Span.column` is character-based (bytes stay byte-true in `start`/`end`); `Newline` spans cover the full physical terminator incl. a stripped `\r`; indentation jumping more than one level is a lexical error | compiler-quality diagnostics: editor-correct columns after multibyte prose, no off-by-one anchors on CRLF files, no phantom `Indent` structure the source doesn't contain |
| 2026-07-10 | lexer (fix round) | Corpus size correction: the rev-2 corpus was 157 `.awl` fixtures at the lexer phase (not "214+"), 158 after `inline_verbatim_constraints.awl`; the corpus gate now asserts ≥158 | phase records must not overstate coverage; the tightened guard catches silent fixture loss |
| 2026-07-10 | parser | Contextual "soft" keywords: `filter`/`map`/`sort`/`count`, `node`/`timeout`/`retry`/`every`/`backoff`, and `empty`/`present`/`absent` are keywords only in their own grammar positions (combinator stages, config lines, `is` predicates) and act as ordinary names wherever a name is expected; structural keywords stay reserved everywhere | the corpus pins `count` as a field name and `retry` as an outcome-arm name; reserving the whole vocabulary would reject the spec's own examples. Narrows the spec's "keywords reserved everywhere" prose — flagged for reconciliation before ratification |
| 2026-07-10 | parser | Outcome-clause layout follows the spec's worked examples over the Canonical-formatting prose: a payload-constructing `route out(…)` ALWAYS breaks after the guard comma onto its own line one level deeper (even under 100 columns); a bare route stays on the guard's line when it fits. Group alignment (type-brace/`=` columns, header-outcome `type`/`route` columns within a run) IS canonical and its padding is width-exempt | the prose ("payload construction breaks after `route` when over 100 columns") and the worked examples (90/99-column payload clauses broken before `route`) disagree; byte-identity with the flagship pins the examples' reading. Recorded at the parser/printer phase in tests/fixtures/rev2/MANIFEST.md; spec prose flagged for reconciliation before ratification |
| 2026-07-10 | parser (fix round) | A stage-less bind chain (`head -> name`) never wraps: the printer keeps it on one line at any width | the break rule is "break before each `\|>`" and a stage-less bind has no `\|>`; a wrapped `-> name` continuation line is output the parser rejects (parse∘print=id violation) |
| 2026-07-10 | parser (fix round) | Loops declare `until` and `max` at most once; a duplicate line is a parse error anchored at the second keyword. A body statement after an outcome clause is likewise a parse error ("outcome clauses close the step") | silent overwrite/reorder made `aion awl fmt` drop or restructure user source; mirrors the duplicate config-key guard |
| 2026-07-11 | checker (fix round) | Named-branch fork branches walk in isolated clones of the pre-fork scope; branch bindings merge into the step scope only at `join`; `join -> name` on the named form is a check error | bare-fork branches run in parallel — a sibling's binding is never guaranteed mid-fork; the spec's named form joins bindless |
| 2026-07-11 | checker (fix round) | The route-cycle SCC includes `after` edges alongside route and fall-through edges | a dependency's completion re-arms its dependents, so a backward route plus a forward `after` edge is as unbounded as two routes; the narrower SCC recorded at the checker phase contradicted "unbounded cycles are unwritable" |
| 2026-07-11 | checker (fix round) | Every non-terminal falling-through step must have its completion consumed: the next step's fall-through edge or an `after` dependent; the file-final step still requires an explicit route | the successor duty ("every non-terminal step has a successor") was enforced only for the last step in the file — a stranded middle step checked clean |
| 2026-07-11 | checker (fix round) | A piped route (`… \|> route <target>`) must target a workflow outcome; steps, sibling substeps, and parent outcome arms are refused | the spec defines the pipe-route terminator as "the piped value is the payload"; steps carry no payloads, so the threaded value vanished silently |
| 2026-07-11 | checker (fix round) | Steps and workflow outcomes share one route-target namespace: a step named like a workflow outcome is a declaration-time error (anchored at the step, second-occurrence convention) | `route <name>` silently resolved to the step, so a run intending to finish transferred control instead |
| 2026-07-11 | checker (fix round) | A binding inside a collection-fork branch never counts as a loop's threaded-value rebind (the loop frame records its fork depth) | branch bindings do not escape the branch, so the loop would carry its seed forever while the checker believed it rebound |
| 2026-07-11 | checker (fix round) | Inline schema-door diagnostics anchor by walking the raw JSON body to the failing path (properties/items/`$defs`-ref navigation), never by first-occurrence token search | nearly every real schema repeats `"type"`; nested refusals anchored at the innocent root keyword — a systematic span lie |
| 2026-07-11 | checker (fix round) | Structural compatibility replaces its depth cap (>24 ⇒ accept) with a coinductive in-progress set of named pairs | the cap converted deep mismatches into silent acceptance; coinduction keeps recursive types terminating without accepting anything unproven |
| 2026-07-11 | checker (fix round) | Dead control flow is refused: statements after an unconditional body `route`, and outcome clauses on a body ending in an unconditional route; call-site config lines on CHILD calls are refused (`node`/`timeout` pin worker actions only) | advisory hardening from the same panel — dead code made the fall-through graph unsound, and the engine routes children, not a queue |
| 2026-07-11 | checker (fix round) | RECORDED, not enforced (flagged for spec reconciliation before ratification): a loop-carrying step with zero outcome clauses keeps the permissive reading (exhaustion falls through as the implicit outcome); `[T?]` in list-element position stays checker-accepted while schema derivation drops the element `?` — RESOLVED 2026-07-11, both now enforced (see the ratified-rulings rows R1/R2 below) | both need a spec ruling; enforcing either silently would invent surface the spec has not settled |
| 2026-07-11 | emitter | DAG mapping: dependency-connected steps group into *regions* (one entry each — the workflow start or a route target), each region lowers to one Gleam function running its steps in topological layers, `route <step>` is a tail call to the target region's function, substeps lower to sibling functions, and bindings thread between functions as parameters computed by a liveness fixed-point over the (cyclic — backward routes) call graph | the plan's proposed topological-layer mapping, adopted; generated Gleam is structured code, so regions/tail-calls are the shape conditional routing can take |
| 2026-07-11 | emitter | Refused shapes (spanned `EmitError`, all `AWL-BC #240` candidates): a route-targeted step WITH `after` dependencies; a routing step with `after`-dependents; a step routing away while parallel/upstream work in its group is outstanding; dependency groups with two entries; routes to mid-chain steps; substeps outside one trailing block at depth one; substep parents without outcome clauses; `on failure` combined with a body-terminal route. The route-target+`after` join is a shape the spec explicitly defines ("its `after` dependencies, if any, must also be complete") — flagged for reconciliation before ratification | the sequential region lowering cannot express these joins; refusing loudly beats lowering them wrongly, and the spec-defined shape among them needs a spec-level ruling on stopgap scope |
| 2026-07-11 | emitter (fix round) | Heterogeneous parallelism is REAL, not sequentialized: named-branch forks and single-call dependency layers with differing actions dispatch on ONE `workflow.all` of raw wire-unified activities — each branch rides an `Activity(String, String)` wrapper twin whose input is pre-encoded with the action's own input codec (byte-identical wire), and the join decodes each payload with its action's return codec (`awl_decoded`). The 2026-07-11 panel confirmed the prior sequential fallback silently violated "bare `fork` is parallel" on the spec's own worked examples | the SDK's `workflow.all` is homogeneous in both type parameters and decodes every payload with the FIRST member's output codec; an identity `Codec(String)` shared by all members is the one unification that survives that decode path |
| 2026-07-11 | emitter (fix round) | RESIDUAL mapping limit: a dependency layer whose members are more than single bare action calls (e.g. release_pipeline's `build_all` fork beside `notes`) still runs member-by-member in written order, and the generated module carries an `// awl stopgap:` comment naming the degradation at the site | the SDK parallelizes activities, not statement sequences — there is no heterogeneous task primitive to compose full step bodies; the comment keeps the limit non-silent until AWL-BC lowers the real graph |
| 2026-07-11 | emitter (fix round) | A `route` inside a `loop` body is refused at emit with a spanned error (statement, pipe-terminal, and nested forms alike). The checker keeps the spec-level walk (the shape stays checkable) — flagged for reconciliation before ratification: the spec says a route "ends the run", and the generated tail-recursive loop function has no early-exit channel, so the stopgap must refuse rather than emit a discarded no-op route (or worse, a mid-loop region call) — RESOLVED 2026-07-11: the language forbids the shape; the refusal moved to the checker and the emitter's stays as a defensive backstop (ruling row R3 below) | the panel's reproducer checked clean, emitted, compiled under `gleam build`, and looped straight past its route; refusal is the only honest stopgap answer, and whether the LANGUAGE forbids the shape is a spec ruling AWL-BC's real early-exit lowering should inform |
| 2026-07-11 | emitter (fix round) | Loop `max` is loop-invariant: the checker types it in the PRE-loop scope (loop-locals get a targeted diagnostic) and the emitter independently refuses a `max` referencing names unbound before the loop | `max` renders once at the loop call site, where loop-locals do not exist; the checker previously typed it in body scope, so `max round.budget` checked clean and emitted Gleam that could not compile — and the spec's "expression over inputs/bindings" reads pre-loop anyway |
| 2026-07-11 | emitter (fix round) | One type per binding name, workflow-wide: the binding pass refuses (spanned, at the second bind) a name bound with two different types in disjoint branches, instead of first-wins | the binding→type map is name-keyed and feeds region parameter annotations; first-wins annotated the other branch's parameters with the wrong type and emitted non-compiling Gleam from a checked-clean document (the checker degrades the conflict to Unknown) |
| 2026-07-11 | emitter (fix round) | Float ordering comparisons render Gleam's Float operator family (`<.` `<=.` `>.` `>=.`), keyed off either operand typing Float; Int pairs keep the bare operators | Gleam's bare ordering operators are Int-only; the checker admits Float/Float ordering, so the bare rendering failed `gleam build` on a checked-clean document (no corpus fixture had a Float comparison — `float_threshold_guard.awl` now pins it with a compile proof) |
| 2026-07-11 | emitter | Collection forks lower one unbound action call per item (`workflow.map`, or `list.try_fold` for `sequential`); literal indexing is refused inside parallel fork branches and outcome guards (contexts that cannot host the fallible-index prelude) | SDK `map` is the exactly-once-per-item primitive and takes one activity constructor; richer branch bodies and prelude-hosting are AWL-BC surface |
| 2026-07-11 | emitter | Retry mapping carried from the proven AWL-0 emitter, flagged for spec reconciliation before ratification: `retry N` → `RetryPolicy(max_attempts: N, …)` (N TOTAL attempts, so `retry 2` = one retry) and `backoff D..D` → `Exponential(initial: D, multiplier: 2.0, max: D)` — the 2.0 multiplier is an invented constant | the spec never says whether N counts retries or attempts and names no backoff curve; one spec sentence settles each, and silently changing the proven AWL-0 wire behavior mid-stopgap would be worse than recording it |
| 2026-07-10 | lexer | `in` (fork collection form `fork lens in config.lenses`) is NOT a keyword and lexes as a plain identifier | the spec's reserved-keyword inventory omits `in` (the dead-word list carries `in order`), so the fork grammar owns the word at parse level and the reserved surface stays exactly the spec's table |
| 2026-07-10 | lexer | `.field` is a dedicated `FieldAccessor(String)` token (keyword lookup NOT applied inside accessor names), the bare `Dot` token is removed, and a lone `.` is a lexical error "expected a field name after `.`" | rev-2 has no other use for bare dot, one token makes `value.field` chains and bare combinator accessors (`filter(.blocking)`) uniform, and undeclarable fields like `.count` die in the checker rather than with a confusing lexer error |
| 2026-07-10 | lexer | `DocHeader` (`//!`) and `DocLine` (`///`) tokens carry the text after the marker VERBATIM (leading space preserved), `//` trivia keeps one-space-normalized text, and `////…` is trivia, not a doc line | doc lines are load-bearing data feeding schema descriptions and narration, so verbatim payloads let the printer round-trip them byte-for-byte, and the `////` carve-out preserves the AWL-0 parser's established convention |
| 2026-07-10 | lexer | Doc lines emit a trailing `Newline` token (they are data lines) but never adjust the Indent/Dedent stack | field-level docs sit at indentation the surrounding code has not yet established, so indentation-neutrality avoids phantom indent levels while the token's span column lets the parser attach and police placement |
| 2026-07-10 | lexer | `=` is an `Equal` token and bare `\|` is `Bar` (enum variant separator) distinct from `\|>` `Pipe` | `=` survives in exactly `loop x = seed` and `type X = …` (the gone statement-binder must lex to be diagnosed), and conflating `\|` with the pipe operator would force parser-side re-splitting |
| 2026-07-10 | lexer | Old-grammar test suites (awl2_defects, checker, emitter, parser_printer, schema, field_trivia_and_duplicates, parser_edge_regressions) kept, not deleted — all still compile, 113/114 pass, the single failure a known-red old-grammar-vs-rev-2 keyword collision | deletion was sanctioned only for suites that no longer compile against the new token API, and these consume the AST-level API rebuilt wholesale in their own phases |
| 2026-07-10 | lexer | tests/lexer.rs split into lexer.rs (corpus/sequences/keywords) and lexer_spans.rs (span discipline + diagnostics) | the rebuilt single file hit 623 lines, over the 500-line standard, and the span-discipline material is a coherent standalone suite |
| 2026-07-10 | lexer | The corpus lex gate asserts INVALID fixtures lex cleanly too, not just valid ones | every sidecar stage is PARSE or CHECK — none lexer-stage — so a fixture dying in the lexer would silently break its intended diagnostic |
| 2026-07-10 | lexer (fix round) | The within-line token scanner split into src/lexer/cursor.rs, with scanner.rs keeping line structure (indentation, comments, newlines, raw capture) | the raw-capture rework pushed scanner.rs past the 500-line standard, and the split lands at 372 + 272 lines with a clean seam while mod.rs stays re-exports only |
| 2026-07-10 | lexer (fix round) | tests/operators_and_literals.rs::inline_schema_json_tokens_lex retained with a corrected comment (it pins JSON-looking fragments OUTSIDE a door lexing as ordinary tokens), the door design itself pinned by the new tests/schema_door.rs suite | no test was deleted to make a finding disappear — only the old comment's claim that schema content goes through the AWL lexer was false |
| 2026-07-10 | lexer (fix round) | Fix-round gate baseline captured BEFORE touching code: fmt porcelain-clean, clippy `-D warnings` exit 0, and the single test failure the known-red old-grammar emitter test on_failure_finish_terminates_workflow | the baseline proves the only red suite predates the fix round |
| 2026-07-10 | parser | Blank lines are preserved trivia: each printable item carries leading trivia (Blank/Comment), runs of blank lines canonicalize to one, and blank detection derives from line-number gaps between consecutive real tokens (the lexer emits nothing for blank lines) | the corpus's blank-line placement is authorial, so byte-identity is impossible under any fixed blank-insertion rule and lossless-trivia parse/print is the phase's core property |
| 2026-07-10 | parser | Liberal parse, canonical print for ordering: header decls and top-level decls parse in any interleaving and print in the grammar's canonical order, and step internals reorder to body → on failure → outcomes | no fixture pins order-violation rejection, so fmt canonicalizes non-canonical documents without inventing unfixtured errors while parse-identity holds on canonical ones |
| 2026-07-10 | parser | The parser validates inline `schema { … }` bodies as JSON (serde_json) with the error position mapped through the SchemaBody token to a document-true span quoting the offending lexeme, defers all deeper schema semantics (unsupported keywords, null types, imports) to the checker, and never reads the filesystem | inline_bad_json.expected is PARSE-staged demanding the lexeme at the in-body line, all import/inline-semantics sidecars are CHECK-staged, and a pure parser keeps parse/print usable without I/O |
| 2026-07-10 | parser | Multi-line structure handling: parentheses transparently skip Newline/Indent/Dedent (a suppressed-indent counter pays the Dedent debt later), square brackets deliberately do NOT participate, wrapped pipe chains continue via an explicit Newline[+Indent]+Pipe lookahead, and block open/close sees through doc-line runs | canonical output itself wraps route payloads and long pipe chains so print∘parse∘print=print requires re-parsing them, excluding brackets keeps unclosed_list_type's diagnostic on its own line, and doc-run transparency fixes real corpus shapes |
| 2026-07-10 | parser | Checker/emitter/schema became single-file typed stubs keeping exact public API shapes (check returns empty; emit/schema return explicit "being rebuilt" spanned errors), the old AWL-0 implementations deleted with git history (9bf366b7) as the porting base | D1 plus the 500-line/no-dead-code standards — check→empty keeps fmt-style pipelines usable mid-build while emit refusing loudly beats emitting wrong Gleam, with their suites the phase's declared known-red |
| 2026-07-10 | parser | Doc lines are grammatically attached data (legal before types, fields, actions, workers, children, steps, substeps); a `///` run in any other position — or `//!` outside the document prologue — is a parse error naming where docs attach | silently dropping or re-homing an author's doc line is corruption, so erroring is the only lossless option for unattachable docs, and the spec limits attachment to declarations |
| 2026-07-10 | parser | The ported span-regression discipline anchors on the flagship: awl2_defects.rs asserts AST-node spans against dev_brief.awl byte offsets, deep-document error spans, dead-keyword fix-its for the eleven gone words the corpus does not witness, and char-column correctness after multibyte prose | the old suite's fixture-anchored span checks died with the old grammar and the flagship is the rev-2 committed golden |
| 2026-07-10 | parser (fix round) | The bind-with-stages trailing comment rides the LAST stage line of a wrapped chain (the line carrying `-> name`), the Route arm unchanged | that line is the statement's final printed line, so the comment stays visually attached to the binding and the wrapped form round-trips idempotently |
| 2026-07-10 | parser (fix round) | serde_json's byte-based error column is clamped to a char boundary before slicing, then converted to a character count, with Span.start/end staying byte-true | the crate's span contract is character columns + byte-true offsets, and the boundary clamp guards against a mid-codepoint column without unwrap/panic paths |
| 2026-07-10 | parser (fix round) | Advisories applied (3 of 4): post-outcome statements rejected ("outcome clauses close the step"), call-headed pipe chains get a bind-first fix-it, and the parser-phase rulings appended to this decision log | all were cheap and clearly right, matching the crate's tolerate-noncanonical-but-never-reorder posture and fix-it bar |
| 2026-07-10 | parser (fix round) | New diagnostics regression tests live in a new tests/parser_diagnostics.rs suite rather than growing awl2_defects.rs | awl2_defects.rs hit 528 lines with the additions, over the 500-line standard — it now sits at 425 lines and the new suite at 128 |
| 2026-07-10 | checker | check keeps its (document)→Vec<CheckError> signature (imports report unresolvable-without-directory) while check_in(document, root) is the real entry resolving schema imports relative to the .awl file's directory, same pattern for schema_for_type_in/schema_for_workflow_in | aion-cli must keep compiling clean this phase (CLI rewire is phase 5) and import resolution genuinely needs the document directory |
| 2026-07-10 | checker | Type compatibility is structural for record shapes (field names + types + optionality) with a nominal fast-path on equal names, and diagnostics display nominal names | dev_brief passes items of the imported RunConfig's $defs Lens to a parameter declared as the shorthand `type Lens` — schema-projected records must satisfy same-shape declared records or the flagship cannot check |
| 2026-07-10 | checker | Ty::Named stays a lazy symbolic reference resolved through the type table at use sites (no eager expansion), with bounded resolve/compare recursion (structural depth cap later replaced by coinduction, above) | laziness kills forward-reference ordering problems and recursive shorthand types (`type Node { next: Node? }`) without Rc cycles |
| 2026-07-10 | checker | The flow walk runs three passes over all steps — two silent passes seed a global binding-name→type map and only the third emits diagnostics | bindings flow along the GRAPH, not file order: a route-targeted earlier step may read a later step's binding (pinned by route_targeted_earlier_step_reads_a_later_steps_binding) |
| 2026-07-10 | checker | Guaranteed-bindings availability: avail_in(S) = inputs ∪ union of after-deps' outs ∪ intersection over control predecessors' outs (route sources, fall-through predecessor, a virtual inputs-only entry path), computed by descending Kleene iteration from the universe set, with unknown after-targets getting the universe for cascade suppression | implements the spec's "readable only where guaranteed on every path" exactly — binding_not_on_all_paths rejects while dev_brief's diamond accepts |
| 2026-07-10 | checker | Fall-through edge S→T exists iff T has no `after`, T is not route-targeted, and S can fall through (no outcome clauses AND body not ending in an unconditional route/pipe-route) | matches the spec's three-way rule and makes unreachable_step's orphan detection work without inventing false edges inside after-diamonds |
| 2026-07-10 | checker | Route-cycle boundedness runs Kosaraju SCC over the flow graph, a cyclic SCC legal iff some member step contains a max-bounded loop (recursively, incl. fork/substep bodies), the diagnostic anchored on the first backward-or-self route edge with both pinned substrings ("cycle", "bound") | the spec's rule is literally "a max-bounded loop on some step in the cycle" and one mechanism satisfies all five cycle sidecars plus both valid bounded-cycle fixtures |
| 2026-07-10 | checker | An `after` dependency cycle reports once (anchored on the first-written participant's header, naming the cycle path) and skips reachability, route-cycle, final-step, and the whole flow walk | the manifest requires the cycle diagnosis to beat the unreachable-step diagnosis, every invalid fixture carries exactly one defect, and a broken dependency graph makes downstream analyses meaningless noise |
| 2026-07-10 | checker | Optional (`?`) parameters may be omitted at call sites exactly like optional fields in record construction, all other parameters required named arguments | spec-silent for calls — one omission rule for both argument surfaces is the consistent reading of "`?` maps to not-in-required" and no fixture contradicts it |
| 2026-07-10 | checker | Bare `route <outcome>` picks up the in-scope binding NAMED like the outcome (by-name, not by-type), checked against the outcome's payload type, with narrowed bindings participating | the manifest's route_outcome_by_name/bare_route_no_binding pair explicitly anticipated the by-name interpretation |
| 2026-07-10 | checker | Explicit `null` is caught as a reference: it lexes as an identifier and Expr::Ref{name:"null"} gets a dedicated absence-is-omission diagnostic everywhere, not just in construction | D4 says null does not exist in the language — special-casing the reference covers construction payloads, arguments, and guards with one rule and the exact pinned substring |
| 2026-07-10 | checker | Schema projection refuses 13 structural keywords (oneOf/anyOf/allOf/not/if/then/else/patternProperties/prefixItems/unevaluatedProperties/dependentSchemas/dependentRequired/propertyNames), `"type":"null"` (and null-admitting unions), and non-$defs-local $ref — each naming keyword and JSON path — while constraint/metadata keywords are ignored for typing; inline-door errors anchor on the raw-body line containing the quoted keyword, import errors on the path literal | the spec makes unsupported structural keywords check ERRORS naming keyword+path, and the sidecars pin inline anchors to the offending JSON line and import anchors to the declaration line |
| 2026-07-10 | checker | Loop duties: `until` and `max` are checker-mandatory ("unbounded" wording for missing max, loop-line anchor), the threaded var's rebinding is the one sanctioned rebinding (type-stable, tracked via a loop-frame stack), the counter binds Int only AFTER the loop, and loop-body bindings do not escape the loop | the spec grammar shows until+max as the loop's shape and the counter as usable "in the step's outcomes and every later step", with conservative body scoping matching the threading rule |
| 2026-07-10 | checker | Collection-fork typing: the item var and branch bindings scope to the branch and `join ->` binds a list of the branch's LAST statement's value | fork_collection_join pins joined-type = [return of the unbound branch call], and the manifest deliberately leaves join-form mismatches unfixtured |
| 2026-07-10 | checker | `on failure` blocks check against the step's avail_in only (body bindings excluded) and must END in a route statement or pipe-route | the failure point inside the body is unknowable, so only pre-step bindings plus the block's own calls are safe (ship_release and on_failure_no_route pin both halves) |
| 2026-07-10 | checker | Substeps use a linear-scope model: bindings accumulate across substeps into the parent scope in written order, inner routes resolve to sibling substeps ∪ parent outcome-arm names, a top-level-only name is an "outside the parent" escape, and reachability/cycle/final-step analyses do not apply inside substep groups | substeps_two_stage requires parent outcomes to read a substep's binding and inner routes to fire parent arms, while analyses beyond the three committed fixtures would be invented semantics (recorded as bounded scope, not silent) |
| 2026-07-10 | checker | One exhaustiveness rule: steps with when-arms need `otherwise` or checker-provable enum totality (every arm `subject == Variant`, one structurally-equal subject path, all variants covered), the generic message carrying "exhaust", the enum message additionally naming missing variants, both anchored on the step header | the three sidecars pin the same anchor and compatible substrings, and the spec's exhausted-loop clause is exactly the all-whens-false path of the same rule |
| 2026-07-10 | checker | Enum comparisons: bare Variant against a declared enum requires membership (unknown variant names the enum), string literals compare against projected string enums WITHOUT membership checking, ordering comparisons require Int/Int or Float/Float, `+` is String/String only, equality requires matching primitives | the manifest pins bare-TitleCase for declared enums and string literals for imported enums, and string-literal membership is on the deliberately-not-fixtured list where refusing would invent semantics |
| 2026-07-10 | checker | Duplicate action names are refused document-globally (not just per worker) and an action/child name collision is an error | call sites resolve callees by bare name with no worker qualifier, so any cross-worker duplicate makes every call ambiguous |
| 2026-07-10 | checker | Schema derivation: named type references emit {"$ref":"#/$defs/Name"} with transitively-collected $defs (root self-references as "#"), door types re-emit their source JSON verbatim, Dir→string and Nil→null, doc lines join into one description paragraph, schema_for_workflow emits an object over the inputs (`?`-inputs out of required, `//!` narration as description), and no additionalProperties is injected | $defs+refs keep recursion representable and shared types single-sourced, verbatim door re-emission is the spec's constraints-pass-through promise, and injecting additionalProperties would alter imported contracts on re-emission |
| 2026-07-10 | checker | Old-grammar fixtures typed_contract.awl and brief.schema.golden.json stay on disk with their tests deleted (likewise research_report/hello/bounded_cycle for tests/emitter.rs) | aion-cli's phase-5-red awl test module include_str!s them at compile time, so deletion would turn known-red runtime failures into compile failures of the whole test target |
| 2026-07-10 | checker (fix round) | Fix-round regression tests split into checker_regressions.rs (graph/flow findings) and checker_hardening.rs (schema anchors, type system, advisories), shared helpers duplicated rather than a test-support module | a single file exceeded the 500-line ceiling and the split keeps both under it |
| 2026-07-10 | emitter | Loops lower to top-level tail-recursive functions returning #(threaded_value, count) when `counting` is declared, the counter incremented in generated code (awl_count + 1) with exit checking until-then-ceiling, and `max` evaluated once at loop entry | D3/survey fix 1 — workers never see or carry the counter (regression-tested on the activity dispatch), and max is a bound over inputs/bindings that cannot change mid-loop |
| 2026-07-10 | emitter | Workflow terminals: success outcomes return Ok(<Workflow>Outcome union value) encoded {"outcome": name, "payload": …}, failure outcomes return Error(AwlOutcomeFailure(outcome, payload_json)) riding the engine's Failed path, and a workflow with no success outcome gets output type Nil | route success/failure must map to Completed/Failed through the SDK's Result seam, and carrying the failure payload in a dedicated AwlError variant keeps the evidence (D5) without inventing a second output channel |
| 2026-07-10 | emitter | Schema doors project onto nominal Gleam records in the emitter's own type environment: root objects bind the declared name, nested objects/$defs reuse any structurally identical registered record before synthesizing a fresh name, PROJECTED string enums type as Gleam String (declared enums are real custom types with string codecs), non-object roots become aliases | the checker is structural where Gleam is nominal — without unification dev_brief's $defs Lens would be incompatible with the declared Lens, and projected string enums must stay String because the checker lets them compare against string literals |
| 2026-07-10 | emitter | Optional record fields honor D4 in both codec directions: encoding omits absent fields entirely (json.object over list.flatten), decoding uses decode.optional_field with a Some-mapping decoder so explicit null fails to decode, present values auto-wrap in Some(…) into optional slots, and [T?] elements keep the SDK nullable form mirroring the checker's recorded note | D4 is absence-never-null — field-level omit-when-absent is enforceable, while element-level optionality has no JSON spelling without null and awaits the spec ruling |
| 2026-07-10 | emitter | Every generated function's parameters carry explicit Gleam type annotations (regions, substeps, loop functions), fed by a workflow-wide binding→type fixed-point pass iterated because route-only steps may be written before their producers | Gleam refuses record field access through unannotated parameters (found by the first flagship compile proof), and the same knowledge powers sort comparator selection and optional wrapping |
| 2026-07-10 | emitter | Sort lowers to list.sort with a typed comparator (int/float/string/bool.compare) selected from the key field's type, comparator imports emitted only when used, and combinator arguments restricted to .field accessors (map/filter/sort on anything else refuses) | the spec fixes combinator arguments to accessors or literals, a comparator needs the key's concrete type, and conditional imports keep modules warning-light |
| 2026-07-10 | emitter | Literal indexing (items[0]) lowers to a generated awl_index helper returning Result — each Index node becomes a `use … <- try(awl_index(…))` prelude line with AwlIndexOutOfRange carrying the source position — and indexing inside outcome GUARDS refuses | out-of-range is a step failure carrying the expression's span, Gleam has no list indexing or happy-path exceptions, and a guard prelude would evaluate later arms' indexes eagerly, changing failure semantics |
| 2026-07-10 | emitter | `on failure` lowers as an attempt closure (`let awl_attempt = fn() { … Ok(#(defs…)) }`) whose Ok arm re-binds the body's definitions and continues (outcomes or the region continuation nest inside, the old continuation-nesting discipline) and whose Error arm runs compensation ending in a route | distinguishes step failure from failure-outcome routes, keeps compensation off the success path, and preserves silent-swallowing-is-unwritable |
| 2026-07-10 | emitter | Substeps lower as sibling functions (sub_<parent>_<name>) at depth one in a trailing block: sibling routes are tail calls, a route to a parent outcome ARM fires that arm's route directly (no guard evaluation), the last falling-through substep evaluates the parent's clauses inline, and resolution order matches the spec (sibling, then parent arm, then workflow outcome/step) | matches the substeps_two_stage fixture semantics exactly, with the depth-one/trailing-block/must-carry-outcomes limits recorded refusals keeping the lowering finite |
| 2026-07-10 | emitter | Panel-hardened AWL-0 behaviors carried verbatim: Gleam reserved-word identifiers gain a trailing underscore while wire JSON keys keep AWL spelling, child calls use the SDK string-name spawn (declared-param JSON input, witness fn, encode-only json_value_codec), detached `spawn` uses workflow.spawn with the handle discarded and AwlChildFailed mapping, and the AWL-0 child-retry helper is dropped (rev-2 cannot express child retry) | the task pinned these as panel-hardened, rev-2's declared child params improve on AWL-0, and dead surface is dropped rather than carried untested |
| 2026-07-10 | emitter | The worker name is the task queue: every activity dispatch appends `\|> activity.task_queue("<worker>")` baked from the declaring worker block, retry/timeout/node config resolves call-site override > action declaration, and an action declared on two workers refuses | the spec says "the worker name is the task queue", call-site pinning follows the checker's rules, and duplicate wrapper names would collide |
| 2026-07-10 | emitter | `wait <signal> timeout D` lowers to workflow.with_timeout around workflow.receive (TimedOutError→Ok(None), arrival→Ok(Some(value))), Dir maps to Gleam String, `//!` narration becomes the `////` module doc, and `///` doc lines flow onto generated types, actions, and wrappers | the same SDK seams the old emitter used, adapted to the optional-binding contract, and Dir has no richer Gleam shape in the stopgap |
| 2026-07-10 | emitter | Public API grows emit_in(document, root) beside emit (mirroring check_in): schema imports resolve against the document's directory, rootless emit refuses imports with a spanned error naming the path, and the CLI uses check_in/emit_in/schema_*_in with the file's parent directory | dev_brief imports schemas/brief.schema.json, so emission needs the same root discipline the checker and schema derivation already had |
| 2026-07-10 | integrate | awl_hello.awl migrated to the rev-2 flagship fixture VERBATIM (no `node hello` config lines added), so the generated module pins task_queue("awl_hello") only and dispatches node-unpinned, with the worker's NODE registration reframed in its doc comments as locality metadata | the task required matching the flagship fixture's grammar exactly, and aion-server's worker_matches_node (registry.rs:956) makes an unpinned dispatch match every worker on the queue — the node-registered worker still receives both activities |
| 2026-07-10 | integrate | schemas/output.json hand-written to the emitter's outcome-envelope shape ({"outcome": const, "payload": type-derived object}) while schemas/input.json is taken verbatim from `aion awl schema` (dropping the old hand-added minLength/additionalProperties) | the public derivation surface has no outcome-envelope derivation, and the run now returns the envelope (AwlHelloOutcome codec), so the old bare `{"type":"string"}` output schema was simply wrong for rev-2 |
| 2026-07-10 | integrate | The old-grammar purge keeps every keyword that exists as REFUSAL surface: parser/lexer migration fix-its, the *_gone invalid fixtures, and awl2_defects.rs entries | these are the mechanism proving the old grammar is dead (source-correct fix-its on rejection) — deleting them would delete the regression guard, not the grammar |
| 2026-07-10 | integrate | AWL-EXECUTION-PLAN.md's supersession note scopes itself to the FRONT END and explicitly leaves the AWL-BC (#240) back half standing | this plan's own "After this build" section states AWL-BC proceeds against the rev-2 canonical model, and a blanket "superseded" header would misstate the still-live back half |
| 2026-07-10 | final | examples/README.md rewritten (concise, rev-2-only) rather than deleted, documenting both golden examples with only claims verified live this session and recording the clean-cut corpus deletion | the directory still needs a front door to examples/rev2/ and its schemas |
| 2026-07-10 | final | The old README's Gap 1-3 findings were not carried forward | all three were rev-0-grammar-specific (child-result opacity, one-call-body rule, fmt trailing-space) and preserving them would reintroduce false old-grammar documentation, the exact defect being fixed |
| 2026-07-10 | final | Flake protocol (#243/#244/#248) not invoked: cargo fmt a no-op, clippy `-D warnings` exit 0 on default and --all-features, cargo test --workspace exit 0 — all judged by exit code from logged output | nothing failed, so there was no flake to adjudicate |
| 2026-07-11 | ratified rulings (Tom) | R1 — loop exhaustion must be explicitly named: a step whose body contains a `loop … max …` (fork branches included; a substep answers for its own clauses) MUST declare conditional outcome clauses covering the exhausted case; a loop-carrying step with zero outcome clauses is a CHECK error anchored on the loop. Enforced in `checker/outcomes.rs`; fixture `loop_step_no_outcomes` | the strong reading of the spec's exhaustion rule is law — the permissive fall-through reading let exhaustion end a run indistinguishably from success |
| 2026-07-11 | ratified rulings (Tom) | R2 — `?` is illegal in list-element position: `[T?]` is a CHECK error in every type position (shorthand fields, inputs/signals/outcomes, action/child signatures — one rule at type-reference resolution in `checker/context.rs`), with a diagnostic suggesting `[T]` / `[T]?`; `[T]?` stays legal; the imported-schema projection cannot manufacture the shape (only object properties wrap in `?`, and a null-admitting `items` type is already refused as a null union). Fixtures `list_element_optional` + `action_list_element_optional` | a list has no holes — absent elements are simply not in the list — and schema derivation cannot express element optionality, so the type named nothing any consumer could honor |
| 2026-07-11 | ratified rulings (Tom) | R3 — `route` is illegal inside a `loop` body, statement and pipe-chain `\|> route` terminator alike: refused at CHECK in the flow walk (`checker/walk.rs`) with the span on the route; the emitter's emit-time refusal is kept as a defensive backstop for unchecked documents. Fixtures `route_in_loop_body` + `pipe_route_in_loop_body` | routes connect steps and outcomes — loop exit is `until`/`max` and routing happens in the step's outcome clauses; the refusal is now a language rule, not a stopgap lowering limit |

## Build outcome

**Phases completed:** lexer, parser/printer, checker, emitter (Gleam stopgap),
integration, and final sweep — each hardened by an adversarial fix round whose
rulings are logged above. The committed rev-2 corpus stands at **164 `.awl`
fixtures** (plus sidecars) under `crates/aion-awl/tests/fixtures/rev2/` —
159 at integration, plus the five 2026-07-11 ratified-rulings fixtures.

**Advisory findings carried forward** (from the phase panels; duplicates merged):

- [Lexer] Trailing `///` / `//!` after code on the same line lex as DocLine/DocHeader DATA tokens; the spec only defines whole-line doc lines
- [Lexer] On CRLF input the Newline token's span covers the `\r` byte, not the full line terminator
- [Lexer] Span.column is byte-based, so columns after multibyte content on the SAME line are wrong in character terms
- [Lexer] Implementing report overstates the corpus: "214+ rev2 .awl fixtures" vs 157 actual at that phase
- [Lexer] Multi-level indent jump is silently accepted: the lexer synthesizes phantom intermediate Indent tokens instead of diagnosing over-indentation
- [Parser] Body statements written after outcome clauses parse and are silently reordered above the outcomes on print
- [Parser] Spec-internal conflict on outcome-clause layout resolved unilaterally; the ruling lives only in the fixture MANIFEST, not the build plan's decision log
- [Parser] Call-headed pipe chain gets a generic non-actionable diagnostic
- [Parser] Parser-phase design rulings not appended to the build plan's decision log
- [Checker] A loop-carrying step with zero outcome clauses lets exhaustion fall through indistinguishably from success — the weak reading of the spec's exhaustion rule — RESOLVED 2026-07-11: the strong reading was ratified and is enforced (CHECK error anchored on the loop; ruling row R1)
- [Checker] Dead control flow accepted silently: mid-body unconditional `route` with trailing statements, and outcome clauses that can never fire after a body-terminal route — falls_through() inspects only the LAST statement, so the fall-through graph is computed off a lie
- [Checker] Call-site `node`/`timeout` pin accepted on CHILD calls, where the spec grants the override for worker actions only
- [Checker] `[T?]` (list of optionals) is accepted but incoherent across consumers: schema derivation silently drops the element `?`, and no expression can ever construct the type — RESOLVED 2026-07-11: `[T?]` is refused at CHECK in every type position (ruling row R2)
- [Checker] Two parallel branches binding the same name with different types degrade the binding to Unknown, silencing all downstream type errors
- [Checker] Named-branch fork accepts `join -> name` (spec shows bindless `join` for the named form) and types the binding Unknown, silencing everything downstream
- [Checker] Use-before-definition within one step body produces the misleading cross-path diagnostic "bound on some path but not guaranteed on every path into this step"
- [Checker] `binding |> route <step>` checks clean and silently drops the piped value — spec grants the pipe-route terminator to outcomes only
- [Checker] Payload refusal on a substep's route to a parent outcome arm mislabels the arm as a "step"
- [Checker] Implementing agent's report overstates counts: 101 tests / "52+" valid fixtures claimed; actual is 99 tests and 50 valid fixtures
- [Emitter] The golden flagship example fails `aion awl check`/`emit` in place: no schemas/ directory beside docs/design/aion-authoring/awl/examples/rev2/dev_brief.awl
- [Emitter] Retry mapping semantics unspecified and partly invented: `retry N` → max_attempts: N (N TOTAL attempts, so `retry 2` = one retry) and `backoff D..D` → Exponential with an invented multiplier 2.0
- [Emitter] emit() on an unchecked document silently drops the piped value when a piped route also constructs a payload, instead of returning the documented EmitError
- [Emitter] Orphaned old-grammar fixtures survive the deletion the phase report claims: typed_contract.awl and brief.schema.golden.json are referenced by nothing
- [Emitter] Doc comment for emit_sub_tail is misattached to struct SubChain, stacking two unrelated doc paragraphs on the struct and leaving the function undocumented
- [Critic] Spec Migration section's research_report and typed_contract fixtures deleted without a traceable rev-2 re-expression
- [Critic] Four of the eight decision-log emitter refusal shapes have no committed test — including the one shape the spec explicitly permits
- [Critic] Start-time explicit-null input rejection has no implementation surface or recorded owner in this build

## Risks

- **Emitter DAG mapping** is the largest unknown (arbitrary `after` graphs +
  conditional routes onto the Gleam SDK's structured constructs). Mitigation:
  topological-layer mapping as the default proposal; the implementing agent
  records the chosen mapping and its limits as decision-log entries; compile
  proofs are the gate.
- **Gleam-toolchain load-sensitive flakes** (#248) can red-herring the
  compile proofs — provenance protocol applies.
- **Checker scope creep** — the checker is the product, but route-cycle
  boundedness and flow-typing are bounded to exactly what the spec states;
  anything beyond is a recorded follow-up, not silent scope.

## After this build

Live proof with Tom (deploy + run a rev-2 workflow end-to-end — needs the
queued server/worker swap), then the UX tail in order: `aion new` scaffold,
LSP over the real checker, tree-sitter highlighting, `scaffold-worker`,
`aion build`, visual editor, on-the-fly tier. The wave ladder in
AWL-EXECUTION-PLAN.md is superseded by this plan for the front end; AWL-BC
(#240) proceeds against the rev-2 canonical model.
