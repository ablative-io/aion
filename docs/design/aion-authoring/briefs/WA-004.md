---
type: brief
id: WA-004
cluster: aion-authoring
title: Time-travel debugger over the event-store oplog
---

# WA-004: Time-travel debugger over the event-store oplog

> **Cluster:** aion-authoring
> **Design anchor:**
> - ADR-007 — Design system v2: JSON ledgers above clusters, enrichment in place — Two project ledgers (roadmap.json, decisions.json) above the clusters; stage contracts as first-class schemas inside the aion codegen subset; the brief is one living document — the pipeline appends scout/dev/review per requirement and an execution block per brief, in place, never touching authored fields. Rejected: a sibling runs/ ledger — the brief as a single spec-plus-record document was the original intent, and aion's event history already provides the append-only audit trail.
> **Checklist:**
> - C16 — A run's history is navigable event-by-event in the dashboard, reading the existing event store with no second debug log (CN5).
> - C17 — At each event the debugger shows the workflow-visible state projection and the recorded now() and random() values for that step.
> - C18 — On a NonDeterminismError the debugger surfaces the exact divergent command (expected vs found at the sequence) the resolver already computes.
> - C19 — The debugger offers a what-if re-run from a chosen event with a mocked outcome via the existing aion/testing replay path.
> - C20 — The per-event state projection is exposed by the engine from history and replay, not maintained as a parallel mutable store.
> **Stories:**
> - S9 (Operator, Diagnosing a run) — As an operator, I want to scrub a run event-by-event and see its state and the recorded clock and random values at each step, so that I can understand exactly what the workflow did and why.
> - S10 (Operator, Diagnosing a run) — As an operator, I want a non-determinism failure to point at the exact divergent command, so that the scariest class of durable-execution bug becomes a one-glance explanation instead of a guessing game.

## Purpose

The differentiated showcase (RM-027). The event store is already a complete oplog, deterministic replay already reconstructs exact state, and the resolver already computes expected-vs-found on a non-determinism fault — so a Golem/Temporal-class time-travel debugger is nearly free on the data side. This brief adds the lens, reading the existing history and replay, never a second store (ADR-007, P6).

## Task

Add a debugging lens over what the engine already records. In: an engine surface that exposes, per event, the workflow-visible state projection and the recorded now()/random() values (computed from existing history and replay), and the divergent command on a NonDeterminismError; and a dashboard scrubber that steps a run event-by-event and offers a what-if re-run from a chosen event via the existing aion/testing replay path. Out: any new persistence or debug log, and any change to the event schema.

## Requirements

### R1: Per-event state projection and determinism diff from history

WHEN inspecting a run, THE SYSTEM SHALL expose, per event, the workflow-visible state projection and the recorded now() and random() values, computed from the existing event history and deterministic replay. WHEN a NonDeterminismError exists for the run, THE SYSTEM SHALL expose the divergent command as expected-vs-found at the sequence. THE SYSTEM SHALL NOT maintain a parallel mutable state store.

**Acceptance:**
- A per-event workflow-visible state projection is returned for a recorded run, computed from history and replay with no second store (C20).
- The recorded now() and random() values for each step are exposed alongside the state.
- An injected determinism fault surfaces the exact divergent command (expected vs found at the sequence) the resolver computes.

**Files:**
- create: crates/aion/src/durability/replay_inspect.rs

**Checklist:**
- C16 — A run's history is navigable event-by-event in the dashboard, reading the existing event store with no second debug log (CN5).
- C17 — At each event the debugger shows the workflow-visible state projection and the recorded now() and random() values for that step.
- C18 — On a NonDeterminismError the debugger surfaces the exact divergent command (expected vs found at the sequence) the resolver already computes.
- C20 — The per-event state projection is exposed by the engine from history and replay, not maintained as a parallel mutable store.

**Stories:**
- S9 (Operator, Diagnosing a run) — As an operator, I want to scrub a run event-by-event and see its state and the recorded clock and random values at each step, so that I can understand exactly what the workflow did and why.
- S10 (Operator, Diagnosing a run) — As an operator, I want a non-determinism failure to point at the exact divergent command, so that the scariest class of durable-execution bug becomes a one-glance explanation instead of a guessing game.

### R2: Dashboard scrubber and what-if re-run

WHEN an operator opens a run in the dashboard, THE SYSTEM SHALL let them step the run event-by-event and run a what-if re-run from a chosen event with a mocked outcome via the existing aion/testing replay path.

**Acceptance:**
- The scrubber steps a recorded run event-by-event, showing the projected state at each step.
- A what-if re-run forks from a chosen event with a mocked outcome and reports the resulting path, using the existing replay harness.
- The scrubber reads the existing event history; no debug-only log is introduced (C16).

**Files:**
- create: crates/aion-dashboard/src/timetravel/

**Checklist:**
- C19 — The debugger offers a what-if re-run from a chosen event with a mocked outcome via the existing aion/testing replay path.

**Stories:**
- S9 (Operator, Diagnosing a run) — As an operator, I want to scrub a run event-by-event and see its state and the recorded clock and random values at each step, so that I can understand exactly what the workflow did and why.

## Boundaries

- SHALL NOT introduce a parallel debug log or state store — time-travel reads existing history and replay (ADR-007).
- SHALL NOT change the Event schema or the recorded payloads.
- SHALL NOT overlap with RM-007's per-run event timeline list view beyond consuming the same history.

## Verification

- Validated against a recorded run and an injected determinism fault.
- CN5 asserted: no second source of truth — projections come from history and replay.
- clippy -D warnings and fmt clean.
