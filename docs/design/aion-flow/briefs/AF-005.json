{
  "id": "AF-005",
  "cluster": "aion-flow",
  "title": "Workflow core: run, now, random, and the typed entry contract",
  "depends_on": ["AF-004"],
  "blocked_by": [],
  "checklist": ["C17", "C18", "C19", "C20", "C21"],
  "stories": ["S2", "S3", "S13"],
  "purpose": "Define the heart of the author surface: workflow.run (the single recorded activity dispatch), the determinism primitives now/random bound to AD, and workflow.define (the typed entry contract the .aion manifest consumes). Establish that there is no side-effect escape hatch other than run.",
  "task": "Create the aion/workflow module (declarations + re-exports) and the workflow/run.gleam and workflow/define.gleam submodules. Implement workflow.run as the single recorded activity dispatch via the ffi binding, encoding the input and decoding the typed output or a typed ActivityError. Bind workflow.now and workflow.random to AD's determinism context via ffi, with no wall-clock or entropy alternative. Define workflow.define for the typed entry contract.",
  "requirements": [
    {
      "id": "R1",
      "title": "workflow.run — the single recorded dispatch",
      "spec": "THE SYSTEM SHALL define workflow.run(Activity(i, o)) -> Result(o, ActivityError) as the single recorded activity dispatch, binding to the engine via the ffi layer, encoding the activity input and decoding the typed output, with no generic side-effect escape hatch.",
      "acceptance": [
        "workflow/run.gleam defines run(Activity(i, o)) -> Result(o, ActivityError); it encodes the input via the activity's Codec, calls the ffi activity-dispatch binding (which records via AD and returns from history on replay), and decodes the result with the output Codec.",
        "A decode failure on the returned payload surfaces as a typed error (the engine-originated decode variant from AF-003), never a panic.",
        "No workflow.side_effect(fn) or equivalent is provided; a doc comment states that the only recorded side-effect path is run (and the concurrency combinators over activities) per CO7.",
        "A harness-backed test (using AF-011's mock once available, or a stub binding) asserts run returns the decoded typed result on success and the typed ActivityError on failure."
      ],
      "files": {
        "create": ["gleam/aion_flow/src/aion/workflow.gleam", "gleam/aion_flow/src/aion/workflow/run.gleam"],
        "modify": ["gleam/aion_flow/src/aion/internal/ffi.gleam"]
      },
      "checklist": ["C17", "C20"],
      "stories": ["S2", "S13"]
    },
    {
      "id": "R2",
      "title": "Determinism: now and random",
      "spec": "THE SYSTEM SHALL define workflow.now() -> Timestamp and workflow.random() -> Float (plus random_int) bound to AD's determinism context via the ffi layer, exposing NO wall-clock or entropy-source binding.",
      "acceptance": [
        "workflow.now() returns the recorded event timestamp from AD's determinism context (via the ffi binding), documented as the only time source available to workflow code.",
        "workflow.random() -> Float and workflow.random_int(min, max) -> Int draw from AD's seeded RNG (keyed on WorkflowId + RunId), via the ffi binding.",
        "The SDK exposes no binding to a wall clock (gleam stdlib / erlang:now) or entropy source; a doc comment states the prohibition on calling them from workflow code (CO6).",
        "A test asserts now and random return values via the binding (using the harness's deterministic stub) and that repeated runs under the same seed produce the same random sequence."
      ],
      "files": {
        "create": [],
        "modify": ["gleam/aion_flow/src/aion/workflow/run.gleam", "gleam/aion_flow/src/aion/internal/ffi.gleam"]
      },
      "checklist": ["C18", "C19"],
      "stories": ["S3"]
    },
    {
      "id": "R3",
      "title": "workflow.define — typed entry contract",
      "spec": "THE SYSTEM SHALL define workflow.define(name, input_codec, output_codec, error_codec, entry_fn) returning an opaque WorkflowDefinition(input, output, error) value carrying the name, the three Codecs, and the entry function, which the engine invokes at spawn to bridge the typed entry to the type-erased boundary.",
      "acceptance": [
        "workflow/define.gleam defines an opaque type WorkflowDefinition(input, output, error) and define(name: String, input_codec: Codec(input), output_codec: Codec(output), error_codec: Codec(error), entry_fn: fn(input) -> Result(output, error)) -> WorkflowDefinition(input, output, error).",
        "A doc comment states the AF/AE/AP contract precisely: the engine (AE) consumes the WorkflowDefinition at spawn — it calls the package's entry function (named in the manifest), receives this value, and uses the codecs to decode the input Payload and encode the result/error Payload; aion-package (AP) records only the entry module and function NAMES (strings) in the manifest and does not introspect this Gleam value.",
        "The WorkflowDefinition carries enough to bridge the typed entry to the type-erased ffi boundary (decode input on entry via input_codec, encode output via output_codec / error via error_codec on completion); the SDK does not invoke entry_fn itself (AE spawns the process and drives it).",
        "A test constructs a WorkflowDefinition for a concrete typed workflow function and asserts the name and the three codecs are carried and recoverable."
      ],
      "files": {
        "create": ["gleam/aion_flow/src/aion/workflow/define.gleam"],
        "modify": ["gleam/aion_flow/src/aion/workflow.gleam"]
      },
      "checklist": ["C21"],
      "stories": ["S13"]
    }
  ],
  "boundaries": [
    "SHALL NOT implement the engine-side activity dispatch, recording, replay, or determinism context — those are AE/AD. This brief binds to them via ffi.",
    "SHALL NOT implement timers, signals, queries, children, or concurrency — those are AF-006 through AF-010.",
    "SHALL NOT build the .aion archive — that is AP; this brief provides the typed entry contract it consumes."
  ],
  "verification": [
    "gleam build passes.",
    "gleam check passes.",
    "gleam format --check passes.",
    "gleam test passes for run, determinism, and define tests.",
    "No wall-clock or entropy binding is exposed; no side_effect escape hatch; no panic/todo in new code."
  ]
}
