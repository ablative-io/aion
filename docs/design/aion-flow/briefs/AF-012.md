---
type: brief
id: AF-012
cluster: aion-flow
title: Type-safety verification and the end-to-end example workflow
---

# AF-012: Type-safety verification and the end-to-end example workflow

> **Cluster:** aion-flow
> **Depends on:** AF-011
> **Checklist:**
> - C35 — Activity, SignalRef, query handler, and ChildHandle types are static type parameters; representative invalid compositions (wrong activity input type, wrong signal payload type, wrong query return type) are documented as compile-fail and verified to be rejected by gleam build.
> - C36 — An end-to-end typed example workflow exercises run/now/sleep/receive/spawn/all and compiles, demonstrating the full surface composes with static types.
> **Stories:**
> - S1 (Workflow Author, Writing Durable Workflows in Gleam) — As a workflow author, I want activity inputs and outputs to be statically typed so that passing the wrong value to an activity is a compile error, not a runtime failure three days into a long-running workflow.

## Purpose

Prove the cluster's defining property — compile-time type safety — and demonstrate the whole surface composing. Assert representative invalid compositions are rejected by gleam build, and write a typed end-to-end example workflow exercising run/now/sleep/receive/spawn/all that compiles and passes under the harness.

## Task

Add type-safety verification: document representative invalid compositions (wrong activity input type, wrong signal payload type, wrong query return type) as compile-fail expectations and verify they are rejected by gleam build (e.g. via a documented compile-fail fixture set or an inline negative-test approach). Write an end-to-end typed example workflow that uses run, now, sleep, receive, spawn, and all, and a harness test that drives it to a typed result.

## Requirements

### R1: Compile-time type-safety verification

THE SYSTEM SHALL document representative invalid compositions as compile-fail expectations and verify they are rejected by gleam build: a wrong activity input type, a wrong signal payload type, and a wrong query return type.

**Acceptance:**
- At least three invalid compositions are captured as standalone negative fixtures under test/compile_fail/ (each a complete .gleam snippet that must FAIL to type-check), kept out of the package build so the package itself still compiles: (a) passing a value of the wrong type as an activity input, (b) receiving a SignalRef(a) into a context expecting payload b, (c) a query handler whose returned type does not match its registered type.
- A committed checker script (test/compile_fail/check.sh) drives the verification: for each negative fixture it builds an ephemeral throwaway Gleam project depending on aion_flow, runs gleam check, and asserts a non-zero exit whose output contains the documented type-mismatch (the expected error substring is recorded alongside each fixture); for each fixture it also checks that the positive counterpart type-checks clean.
- The approach is reproducible by a reviewer running check.sh — the static guarantee is demonstrated by an actual failing gleam check, not merely asserted in prose.

**Files:**
- create: gleam/aion_flow/test/aion_flow_test.gleam
- create: gleam/aion_flow/test/compile_fail/check.sh

**Checklist:**
- C35 — Activity, SignalRef, query handler, and ChildHandle types are static type parameters; representative invalid compositions (wrong activity input type, wrong signal payload type, wrong query return type) are documented as compile-fail and verified to be rejected by gleam build.

**Stories:**
- S1 (Workflow Author, Writing Durable Workflows in Gleam) — As a workflow author, I want activity inputs and outputs to be statically typed so that passing the wrong value to an activity is a compile error, not a runtime failure three days into a long-running workflow.

### R2: End-to-end typed example workflow

THE SYSTEM SHALL provide an end-to-end typed example workflow that exercises run, now, sleep, receive, spawn, and all, compiling and passing under the test harness.

**Acceptance:**
- The example is a plain Gleam workflow function fn(input) -> Result(output, error) registered with workflow.define, using typed activities (run), workflow.now, a sleep, a typed receive, a child spawn, and an all fan-out — demonstrating the full surface composes with static types.
- A harness test (AF-011) drives the example: mocks its activities and child, advances the clock past its sleep, sends its signal, and asserts the typed output.
- The example doubles as the canonical documentation snippet for the package (referenced from aion_flow.gleam's module doc).

**Files:**
- modify: gleam/aion_flow/test/aion_flow_test.gleam
- modify: gleam/aion_flow/src/aion_flow.gleam

**Checklist:**
- C36 — An end-to-end typed example workflow exercises run/now/sleep/receive/spawn/all and compiles, demonstrating the full surface composes with static types.

**Stories:**
- S1 (Workflow Author, Writing Durable Workflows in Gleam) — As a workflow author, I want activity inputs and outputs to be statically typed so that passing the wrong value to an activity is a compile error, not a runtime failure three days into a long-running workflow.

## Boundaries

- SHALL NOT add new primitives — this brief verifies and demonstrates the surface built in AF-001 through AF-011.
- SHALL NOT require a running engine — the example runs under the pure-Gleam harness.
- SHALL NOT weaken any type signature to make the example compile; if a composition needs a type that does not exist, that is a gap to raise, not to paper over.

## Verification

- gleam build passes for the package and the positive example.
- gleam check passes.
- gleam format --check passes.
- gleam test passes for the end-to-end example under the harness.
- The documented invalid compositions are confirmed rejected by gleam build; the positive counterparts compile.
- No panic/todo; no type signature weakened to force compilation.
