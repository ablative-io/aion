# Remediation briefs and design records

Working design briefs and decision records produced during the 2026-06 production-hardening remediation. These are narrative working documents — the formal design system (JSON source of truth, rendered markdown, coverage tooling) lives under `docs/design/` and is unchanged by anything here. When a brief's decisions harden into permanent contract, they belong in `docs/design/` / `CLIENT-CONTRACT.md`; these files record how and why the decisions were made.

| Brief | Task | Status |
|---|---|---|
| [websocket-resume.md](websocket-resume.md) | #37 | Server splice landed (`ff9061ec`, review APPROVE; findings closed in `42fe0e91`, verified): resume, required `event_broadcast_capacity`, namespace gate, real filtered selectors, contiguity tripwire. Remaining: Rust client wave + live conformance closeout. |
| [schedule-namespace-gating.md](schedule-namespace-gating.md) | #32/#33 | Implemented and committed (`9f554017`). Design record. |
| [worker-reconnect-policy.md](worker-reconnect-policy.md) | #46 | CLOSED (`7453a6d4` → `1abc4aa4` → `9f51e466` → `8e60eadb`, final verification APPROVE): budget reset on healthy serving, clean close budgeted, shutdown wins promptly at every phase in all three SDKs. Protocol drain signal lives in [worker-protocol-acks.md](worker-protocol-acks.md). |
| [query-execution.md](query-execution.md) | #45 | Waves 1/2a/2b/3 committed (`694b197b`, `4d39ba88`, `98fcb6f3`, `2e7c2660`+`e845a212`, Wave 3 review findings closed): queries live end-to-end with determinism proofs, `query_failed` wire code, required `query_timeout`. #58 Wave D extended the e2e matrix to every yield-point family — query-while-parked plus queried crash/replay byte-identity proofs at signal, sleep, activity, child-await, and collect — all ungated (the historical live-query re-entry VM crash no longer reproduces); the Gleam-SDK batch-orchestrator example e2e is gated behind the `beamr_query_reentry_fixed` cargo feature on unrelated beamr 0.4.9 json/stdlib decode gaps. Remaining: Wave E combined closeout review. |
| [child-await-two-phase.md](child-await-two-phase.md) | #58 (+#56 ✓, #55 ✓) | Waves A (`682f6356`) + B (`e0512be0`) + C committed: D3 skip-tolerant cursor, two-phase await_child + collect_*, child-terminal watcher, record-then-spawn (#56 closed), CAN transparency, D4 envelope, >10-parents thread-pinning proof. Wave D done: SDK `collect_all`/`collect_race` pump-wrapped with tests, dead `cancelled:`/`non_determinism:` child/query error arms deleted and the SDK query taxonomy aligned to the engine (`QueryTimedOut`/`QueryNotRunning`/`QueryHandlerFailed`), `activity_gated` query fixture + per-family queried crash/replay e2e, gated example e2e behind `beamr_query_reentry_fixed` (note: batch-orchestrator `.aion` archives are local build artifacts — any built before the `register_query/2` arity change must be rebuilt or every run fails at registration). Remaining: Wave E review. |
| [worker-protocol-acks.md](worker-protocol-acks.md) | #39/#47/#59 | IMPLEMENTED (one coordinated wire break, all three SDKs): `RegisterAck` (worker_id/namespace/heartbeat_window_ms) is the guaranteed first response frame and the registration-success signal — no nack, denial taxonomy unchanged; per-result `ResultAck` makes the unacked trackers ack-driven (replay decays to the unacked residue); `ActivityTask.attempt` stamped by the engine seam (`ActivityDispatcher` trait replaced, `FIRST_DELIVERY_ATTEMPT = 1` producer constant), `attempt == 0` rejected by all consumers, `WIRE_DEFAULT_ATTEMPT` deleted from all three SDKs; drain frame re-classified per D9/D10/D11 — unbudgeted latched drain drop, redial at `initial_backoff`, cross-SDK shutdown outcome aligned (pending error-class drop surfaces; drain/clean-close exits clean). Ack-wait + per-send deadlines derive from `reconnect.max_backoff`; shutdown interrupts `re_report_unacked` in all three SDKs. D12 (#59 pair keying) verified already landed with regression test. |
| [workflow-packaging.md](workflow-packaging.md) | #61 | COMPLETE. All 12 decisions adopted (2026-06-11, D-10 = `workflow.toml`). W1 library (`489be454`), W2 CLI + seven packagers deleted + docs (`afe5a90c`, archives byte-identical to the old packagers'), W3 Fable review CHANGES REQUIRED → fixed + re-verified (`4f2ea686`): `workflow.toml` paths lexically normalized and confined to the project root (typed `PathEscapesRoot`; output conflicts detected on normalized paths; write failures carry the path via `OutputWrite`; `--out`/`output_override` caller-owned and exempt). Follow-on: #62. |
| [reload-server-endpoint.md](reload-server-endpoint.md) | #62 follow-up (D3) | IMPLEMENTED. Decisions D1–D10 signed off by Tom 2026-06-12, recommendation (a) in every case. Exposes the landed reload seam as an operator deploy API: `deploy` JWT claim + dark-by-default `[deploy]` config gate, separate `DeployService` + `/deploy/*` via shared handlers, unary upload with required explicit `max_archive_bytes` (and, per D11 security rider, required `max_inflated_bytes` enforced as a running inflate budget in aion-package), engine-global scope (per-namespace engines verified nonexistent in code), new `deploy_denied`/`version_pinned` wire codes, engine riders for typed refusals + `LoadOutcome` + the same-hash-different-manifest tripwire (banked #62 finding), `aion-cli deploy/versions/route/unload`. CLIENT-CONTRACT and worker protocol untouched. |
| [runtime-package-reload.md](runtime-package-reload.md) | #62 | Designed; decisions D1–D4 ADOPTED (Tom, 2026-06-12, all as recommended): latest-at-record-time recorded durably for children + CAN; manual `unload_workflow_version` with engine-enforced safety checks; embedded-only API now (server endpoint = follow-up brief with deploy authz); required `package_version` event field, `single_loaded` deleted, D4 = **no migration** — existing stores are dev/test and will be wiped. Unblocks Meridian P8/C27 (live reload seam). Key finding: durable history records **no** package version today — pinning is in-memory only and recovery refuses multi-version state — so Wave 0 (durable version pinning) is a hard prerequisite. Implementation queued after the #58/#45 closeout. |
