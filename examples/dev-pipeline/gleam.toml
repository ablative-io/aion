name = "aion_dev_pipeline"
version = "0.1.0"
target = "erlang"
description = "Aion dev-pipeline workflow package, slice 1: brief_forge — task in, grounded + refuted + dispatchable brief out (scout → design ⇄ refute loop, workflow-stamped acceptance)."
licences = ["MIT"]

[dependencies]
aion_flow = { path = "../../gleam/aion_flow" }
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_json = ">= 2.0.0 and < 4.0.0"
