name = "agent_dev"
version = "0.1.0"
target = "erlang"
description = "Aion agent-dev workflow: a norn-harnessed dev pipeline — provision, scout, a bounded dev<->review loop, a bounded gate loop, land on Passed."
licences = ["MIT"]

[dependencies]
aion_flow = { path = "../../gleam/aion_flow" }
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_json = ">= 2.0.0 and < 4.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
