name = "aion_approval_gate"
version = "0.1.0"
target = "erlang"
description = "Aion approval-gate workflow example with signals and durable timeouts."
licences = ["MIT"]

[dependencies]
aion_flow = { path = "../../gleam/aion_flow" }
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_json = ">= 2.0.0 and < 4.0.0"
