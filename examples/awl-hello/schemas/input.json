{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Greet a name, then shout it — the first workflow written in AWL and run for real.",
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": { "type": "string" }
  }
}
